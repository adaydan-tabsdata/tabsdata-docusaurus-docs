---
title: "tabsdata.AzureDestination"
sidebarTitle: "tabsdata.AzureDestination"
description: "tabsdata.AzureDestination API reference."
---
import { ParamField, ResponseField } from '@site/src/components/ApiField';


<a id="tabsdata.AzureDestination"></a>
<details>
<summary>AzureDestination</summary>


```python
class AzureDestination(
    uri: str | List [ str ],
    credentials: dict | AzureCredentials,
    format: str | dict | FileFormat = None,
)
```

[View source ↗](https://github.com/tabsdata/tabsdata/blob/main/client/td-sdk/tabsdata/io/output.py#L425)

Bases: `Output`

Class for managing the configuration of Azure-file-based data outputs.

<a id="tabsdata.AzureDestination.to_dict"></a>
#### `to_dict`

```python
def to_dict()
```

[View source ↗](https://github.com/tabsdata/tabsdata/blob/main/client/td-sdk/tabsdata/io/output.py#L534)

Converts the AzureDestination object to a dictionary.

<a id="tabsdata.AzureDestination.__init__"></a>
#### `__init__`

```python
def __init__(
    uri: str | List [ str ],
    credentials: dict | AzureCredentials,
    format: str | dict | FileFormat = None,
)
```

[View source ↗](https://github.com/tabsdata/tabsdata/blob/main/client/td-sdk/tabsdata/io/output.py#L455)

**Parameters:**

<ParamField path="uri" type="str | List [ str ]" required>
  The URI of the files to export with format: ‘az://path/to/files’. It can be a single URI or a list of URIs.
</ParamField>

<ParamField path="credentials" type="dict | AzureCredentials" required>
  The credentials required to access Azure. Can be a dictionary or a AzureCredentials object.
</ParamField>

<ParamField path="format" type="str | dict | FileFormat" default="None">
  The format of the file. If not provided, it will be inferred from the file extension. Can be either a string with the format, a FileFormat object or a dictionary with the format as the ‘type’ key and any additional format-specific information. Currently supported formats are ‘csv’, ‘parquet’, ‘ndjson’ and ‘jsonl’.
</ParamField>

**Raises:**

<ResponseField name="OutputConfigurationError" type="Exception"></ResponseField>

<ResponseField name="FormatConfigurationError" type="Exception"></ResponseField>

<a id="tabsdata.AzureDestination.format"></a>
<ResponseField name="format" type="FileFormat">
The format of the file to be created. If not provided, it will be inferred from the file extension.
</ResponseField>

<a id="tabsdata.AzureDestination.uri"></a>
<ResponseField name="uri" type="str | List[ str ]">
The URI of the files with format: ‘az://path/to/files’. It can be a single URI or a list of URIs.
</ResponseField>

<a id="tabsdata.AzureDestination.credentials"></a>
<ResponseField name="credentials" type="AzureCredentials">
The credentials required to access Azure.
</ResponseField>

#### Methods

#### Attributes


</details>
