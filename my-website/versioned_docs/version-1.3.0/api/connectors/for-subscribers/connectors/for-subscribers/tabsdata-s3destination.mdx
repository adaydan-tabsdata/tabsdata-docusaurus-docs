---
title: "tabsdata.S3Destination"
sidebarTitle: "tabsdata.S3Destination"
description: "Bases: DestinationPlugin"
---
import { ParamField, ResponseField } from '@site/src/components/ApiField';


<a id="tabsdata.S3Destination"></a>
<details>
<summary>S3Destination</summary>


```python
class S3Destination(
    uri: str | list [ str ],
    credentials: S3Credentials,
    format: str | FileFormat = None,
    region: str = None,
    catalog: AWSGlue = None,
)
```

[View source ↗](https://github.com/tabsdata/tabsdata/blob/main/client/td-sdk/tabsdata/_io/outputs/file_outputs.py#L1002)

Bases: [`DestinationPlugin`](../plugins/tabsdata.DestinationPlugin.html#tabsdata.DestinationPlugin)

Class for managing the configuration of S3-file-based data outputs.

<a id="tabsdata.S3Destination.__init__"></a>
#### `__init__`

```python
def __init__(
    uri: str | list [ str ],
    credentials: S3Credentials,
    format: str | FileFormat = None,
    region: str = None,
    catalog: AWSGlue = None,
)
```

[View source ↗](https://github.com/tabsdata/tabsdata/blob/main/client/td-sdk/tabsdata/_io/outputs/file_outputs.py#L1024)

**Parameters:**

<ParamField path="uri" type="str | list [ str ]" required>
  The URI of the files with format: ‘s3://path/to/files’. It can be a single URI or a list of URIs.
</ParamField>

<ParamField path="credentials" type="S3Credentials" required>
  The credentials required to access the S3 bucket. Must be a S3Credentials object.
</ParamField>

<ParamField path="format" type="str | FileFormat" default="None">
  The format of the file. If not provided, it will be inferred from the file extension of the data. Can be either a string with the format or a FileFormat object. Currently supported formats are ‘csv’, ‘parquet’, ‘ndjson’ and ‘jsonl’.
</ParamField>

<ParamField path="region" type="str" default="None">
  The region where the S3 bucket is located. If not provided, the default AWS region will be used.
</ParamField>

**Raises:**

<ResponseField name="OutputConfigurationError" type="Exception"></ResponseField>

<ResponseField name="FormatConfigurationError" type="Exception"></ResponseField>

<a id="tabsdata.S3Destination.format"></a>
<ResponseField name="format" type="FileFormat">
The format of the file. If not provided, it will be inferred from the file extension.
</ResponseField>

<a id="tabsdata.S3Destination.uri"></a>
<ResponseField name="uri" type="str | list [ str ]">
The URI of the files with format: ‘s3://path/to/files’. It can be a single URI or a list of URIs.
</ResponseField>

<a id="tabsdata.S3Destination.credentials"></a>
<ResponseField name="credentials" type="S3Credentials">
The credentials required to access the S3 bucket.
</ResponseField>

#### Methods

#### Attributes


</details>
