---
title: "File Formats"
sidebarTitle: "File Formats"
description: "File Formats API reference."
---
import { ParamField, ResponseField } from '@site/src/components/ApiField';


## File Format

<a id="tabsdata.format.FileFormat"></a>
<details>
<summary>FileFormat</summary>


```python
class FileFormat()
```

[View source ↗](https://github.com/tabsdata/tabsdata/tree/main/client/td-sdk/tabsdata/format.py#L18)

Bases: [`ABC`](https://docs.python.org/3/library/abc.html#abc.ABC)

The class of the different possible formats for files.


</details>

## CSV Format

<a id="tabsdata.format.CSVFormat"></a>
<details>
<summary>CSVFormat</summary>


```python
class CSVFormat(
    separator: str | int = ',',
    quote_char: str | int = '"',
    eol_char: str | int = '\n',
    input_encoding: str = 'Utf8',
    input_null_values: list | None = None,
    input_missing_is_null: bool = True,
    input_truncate_ragged_lines: bool = False,
    input_comment_prefix: str | int | None = None,
    input_try_parse_dates: bool = False,
    input_decimal_comma: bool = False,
    input_has_header: bool = True,
    input_skip_rows: int = 0,
    input_skip_rows_after_header: int = 0,
    input_raise_if_empty: bool = True,
    input_ignore_errors: bool = False,
    output_include_header: bool = True,
    output_datetime_format: str | None = None,
    output_date_format: str | None = None,
    output_time_format: str | None = None,
    output_float_scientific: bool | None = None,
    output_float_precision: int | None = None,
    output_null_value: str | None = None,
    output_quote_style: str | None = None,
    output_maintain_order: bool = True,
)
```

[View source ↗](https://github.com/tabsdata/tabsdata/tree/main/client/td-sdk/tabsdata/format.py#L67)

Bases: [`FileFormat`](#tabsdata.format.FileFormat)

The class of the CSV file format.

<a id="tabsdata.format.CSVFormat.separator"></a>
<ResponseField name="separator" type="str | int">
The separator of the CSV file.
</ResponseField>

<a id="tabsdata.format.CSVFormat.quote_char"></a>
<ResponseField name="quote_char" type="str | int">
The quote character of the CSV file.
</ResponseField>

<a id="tabsdata.format.CSVFormat.eol_char"></a>
<ResponseField name="eol_char" type="str | int">
The end of line character of the CSV file.
</ResponseField>

<a id="tabsdata.format.CSVFormat.input_encoding"></a>
<ResponseField name="input_encoding" type="str">
The encoding of the CSV file. Only used when importing data.
</ResponseField>

<a id="tabsdata.format.CSVFormat.input_null_values"></a>
<ResponseField name="input_null_values" type="list | None">
The null values of the CSV file. Only used when importing data.
</ResponseField>

<a id="tabsdata.format.CSVFormat.input_missing_is_null"></a>
<ResponseField name="input_missing_is_null" type="bool">
Whether missing values should be marked as null. Only used when importing data.
</ResponseField>

<a id="tabsdata.format.CSVFormat.input_truncate_ragged_lines"></a>
<ResponseField name="input_truncate_ragged_lines" type="bool">
Whether to truncate ragged lines of the CSV file. Only used when importing data.
</ResponseField>

<a id="tabsdata.format.CSVFormat.input_comment_prefix"></a>
<ResponseField name="input_comment_prefix" type="str | int | None">
The comment prefix of the CSV file. Only used when importing data.
</ResponseField>

<a id="tabsdata.format.CSVFormat.input_try_parse_dates"></a>
<ResponseField name="input_try_parse_dates" type="bool">
Whether to try parse dates of the CSV file. Only used when importing data.
</ResponseField>

<a id="tabsdata.format.CSVFormat.input_decimal_comma"></a>
<ResponseField name="input_decimal_comma" type="bool">
Whether the CSV file uses decimal comma. Only used when importing data.
</ResponseField>

<a id="tabsdata.format.CSVFormat.input_has_header"></a>
<ResponseField name="input_has_header" type="bool">
If the CSV file has header. Only used when importing data.
</ResponseField>

<a id="tabsdata.format.CSVFormat.input_skip_rows"></a>
<ResponseField name="input_skip_rows" type="int">
How many rows should be skipped in the CSV file. Only used when importing data.
</ResponseField>

<a id="tabsdata.format.CSVFormat.input_skip_rows_after_header"></a>
<ResponseField name="input_skip_rows_after_header" type="int">
How many rows should be skipped after the header in the CSV file. Only used when importing data.
</ResponseField>

<a id="tabsdata.format.CSVFormat.input_raise_if_empty"></a>
<ResponseField name="input_raise_if_empty" type="bool">
If an error should be raised for an empty CSV. Only used when importing data.
</ResponseField>

<a id="tabsdata.format.CSVFormat.input_ignore_errors"></a>
<ResponseField name="input_ignore_errors" type="bool">
If the errors loading the CSV must be ignored. Only used when importing data.
</ResponseField>

<a id="tabsdata.format.CSVFormat.output_include_header"></a>
<ResponseField name="output_include_header" type="bool">
Whether to include header in the CSV output. Only used when exporting data.
</ResponseField>

<a id="tabsdata.format.CSVFormat.output_datetime_format"></a>
<ResponseField name="output_datetime_format" type="str | None">
A format string, with the specifiers defined by the chrono Rust crate. If no format specified, the default fractional-second precision is inferred from the maximum timeunit found in the frame’s Datetime cols (if any). Only used when exporting data.
</ResponseField>

<a id="tabsdata.format.CSVFormat.output_date_format"></a>
<ResponseField name="output_date_format" type="str | None">
A format string, with the specifiers defined by the chrono Rust crate. Only used when exporting data.
</ResponseField>

<a id="tabsdata.format.CSVFormat.output_time_format"></a>
<ResponseField name="output_time_format" type="str | None">
A format string, with the specifiers defined by the chrono Rust crate. Only used when exporting data.
</ResponseField>

<a id="tabsdata.format.CSVFormat.output_float_scientific"></a>
<ResponseField name="output_float_scientific" type="bool | None">
Whether to use scientific form always (true), never (false), or automatically (None). Only used when exporting data.
</ResponseField>

<a id="tabsdata.format.CSVFormat.output_float_precision"></a>
<ResponseField name="output_float_precision" type="int | None">
Number of decimal places to write. Only used when exporting data.
</ResponseField>

<a id="tabsdata.format.CSVFormat.output_null_value"></a>
<ResponseField name="output_null_value" type="str | None">
A string representing null values (defaulting to the empty string). Only used when exporting data.
</ResponseField>

<a id="tabsdata.format.CSVFormat.output_quote_style"></a>
<ResponseField name="output_quote_style" type="str | None">
Determines the quoting strategy used. Only used when exporting data. * necessary (default): This puts quotes around fields only when necessary. They are necessary when fields contain a quote, separator or record terminator. Quotes are also necessary when writing an empty record (which is indistinguishable from a record with one empty field). This is the default. * always: This puts quotes around every field. Always. * never: This never puts quotes around fields, even if that results in invalid CSV data (e.g.: by not quoting strings containing the separator). * non_numeric: This puts quotes around all fields that are non-numeric. Namely, when writing a field that does not parse as a valid float or integer, then quotes will be used even if they aren`t strictly necessary.
</ResponseField>

<a id="tabsdata.format.CSVFormat.output_maintain_order"></a>
<ResponseField name="output_maintain_order" type="bool">
Maintain the order in which data is processed. Setting this to False will be slightly faster. Only used when exporting data.
</ResponseField>


</details>

## NDJSON Format

<a id="tabsdata.format.NDJSONFormat"></a>
<details>
<summary>NDJSONFormat</summary>


```python
class NDJSONFormat()
```

[View source ↗](https://github.com/tabsdata/tabsdata/tree/main/client/td-sdk/tabsdata/format.py#L415)

Bases: [`FileFormat`](#tabsdata.format.FileFormat)

The class of the log file format.


</details>

## Log Format

<a id="tabsdata.format.LogFormat"></a>
<details>
<summary>LogFormat</summary>


```python
class LogFormat()
```

[View source ↗](https://github.com/tabsdata/tabsdata/tree/main/client/td-sdk/tabsdata/format.py#L430)

Bases: [`FileFormat`](#tabsdata.format.FileFormat)

The class of the log file format.


</details>

## Parquet Format

<a id="tabsdata.format.ParquetFormat"></a>
<details>
<summary>ParquetFormat</summary>


```python
class ParquetFormat()
```

[View source ↗](https://github.com/tabsdata/tabsdata/tree/main/client/td-sdk/tabsdata/format.py#L445)

Bases: [`FileFormat`](#tabsdata.format.FileFormat)

The class of the Parquet file format.


</details>
