---
title: "tabsdata.AzureSource"
sidebarTitle: "tabsdata.AzureSource"
description: "tabsdata.AzureSource API reference."
---
import { ParamField, ResponseField } from '@site/src/components/ApiField';


<a id="tabsdata.AzureSource"></a>
<details>
<summary>AzureSource</summary>


```python
class AzureSource(
    uri: str | list [ str ],
    credentials: AzureCredentials,
    format: str | FileFormat = None,
    initial_last_modified: str | datetime = None,
)
```

Bases: [`SourcePlugin`](../plugins/tabsdata.SourcePlugin.html#tabsdata.SourcePlugin)

Class for managing the configuration of Azure-file-based data inputs.

<a id="tabsdata.AzureSource.__init__"></a>
#### `__init__`

```python
def __init__(
    uri: str | list [ str ],
    credentials: AzureCredentials,
    format: str | FileFormat = None,
    initial_last_modified: str | datetime = None,
)
```

**Parameters:**

<ParamField path="uri" type="str | list [ str ]" required>
  The URI of the files with format: ‘az://path/to/files’. It can be a single URI or a list of URIs.
</ParamField>

<ParamField path="credentials" type="AzureCredentials" required>
  The credentials required to access Azure. Must be an AzureCredentials object.
</ParamField>

<ParamField path="format" type="str | FileFormat" default="None">
  The format of the file. If not provided, it will be inferred from the file extension of the data. Can be either a string with the format or a FileFormat object . Currently supported formats are ‘csv’, ‘parquet’, ‘avro’, ‘ndjson’, ‘jsonl’ and ‘log’.
</ParamField>

<ParamField path="initial_last_modified" type="str | datetime" default="None">
  If provided, only the files modified after this date and time will be considered. The date and time can be provided as a string in [ISO 8601 format](https://en.wikipedia.org/wiki/ISO_8601) or as a datetime object. If no timezone is provided, UTC will be assumed.
</ParamField>

**Raises:**

<ResponseField name="InputConfigurationError" type="Exception"></ResponseField>

<ResponseField name="FormatConfigurationError" type="Exception"></ResponseField>

<a id="tabsdata.AzureSource.format"></a>
<ResponseField name="format" type="FileFormat">
The format of the file. If not provided, it will be inferred from the file extension of the data.
</ResponseField>

<a id="tabsdata.AzureSource.uri"></a>
<ResponseField name="uri" type="str | list [ str ]">
The URI of the files with format: ‘az://path/to/files’. It can be a single URI or a list of URIs.
</ResponseField>

<a id="tabsdata.AzureSource.credentials"></a>
<ResponseField name="credentials" type="AzureCredentials">
The credentials required to access Azure.
</ResponseField>

<a id="tabsdata.AzureSource.initial_last_modified"></a>
<ResponseField name="initial_last_modified" type="str | datetime">
If provided, only the files modified after this date and time will be considered.
</ResponseField>

#### Methods

#### Attributes


</details>
