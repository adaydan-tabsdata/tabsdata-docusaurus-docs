---
title: "tabsdata.GCSDestination"
sidebarTitle: "tabsdata.GCSDestination"
description: "Bases: DestinationPlugin"
---
import { ParamField, ResponseField } from '@site/src/components/ApiField';


<a id="tabsdata.GCSDestination"></a>
<details>
<summary>GCSDestination</summary>


```python
class GCSDestination(
    uri: str | list [ str ],
    credentials: GCPCredentials,
    format: str | FileFormat = None,
)
```

Bases: [`DestinationPlugin`](../plugins/tabsdata.DestinationPlugin.html#tabsdata.DestinationPlugin)

Class for managing the configuration of GCS-file-based data outputs.

<a id="tabsdata.GCSDestination.__init__"></a>
#### `__init__`

```python
def __init__(
    uri: str | list [ str ],
    credentials: GCPCredentials,
    format: str | FileFormat = None,
)
```

**Parameters:**

<ParamField path="uri" type="str | list [ str ]" required>
  The URI of the files to export with format: ‘gs://path/to/files’. It can be a single URI or a list of URIs.
</ParamField>

<ParamField path="credentials" type="GCPCredentials" required>
  The credentials required to access GCS. Must be a GCPCredentials object.
</ParamField>

<ParamField path="format" type="str | FileFormat" default="None">
  The format of the file. If not provided, it will be inferred from the file extension. Can be either a string with the format or a FileFormat object. Currently supported formats are ‘csv’, ‘parquet’, ‘ndjson’ and ‘jsonl’.
</ParamField>

**Raises:**

<ResponseField name="OutputConfigurationError" type="Exception"></ResponseField>

<ResponseField name="FormatConfigurationError" type="Exception"></ResponseField>

<a id="tabsdata.GCSDestination.format"></a>
<ResponseField name="format" type="FileFormat">
The format of the file to be created. If not provided, it will be inferred from the file extension.
</ResponseField>

<a id="tabsdata.GCSDestination.uri"></a>
<ResponseField name="uri" type="str | list [ str ]">
The URI of the files with format: ‘gs://path/to/files’. It can be a single URI or a list of URIs.
</ResponseField>

<a id="tabsdata.GCSDestination.credentials"></a>
<ResponseField name="credentials" type="GCPCredentials">
The credentials required to access GCS.
</ResponseField>

#### Methods

#### Attributes


</details>
