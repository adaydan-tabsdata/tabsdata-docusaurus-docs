---
title: "tabsdata.subscriber"
sidebarTitle: "tabsdata.subscriber"
description: "convert it to a TabsdataFunction."
---
import { ParamField, ResponseField } from '@site/src/components/ApiField';


<a id="tabsdata.subscriber"></a>
<details>
<summary>subscriber</summary>


```python
def subscriber(
    tables: TableInput | str | List [ str ],
    destination: AzureDestination | LocalFileDestination | MariaDBDestination | MySQLDestination | OracleDestination | PostgresDestination | S3Destination | DestinationPlugin,
    name: str = None,
    trigger_by: str | List [ str ] | None = '*',
) -> callable
```

**Parameters:**

<ParamField path="tables" type="TableInput | str | List [ str ]" required>
  Where to obtain the data that will be provided as an input to the function.
</ParamField>

<ParamField path="|" required>
  MySQLDestination | OracleDestination | PostgresDestination | S3Destination | DestinationPlugin): Where to store the output of the function.
</ParamField>

<ParamField path="name" type="str" default="None">
  The name with which the function will be registered. If not provided, the current function name will be used.
</ParamField>

<ParamField path="trigger_by" type="str | List [ str ] | None" default="'*'">
  The trigger that will cause the function to execute. It can be a table in the system, a list of tables, or None (in which case it must be triggered manually). Defaults to all dependencies.
</ParamField>

**Returns:** The function converted to a TabsdataFunction.

**Return type:** callable


</details>
