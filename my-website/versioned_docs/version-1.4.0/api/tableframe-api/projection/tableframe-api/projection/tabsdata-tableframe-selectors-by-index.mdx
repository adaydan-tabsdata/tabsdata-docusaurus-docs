---
title: "tabsdata.tableframe.selectors.by_index"
sidebarTitle: "tabsdata.tableframe.selectors.by_index"
description: "Select columns by their position in the TableFrame."
---
import { ParamField, ResponseField } from '@site/src/components/ApiField';


<a id="tabsdata.tableframe.selectors.by_index"></a>
<details>
<summary>by_index</summary>


```python
def by_index(
    * indices: int | range | Sequence [ int | range ],
) -> SelectorProxy
```

Select columns by their position in the TableFrame.

**Parameters:**

<ParamField path="indices" required>
  One or more integer positions or ranges. Negative indexes are
</ParamField>

<ParamField path="supported" required>
  **supported** (*indicating last column*)
</ParamField>

<ParamField path="-1" required>
  **-1** (*ranging from*)
</ParamField>

<ParamField path="columns}" required>
  **columns}** (*-\{number of*)
</ParamField>

<ParamField path="fail." required>
  **fail.** (*absolute value**)**than the number**of**columns will*)
</ParamField>

#### Example

```python
>>> import tabsdata.tableframe as td_tf
>>>
>>> tf = td_tf.TableFrame({
...     "Id": [1, 2, 3, 4, 5, 6, 7, 8],
...     "Name": ["A", "B", "C", "D", "E", "F", "G", "H"],
...     "IsActive": [True, False, True, True, False, True, False, True],
...     "Score": [90.0, 85.5, 88.0, 91.0, 86.0, 87.5, 84.0, 89.5]
... })
```

```python
>>> tf.select(td_tf.selectors.by_index(1, 3))
```


</details>
