---
title: "tabsdata.LocalFileDestination"
sidebarTitle: "tabsdata.LocalFileDestination"
description: "tabsdata.LocalFileDestination API reference."
---
import { ParamField, ResponseField } from '@site/src/components/ApiField';


<a id="tabsdata.LocalFileDestination"></a>
<details>
<summary>LocalFileDestination</summary>


```python
class LocalFileDestination(
    path: str | List [ str ],
    format: str | dict | FileFormat = None,
)
```

[View source ↗](https://github.com/tabsdata/tabsdata/blob/main/client/td-sdk/tabsdata/io/output.py#L623)

Bases: `Output`

<a id="tabsdata.LocalFileDestination.__init__"></a>
#### `__init__`

```python
def __init__(
    path: str | List [ str ],
    format: str | dict | FileFormat = None,
)
```

[View source ↗](https://github.com/tabsdata/tabsdata/blob/main/client/td-sdk/tabsdata/io/output.py#L638)

Initializes the LocalFileDestination with the given path; and optionally a format.

**Parameters:**

<ParamField path="path" type="str | List [ str ]" required>
  The path where the files must be stored. It can be a single path or a list of paths.
</ParamField>

<ParamField path="format" type="str | dict | FileFormat" default="None">
  The format of the file. If not provided, it will be inferred from the file extension of the data. Can be either a string with the format, a FileFormat object or a dictionary with the format as the ‘type’ key and any additional format-specific information. Currently supported formats are ‘csv’, ‘parquet’, ‘ndjson’ and ‘jsonl’.
</ParamField>

**Raises:**

<ResponseField name="OutputConfigurationError" type="Exception"></ResponseField>

<ResponseField name="FormatConfigurationError" type="Exception"></ResponseField>

#### Methods

#### Attributes


</details>
