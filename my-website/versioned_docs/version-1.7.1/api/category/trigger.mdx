---
title: "Triggers"
sidebarTitle: "Triggers"
description: "Triggers API reference."
---
import { ParamField, ResponseField } from '@site/src/components/ApiField';


<a id="tabsdata.CronTrigger"></a>
<details>
<summary>CronTrigger</summary>


```python
class CronTrigger(
    mask: str,
    start: datetime | None = None,
    end: datetime | None = None,
)
```

Bases: [`object`](https://docs.python.org/3/library/functions.html#object)

Represents a cron-based trigger for scheduling Tabsdata publishers, subscribers and transformers.

This class defines a trigger based on a cron expression, with optional start and end times to constrain its activation period. It validates the cron mask and time boundaries to ensure the trigger is well-formed.

**Categories:** trigger

<a id="tabsdata.CronTrigger.end"></a>
<ResponseField name="end" type="str | None">
Gets the end time of the trigger as a timezone-aware ISO 8601 string with ‘Z’ timezone.

**Returns:** The end time in ISO format, or None if not set.
</ResponseField>

<a id="tabsdata.CronTrigger.mask"></a>
<ResponseField name="mask" type="str">
Gets the cron expression mask.

**Returns:** The cron expression string.
</ResponseField>

<a id="tabsdata.CronTrigger.start"></a>
<ResponseField name="start" type="str | None">
Gets the start time of the trigger as a timezone-aware ISO 8601 string with ‘Z’ timezone.

**Returns:** The start time in ISO format, or None if not set.
</ResponseField>


</details>

<a id="tabsdata.KafkaTrigger"></a>
<details>
<summary>KafkaTrigger</summary>


```python
class KafkaTrigger(
    ** kwargs,
)
```

Bases: [`StageTrigger`](/docs/api/module/tabsdata#tabsdata.StageTrigger)

A StageTrigger that consumes messages from a Kafka topic and stages them.

This trigger connects to a Kafka cluster, consumes messages from a specified topic, and uses rollover conditions (time, size, and message count) to buffer and stage the data.

**Categories:** trigger

<a id="tabsdata.KafkaTrigger.run"></a>
#### `run`

```python
def run(
    context: StageTriggerContext,
)
```

Run the Kafka stage trigger to consume messages, buffer them, and stage them based on rollover conditions.

**Parameters:**

<ParamField path="context" type="StageTriggerContext" required>
  The StageTriggerContext instance.
</ParamField>

<a id="tabsdata.KafkaTrigger.conn"></a>
<ResponseField name="conn" type="KafkaConn">
Kafka consumer connection configuration.
</ResponseField>

<a id="tabsdata.KafkaTrigger.data_format"></a>
<ResponseField name="data_format" type="Literal['avro', 'json']| Protobuf">
Data format of the messages (“avro”, “json”, “protobuf”).
</ResponseField>

<a id="tabsdata.KafkaTrigger.dlq_topic"></a>
<ResponseField name="dlq_topic" type="str | None">
Dead-letter queue topic.
</ResponseField>

<a id="tabsdata.KafkaTrigger.group_id"></a>
<ResponseField name="group_id" type="str | None">
Kafka consumer group ID.
</ResponseField>

<a id="tabsdata.KafkaTrigger.messages_rollover"></a>
<ResponseField name="messages_rollover" type="Annotated[int, Gt ( gt = 0 )]| None">
Message count-based rollover configuration.
</ResponseField>

<a id="tabsdata.KafkaTrigger.schema"></a>
<ResponseField name="schema" type="str">
Schema string for deserializing messages.
</ResponseField>

<a id="tabsdata.KafkaTrigger.size_rollover_mb"></a>
<ResponseField name="size_rollover_mb" type="Annotated[int, Gt ( gt = 0 )]| None">
Size-based rollover configuration. Size is specified in megabytes.
</ResponseField>

<a id="tabsdata.KafkaTrigger.time_rollover_secs"></a>
<ResponseField name="time_rollover_secs" type="int">
Timeout in seconds for consuming each message.
</ResponseField>

<a id="tabsdata.KafkaTrigger.topic"></a>
<ResponseField name="topic" type="str">
Kafka topic to consume from.
</ResponseField>


</details>
