---
title: "tabsdata.MariaDBSource"
sidebarTitle: "tabsdata.MariaDBSource"
description: "tabsdata.MariaDBSource API reference."
---
import { ParamField, ResponseField } from '@site/src/components/ApiField';


<a id="tabsdata.MariaDBSource"></a>
<details>
<summary>MariaDBSource</summary>


```python
class MariaDBSource(
    uri: str,
    query: str | List [ str ],
    credentials: UserPasswordCredentials | None = None,
    initial_values: dict | None = None,
)
```

Bases: [`SourcePlugin`](../plugins/tabsdata.SourcePlugin.html#tabsdata.SourcePlugin)

MariaDB-based data inputs.

<a id="tabsdata.MariaDBSource.__init__"></a>
#### `__init__`

```python
def __init__(
    uri: str,
    query: str | List [ str ],
    credentials: UserPasswordCredentials | None = None,
    initial_values: dict | None = None,
)
```

Initializes the MariaDBSource with the given URI and query, and optionally connection credentials and initial values for the parameters in the SQL queries.

**Parameters:**

<ParamField path="uri" type="str" required>
  The URI of the database where the data is located
</ParamField>

<ParamField path="query" type="str | List [ str ]" required>
  The SQL query(s) to execute. If multiple queries are provided, they must be provided as a list, and they will be mapped to the function inputs in the same order as they are defined.
</ParamField>

<ParamField path="credentials" type="UserPasswordCredentials | None" default="None">
  The credentials required to access the MariaDB database. Must be a UserPasswordCredentials object.
</ParamField>

<ParamField path="initial_values" type="dict | None" default="None">
  The initial values for the parameters in the SQL queries.
</ParamField>

**Raises:**

<ResponseField name="InputConfigurationError" type="Exception"></ResponseField>

#### Methods

#### Attributes


</details>
