---
title: "tabsdata.publisher"
sidebarTitle: "tabsdata.publisher"
description: "Decorator to define a function to publish data to Tabsdata."
---
import { ParamField, ResponseField } from '@site/src/components/ApiField';


<a id="tabsdata.publisher"></a>
<details>
<summary>publisher</summary>


```python
def publisher(
    source: AzureSource | LocalFileSource | MariaDBSource | MySQLSource | OracleSource | PostgresSource | S3Source | SourcePlugin,
    tables: TableOutput | str | List [ str ],
    name: str = None,
    trigger_by: str | List [ str ] | None = None,
) -> callable
```

Decorator to define a function to publish data to Tabsdata.

**Parameters:**

<ParamField path="source" type="AzureSource | LocalFileSource | MariaDBSource | MySQLSource | OracleSource | PostgresSource | S3Source | SourcePlugin" required>
  Where to obtain the data that will be provided as an input to the function.
</ParamField>

<ParamField path="tables" type="TableOutput | str | List [ str ]" required>
  Where to store the output of the function.
</ParamField>

<ParamField path="name" type="str" default="None">
  The name with which the function will be registered. If not provided, the current function name will be used.
</ParamField>

<ParamField path="trigger_by" type="str | List [ str ] | None" default="None">
  The trigger that will cause the function to execute. It can be a table in the system, a list of tables, or None (in which case it must be triggered manually).
</ParamField>

**Returns:** The function converted to a Tabsdata Function.

**Return type:** callable


</details>
