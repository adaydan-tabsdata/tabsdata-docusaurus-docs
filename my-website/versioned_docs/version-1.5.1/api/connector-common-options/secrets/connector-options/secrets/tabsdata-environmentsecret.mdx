---
title: "tabsdata.EnvironmentSecret"
sidebarTitle: "tabsdata.EnvironmentSecret"
description: "tabsdata.EnvironmentSecret API reference."
---
import { ParamField, ResponseField } from '@site/src/components/ApiField';


<a id="tabsdata.EnvironmentSecret"></a>
<details>
<summary>EnvironmentSecret</summary>


```python
class EnvironmentSecret(
    environment_variable_name: str,
)
```

Bases: `Secret`

Secrets class representing a secret obtained from an environment variable in the server.

<a id="tabsdata.EnvironmentSecret.to_dict"></a>
#### `to_dict`

```python
def to_dict() -> dict
```

Convert the EnvironmentSecret object to a dictionary.

<a id="tabsdata.EnvironmentSecret.secret_value"></a>
#### `secret_value`

```python
def secret_value() -> str
```

Get the secret value.

<a id="tabsdata.EnvironmentSecret.__init__"></a>
#### `__init__`

```python
def __init__(
    environment_variable_name: str,
)
```

Initialize the EnvironmentSecret object.

**Parameters:**

<ParamField path="environment_variable_name" type="str" required>
  Name of the environment variable from which
</ParamField>

<ParamField path="value." required>
  **value.** (*we will obtain the secret*)
</ParamField>

<a id="tabsdata.EnvironmentSecret.environment_variable_name"></a>
<ResponseField name="environment_variable_name" type="str">
Name of the environment variable from which we will obtain the secret value.
</ResponseField>

#### Methods


</details>
