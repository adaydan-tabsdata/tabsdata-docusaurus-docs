---
title: "Aggregation"
---

import { ParamField, ResponseField } from '@site/src/components/ApiField';

export const toc = [
  {value: '<span class="toc-kind-badge toc-kind-method">method</span>group_by()', id: 'tabsdata.TableFrame.group_by', level: 2},
  {value: '<span class="toc-kind-badge toc-kind-method">method</span>count()', id: 'tabsdata.tableframe.expr.expr.Expr.count', level: 2},
  {value: '<span class="toc-kind-badge toc-kind-method">method</span>first()', id: 'tabsdata.tableframe.expr.expr.Expr.first', level: 2},
  {value: '<span class="toc-kind-badge toc-kind-method">method</span>last()', id: 'tabsdata.tableframe.expr.expr.Expr.last', level: 2},
  {value: '<span class="toc-kind-badge toc-kind-method">method</span>len()', id: 'tabsdata.tableframe.expr.expr.Expr.len', level: 2},
  {value: '<span class="toc-kind-badge toc-kind-method">method</span>max()', id: 'tabsdata.tableframe.expr.expr.Expr.max', level: 2},
  {value: '<span class="toc-kind-badge toc-kind-method">method</span>mean()', id: 'tabsdata.tableframe.expr.expr.Expr.mean', level: 2},
  {value: '<span class="toc-kind-badge toc-kind-method">method</span>median()', id: 'tabsdata.tableframe.expr.expr.Expr.median', level: 2},
  {value: '<span class="toc-kind-badge toc-kind-method">method</span>min()', id: 'tabsdata.tableframe.expr.expr.Expr.min', level: 2},
  {value: '<span class="toc-kind-badge toc-kind-method">method</span>n_unique()', id: 'tabsdata.tableframe.expr.expr.Expr.n_unique', level: 2},
  {value: '<span class="toc-kind-badge toc-kind-method">method</span>rank()', id: 'tabsdata.tableframe.expr.expr.Expr.rank', level: 2},
  {value: '<span class="toc-kind-badge toc-kind-method">method</span>sum()', id: 'tabsdata.tableframe.expr.expr.Expr.sum', level: 2},
];

# Aggregation

 <a id="tabsdata.TableFrame.group_by"></a>
<div class="api-entry-wrap">
<details open>
<summary>group_by</summary>

```python
def group_by(
    *by: td_typing.IntoExpr | Iterable[td_typing.IntoExpr],
)
```

Perform a group by on the TableFrame.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌─────┬─────┐
│ a   ┆ b   │
│ --- ┆ --- │
│ str ┆ i64 │
╞═════╪═════╡
│ A   ┆ 1   │
│ X   ┆ 10  │
│ C   ┆ 3   │
│ D   ┆ 5   │
│ M   ┆ 9   │
│ A   ┆ 100 │
│ M   ┆ 50  │
└─────┴─────┘
>>>
>>> tf.group_by(td.col("a")).agg(td.col("b").sum())
>>>
┌─────┬─────┐
│ a   ┆ b   │
│ --- ┆ --- │
│ str ┆ i64 │
╞═════╪═════╡
│ M   ┆ 59  │
│ A   ┆ 101 │
│ C   ┆ 3   │
│ D   ┆ 5   │
│ X   ┆ 10  │
└─────┴─────┘
```

**Categories:** aggregation

**Parameters:**

<ParamField path="by">
  Columns or expressions to group by.
</ParamField>

</details>
<a class="entry-source-link" href="https://github.com/tabsdata/tabsdata/blob/b1.9/client/platform/src/main/python/tabsdata/tableframe/lazyframe/frame.py#L2245" target="_blank" rel="noopener noreferrer">View source ↗</a>
</div>

 <a id="tabsdata.tableframe.expr.expr.Expr.count"></a>
<div class="api-entry-wrap">
<details open>
<summary>count</summary>

```python
def count()
```

Aggregation operation that counts the non null values of the given column in
the group.

Examples:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌──────┬──────┐
│ a    ┆ b    │
│ ---  ┆ ---  │
│ str  ┆ i64  │
╞══════╪══════╡
│ A    ┆ 1    │
│ B    ┆ 2    │
│ A    ┆ 3    │
│ B    ┆ 0    │
│ C    ┆ 5    │
│ null ┆ 6    │
│ C    ┆ null │
└──────┴──────┘
>>>
>>> tf.group_by(td.col("a")).agg(td.col("b")).count().alias("count"))
>>>
┌──────┬───────┐
│ a    ┆ count │
│ ---  ┆ ---   │
│ str  ┆ u32   │
╞══════╪═══════╡
│ null ┆ 1     │
│ A    ┆ 2     │
│ B    ┆ 2     │
│ C    ┆ 1     │
└──────┴───────┘
```

**Categories:** aggregation

</details>
<a class="entry-source-link" href="https://github.com/tabsdata/tabsdata/blob/b1.9/client/platform/src/main/python/tabsdata/tableframe/expr/expr.py#L2273" target="_blank" rel="noopener noreferrer">View source ↗</a>
</div>

 <a id="tabsdata.tableframe.expr.expr.Expr.first"></a>
<div class="api-entry-wrap">
<details open>
<summary>first</summary>

```python
def first()
```

Get the first element.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf = tf.select(td.col("age"), td.col("age").first().alias("first"))
>>>
┌──────┬─────────┐
│ age  ┆ first   │
│ ---  ┆ ------- │
│ i64  ┆ i64     │
╞══════╪═════════╡
│ 10   ┆ 10      │
│ 11   ┆ 10      │
│ 18   ┆ 10      │
│ 65   ┆ 10      │
│ 70   ┆ 10      │
└──────┴─────────┘
```

**Categories:** aggregation

</details>
<a class="entry-source-link" href="https://github.com/tabsdata/tabsdata/blob/b1.9/client/platform/src/main/python/tabsdata/tableframe/expr/expr.py#L1013" target="_blank" rel="noopener noreferrer">View source ↗</a>
</div>

 <a id="tabsdata.tableframe.expr.expr.Expr.last"></a>
<div class="api-entry-wrap">
<details open>
<summary>last</summary>

```python
def last()
```

Get the last element.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf = tf.select(td.col("age"), td.col("age").first().alias("last"))
>>>
┌──────┬─────────┐
│ age  ┆ last    │
│ ---  ┆ ------- │
│ i64  ┆ i64     │
╞══════╪═════════╡
│ 10   ┆ 70      │
│ 11   ┆ 70      │
│ 18   ┆ 70      │
│ 65   ┆ 70      │
│ 70   ┆ 70      │
└──────┴─────────┘
```

**Categories:** aggregation

</details>
<a class="entry-source-link" href="https://github.com/tabsdata/tabsdata/blob/b1.9/client/platform/src/main/python/tabsdata/tableframe/expr/expr.py#L1473" target="_blank" rel="noopener noreferrer">View source ↗</a>
</div>

 <a id="tabsdata.tableframe.expr.expr.Expr.len"></a>
<div class="api-entry-wrap">
<details open>
<summary>len</summary>

```python
def len()
```

Aggregation operation that counts the rows in the group.

Examples:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌──────┬──────┐
│ a    ┆ b    │
│ ---  ┆ ---  │
│ str  ┆ i64  │
╞══════╪══════╡
│ A    ┆ 1    │
│ B    ┆ 2    │
│ A    ┆ 3    │
│ B    ┆ 0    │
│ C    ┆ 5    │
│ null ┆ 6    │
│ C    ┆ null │
└──────┴──────┘
>>>
>>> tf.group_by(td.col("a")).agg(td.col("b")).len().alias("len"))
>>>
┌──────┬─────┐
│ a    ┆ len │
│ ---  ┆ --- │
│ str  ┆ u32 │
╞══════╪═════╡
│ null ┆ 1   │
│ A    ┆ 2   │
│ B    ┆ 2   │
│ C    ┆ 2   │
└──────┴─────┘
```

**Categories:** aggregation

</details>
<a class="entry-source-link" href="https://github.com/tabsdata/tabsdata/blob/b1.9/client/platform/src/main/python/tabsdata/tableframe/expr/expr.py#L2316" target="_blank" rel="noopener noreferrer">View source ↗</a>
</div>

 <a id="tabsdata.tableframe.expr.expr.Expr.max"></a>
<div class="api-entry-wrap">
<details open>
<summary>max</summary>

```python
def max()
```

Aggregation operation that finds the maximum value in the group.

Examples:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌──────┬──────┐
│ ss   ┆ i    │
│ ---  ┆ ---  │
│ str  ┆ i64  │
╞══════╪══════╡
│ A    ┆ 1    │
│ B    ┆ 0    │
│ A    ┆ 2    │
│ B    ┆ 3    │
│ B    ┆ 4    │
│ C    ┆ -1   │
│ C    ┆ -2   │
│ C    ┆ -3   │
│ D    ┆ -4   │
│ F    ┆ -5   │
│ null ┆ null │
└──────┴──────┘
>>>
>>> tf.group_by(td.col("a")).agg(td.col("b")).max())
>>>
┌──────┬──────┐
│ ss   ┆ i    │
│ ---  ┆ ---  │
│ str  ┆ i64  │
╞══════╪══════╡
│ F    ┆ -5   │
│ C    ┆ -1   │
│ A    ┆ 2    │
│ B    ┆ 4    │
│ D    ┆ -4   │
│ null ┆ null │
└──────┴──────┘
```

**Categories:** aggregation

</details>
<a class="entry-source-link" href="https://github.com/tabsdata/tabsdata/blob/b1.9/client/platform/src/main/python/tabsdata/tableframe/expr/expr.py#L2472" target="_blank" rel="noopener noreferrer">View source ↗</a>
</div>

 <a id="tabsdata.tableframe.expr.expr.Expr.mean"></a>
<div class="api-entry-wrap">
<details open>
<summary>mean</summary>

```python
def mean()
```

Aggregation operation that finds the mean of the values in the group.

Examples:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌──────┬──────┐
│ ss   ┆ i    │
│ ---  ┆ ---  │
│ str  ┆ i64  │
╞══════╪══════╡
│ A    ┆ 1    │
│ B    ┆ 0    │
│ A    ┆ 2    │
│ B    ┆ 3    │
│ B    ┆ 4    │
│ C    ┆ -1   │
│ C    ┆ -2   │
│ C    ┆ -3   │
│ D    ┆ -4   │
│ F    ┆ -5   │
│ null ┆ null │
└──────┴──────┘
>>>
>>> tf.group_by(td.col("a")).agg(td.col("b").mean())
>>>
┌──────┬──────────┐
│ ss   ┆ i        │
│ ---  ┆ ---      │
│ str  ┆ f64      │
╞══════╪══════════╡
│ null ┆ null     │
│ A    ┆ 1.5      │
│ F    ┆ -5.0     │
│ C    ┆ -2.0     │
│ D    ┆ -4.0     │
│ B    ┆ 2.333333 │
└──────┴──────────┘
```

**Categories:** aggregation

</details>
<a class="entry-source-link" href="https://github.com/tabsdata/tabsdata/blob/b1.9/client/platform/src/main/python/tabsdata/tableframe/expr/expr.py#L2616" target="_blank" rel="noopener noreferrer">View source ↗</a>
</div>

 <a id="tabsdata.tableframe.expr.expr.Expr.median"></a>
<div class="api-entry-wrap">
<details open>
<summary>median</summary>

```python
def median()
```

Aggregation operation that finds the median of the values in the group.

Examples:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌──────┬──────┐
│ ss   ┆ i    │
│ ---  ┆ ---  │
│ str  ┆ i64  │
╞══════╪══════╡
│ A    ┆ 1    │
│ B    ┆ 0    │
│ A    ┆ 2    │
│ B    ┆ 3    │
│ B    ┆ 4    │
│ C    ┆ -1   │
│ C    ┆ -2   │
│ C    ┆ -3   │
│ D    ┆ -4   │
│ F    ┆ -5   │
│ null ┆ null │
└──────┴──────┘
>>>
>>> tf.group_by(td.col("a")).agg(td.col("b")).median())
>>>
┌──────┬──────┐
│ ss   ┆ i    │
│ ---  ┆ ---  │
│ str  ┆ f64  │
╞══════╪══════╡
│ F    ┆ -5.0 │
│ C    ┆ -2.0 │
│ B    ┆ 3.0  │
│ D    ┆ -4.0 │
│ A    ┆ 1.5  │
│ null ┆ null │
└──────┴──────┘
```

**Categories:** aggregation

</details>
<a class="entry-source-link" href="https://github.com/tabsdata/tabsdata/blob/b1.9/client/platform/src/main/python/tabsdata/tableframe/expr/expr.py#L2664" target="_blank" rel="noopener noreferrer">View source ↗</a>
</div>

 <a id="tabsdata.tableframe.expr.expr.Expr.min"></a>
<div class="api-entry-wrap">
<details open>
<summary>min</summary>

```python
def min()
```

Aggregation operation that finds the minimum value in the group.

Examples:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌──────┬──────┐
│ ss   ┆ i    │
│ ---  ┆ ---  │
│ str  ┆ i64  │
╞══════╪══════╡
│ A    ┆ 1    │
│ B    ┆ 0    │
│ A    ┆ 2    │
│ B    ┆ 3    │
│ B    ┆ 4    │
│ C    ┆ -1   │
│ C    ┆ -2   │
│ C    ┆ -3   │
│ D    ┆ -4   │
│ F    ┆ -5   │
│ null ┆ null │
└──────┴──────┘
>>>
>>> tf.group_by(td.col("a")).agg(td.col("b")).min())
>>>
┌──────┬──────┐
│ ss   ┆ i    │
│ ---  ┆ ---  │
│ str  ┆ i64  │
╞══════╪══════╡
│ C    ┆ -3   │
│ A    ┆ 1    │
│ null ┆ null │
│ B    ┆ 0    │
│ F    ┆ -5   │
│ D    ┆ -4   │
└──────┴──────┘
```

**Categories:** aggregation

</details>
<a class="entry-source-link" href="https://github.com/tabsdata/tabsdata/blob/b1.9/client/platform/src/main/python/tabsdata/tableframe/expr/expr.py#L2520" target="_blank" rel="noopener noreferrer">View source ↗</a>
</div>

 <a id="tabsdata.tableframe.expr.expr.Expr.n_unique"></a>
<div class="api-entry-wrap">
<details open>
<summary>n_unique</summary>

```python
def n_unique()
```

Aggregation operation that counts the unique values of the given column
in the group.

Examples:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌──────┬──────┐
│ ss   ┆ i    │
│ ---  ┆ ---  │
│ str  ┆ i64  │
╞══════╪══════╡
│ A    ┆ 1    │
│ B    ┆ 0    │
│ A    ┆ 2    │
│ B    ┆ 3    │
│ B    ┆ 4    │
│ C    ┆ -1   │
│ C    ┆ -2   │
│ C    ┆ -3   │
│ D    ┆ -4   │
│ F    ┆ -5   │
│ null ┆ null │
└──────┴──────┘
>>>
>>> tf.group_by(td.col("a")).agg(td.col("b")).n_unique())
>>>
┌──────┬─────┐
│ ss   ┆ i   │
│ ---  ┆ --- │
│ str  ┆ u32 │
╞══════╪═════╡
│ D    ┆ 1   │
│ C    ┆ 3   │
│ A    ┆ 2   │
│ B    ┆ 3   │
│ F    ┆ 1   │
│ null ┆ 1   │
└──────┴─────┘
```

**Categories:** aggregation

</details>
<a class="entry-source-link" href="https://github.com/tabsdata/tabsdata/blob/b1.9/client/platform/src/main/python/tabsdata/tableframe/expr/expr.py#L2712" target="_blank" rel="noopener noreferrer">View source ↗</a>
</div>

 <a id="tabsdata.tableframe.expr.expr.Expr.rank"></a>
<div class="api-entry-wrap">
<details open>
<summary>rank</summary>

```python
def rank(
    method: Literal['average', 'min', 'max', 'dense', 'ordinal', 'random'] = 'average',
    *,
    descending: bool = False,
    seed: int | None = None,
)
```

Compute the rank of the element values. Multiple rank types are available.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("val"), td.col("val").rank("max").alias("rank"))
>>>
┌──────┬──────┐
│ val  ┆ rank │
│ ---  ┆ ---  │
│ f64  ┆ u32  │
╞══════╪══════╡
│ -1.0 ┆ 1    │
│ 0.0  ┆ 2    │
│ 1.1  ┆ 3    │
│ 2.0  ┆ 4    │
│ inf  ┆ 5    │
│ null ┆ null │
│ NaN  ┆ 6    │
└──────┴──────┘
```

**Categories:** aggregation

**Parameters:**

<ParamField path="method">
  the ranking type: ‘average’ (default), ‘dense’, ‘max’, ‘min’,
‘ordinal’ or ‘random’.
</ParamField>

<ParamField path="descending">
  if the order is ascending (default) or descending.
</ParamField>

<ParamField path="seed">
  random seed when using ‘random’ rank type.
</ParamField>

</details>
<a class="entry-source-link" href="https://github.com/tabsdata/tabsdata/blob/b1.9/client/platform/src/main/python/tabsdata/tableframe/expr/expr.py#L1947" target="_blank" rel="noopener noreferrer">View source ↗</a>
</div>

 <a id="tabsdata.tableframe.expr.expr.Expr.sum"></a>
<div class="api-entry-wrap">
<details open>
<summary>sum</summary>

```python
def sum()
```

Aggregation operation that sums the values in the group.

Examples:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌──────┬──────┐
│ ss   ┆ i    │
│ ---  ┆ ---  │
│ str  ┆ i64  │
╞══════╪══════╡
│ A    ┆ 1    │
│ B    ┆ 0    │
│ A    ┆ 2    │
│ B    ┆ 3    │
│ B    ┆ 4    │
│ C    ┆ -1   │
│ C    ┆ -2   │
│ C    ┆ -3   │
│ D    ┆ -4   │
│ F    ┆ -5   │
│ null ┆ null │
└──────┴──────┘
>>>
>>> tf.group_by(td.col("a")).agg(td.col("b")).sum())
>>>
┌──────┬─────┐
│ ss   ┆ i   │
│ ---  ┆ --- │
│ str  ┆ i64 │
╞══════╪═════╡
│ null ┆ 0   │
│ A    ┆ 3   │
│ B    ┆ 7   │
│ C    ┆ -6  │
│ D    ┆ -4  │
│ F    ┆ -5  │
└──────┴─────┘
```

**Categories:** aggregation

</details>
<a class="entry-source-link" href="https://github.com/tabsdata/tabsdata/blob/b1.9/client/platform/src/main/python/tabsdata/tableframe/expr/expr.py#L2568" target="_blank" rel="noopener noreferrer">View source ↗</a>
</div>
