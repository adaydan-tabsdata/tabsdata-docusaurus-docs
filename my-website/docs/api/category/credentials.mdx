---
title: "Credentials"
---

import { ParamField, ResponseField } from '@site/src/components/ApiField';

# Credentials

 <details open>
<summary>**`AwsGlueSchemaRegistryCredentialsSpec`**</summary>

`AwsGlueSchemaRegistryCredentialsSpec`

alias of [S3AccessKeyCredentials](bymod-tabsdata#tabsdata.S3AccessKeyCredentials)

</details>

 <details open>
<summary>**`AzureAccountKeyCredentials`**</summary>

**`AzureAccountKeyCredentials(account_name: str | Secret, account_key: str | Secret)`**

Bases: `AzureCredentials`

Credentials class to access Azure using account key credentials (account name and
account key).

<details open>
<summary>**`account_key`**</summary>

`account_key`

The Azure account key.

 **Type:** Secret

</details>

<details open>
<summary>**`account_name`**</summary>

`account_name`

The Azure account name.

 **Type:** Secret

</details>

<ParamField path="account_name" type="Secret">
  The Azure account name.
</ParamField>

<ParamField path="account_key" type="Secret">
  The Azure account key.
</ParamField>

<ParamField path="dict" type="to_dict">
  Convert the AzureAccountNameKeyCredentials object to a
dictionary
</ParamField>

</details>

 <details open>
<summary>**`ConfluentSchemaRegistryCredentialsSpec`**</summary>

`ConfluentSchemaRegistryCredentialsSpec`

alias of [UserPasswordCredentials](bymod-tabsdata#tabsdata.UserPasswordCredentials)

</details>

 <details open>
<summary>**`GCPServiceAccountKeyCredentials`**</summary>

**`GCPServiceAccountKeyCredentials(service_account_key: str | Secret)`**

Bases: `GCPCredentials`

Credentials class to store the credentials needed to access GCS
using account key credentials (service account key).

<details open>
<summary>**`service_account_key`**</summary>

`service_account_key`

The GCS service account key.

 **Type:** Secret

</details>

<ParamField path="service_account_key" type="Secret">
  The GCS service account key.
</ParamField>

<ParamField path="dict" type="to_dict">
  Convert the GCPServiceAccountKeyCredentials object to a
dictionary
</ParamField>

</details>

 <details open>
<summary>**`HMACCredentials`**</summary>

**`HMACCredentials(access_key: str | Secret, secret_key: str | Secret)`**

Bases: `Credentials`

Generic HMAC credentials consisting of an access key and a secret key.

These credentials follow the same shape as AWS S3 access keys and are
compatible with any service that uses HMAC-based authentication over the
S3-compatible API, such as GCS HMAC keys or MinIO credentials.

<details open>
<summary>**`access_key`**</summary>

`access_key`

The HMAC access key id.

 **Type:** Secret

</details>

<details open>
<summary>**`secret_key`**</summary>

`secret_key`

The HMAC secret key.

 **Type:** Secret

</details>

<ParamField path="access_key" type="Secret">
  The HMAC access key id.
</ParamField>

<ParamField path="secret_key" type="Secret">
  The HMAC secret key.
</ParamField>

<ParamField path="dict" type="to_dict">
  Convert the HMACCredentials object to a dictionary.
</ParamField>

</details>

 <details open>
<summary>**`KafkaCredentialsSpec`**</summary>

`KafkaCredentialsSpec`

alias of [UserPasswordCredentials](bymod-tabsdata#tabsdata.UserPasswordCredentials)

</details>

 <details open>
<summary>**`S3AccessKeyCredentials`**</summary>

**`S3AccessKeyCredentials(aws_access_key_id: str | Secret, aws_secret_access_key: str | Secret)`**

Bases: `S3Credentials`

Credentials class to access an S3 bucket using access key credentials (access key
id and secret access key).

<details open>
<summary>**`aws_access_key_id`**</summary>

`aws_access_key_id`

The AWS access key id.

 **Type:** Secret

</details>

<details open>
<summary>**`aws_secret_access_key`**</summary>

`aws_secret_access_key`

The AWS secret access key.

 **Type:** Secret

</details>

<ParamField path="aws_access_key_id" type="Secret">
  The AWS access key id.
</ParamField>

<ParamField path="aws_secret_access_key" type="Secret">
  The AWS secret access key.
</ParamField>

<ParamField path="dict" type="to_dict">
  Convert the S3AccessKeyCredentials object to a dictionary
</ParamField>

</details>

 <details open>
<summary>**`SalesforceTokenCredentials`**</summary>

**`SalesforceTokenCredentials(username: str | Secret, password: str | Secret, security_token: str | Secret)`**

Bases: `SalesforceCredentials`

Credentials class to store the credentials needed to access a Salesforce
using a username, a password and a token.

<details open>
<summary>**`password`**</summary>

`password`

The password to access Salesforce.

 **Type:** Secret

</details>

<details open>
<summary>**`security_token`**</summary>

`security_token`

The security token to access Salesforce.

 **Type:** Secret

</details>

<details open>
<summary>**`username`**</summary>

`username`

The username to access Salesforce.

 **Type:** Secret

</details>

<ParamField path="username" type="Secret">
  The username to access Salesforce.
</ParamField>

<ParamField path="password" type="Secret">
  The password to access Salesforce.
</ParamField>

<ParamField path="security_token" type="Secret">
  The security token to access Salesforce.
</ParamField>

<ParamField path="dict" type="to_dict">
  Convert the AzureAccountNameKeyCredentials object to a
dictionary
</ParamField>

</details>

 <details open>
<summary>**`UserPasswordCredentials`**</summary>

**`UserPasswordCredentials(user: str | Secret, password: str | Secret)`**

Bases: `Credentials`

Represents credentials consisting of a user and a password.

This class is used to manage and securely handle user and password
credentials. It includes functionalities to set user and password
details, convert them into a dictionary representation, and provide
a string representation for debugging purposes.

<details open>
<summary>**`password`**</summary>

`password`

The password.

 **Type:** Secret

</details>

<details open>
<summary>**`user`**</summary>

`user`

The user.

 **Type:** Secret

</details>

</details>
