---
title: "tabsdata.tableframe.functions.lit"
---

import { ParamField, ResponseField } from '@site/src/components/ApiField';

export const toc = [
  {value: 'lit()', id: 'tabsdata.tableframe.functions.lit.lit', level: 2},
];

# tabsdata.tableframe.functions.lit

 <a id="tabsdata.tableframe.functions.lit.lit"></a>
<div class="api-entry-wrap">
<details open>
<summary>lit</summary>

```python
def lit(
    value: Any,
    dtype: Boolean | Categorical | Date | Datetime | Decimal | Duration | Enum | Float16 | Float32 | Float64 | Int8 | Int16 | Int64 | Int32 | Int128 | Null | String | Time | UInt8 | UInt16 | UInt32 | UInt64 | None = None,
    *,
    allow_object: bool = False,
)
```

Expression for the given literal value.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.lit("Hi").alias("lit"), td.col("age").alias("Age"))
>>>
┌──────┬──────┐
│ lit  ┆ Age  │
│ ---  ┆ ---  │
│ str  ┆ i64  │
╞══════╪══════╡
│ Hi   ┆ 1    │
│ Hi   ┆ 15   │
│ Hi   ┆ 18   │
│ Hi   ┆ null │
└──────┴──────┘
```

**Categories:** generation

**Parameters:**

<ParamField path="value">
  The literal value.
</ParamField>

<ParamField path="dtype">
  The data type of the literal value.
</ParamField>

<ParamField path="allow_object">
  Whether to allow object data type.
</ParamField>

</details>
<a class="entry-source-link" href="https://github.com/tabsdata/tabsdata/blob/b1.9/client/platform/src/main/python/tabsdata/tableframe/functions/lit.py#L17" target="_blank" rel="noopener noreferrer">View source ↗</a>
</div>
