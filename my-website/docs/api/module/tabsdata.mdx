---
title: "tabsdata"
---

import { ParamField, ResponseField } from '@site/src/components/ApiField';

# tabsdata

 <details>
<summary>

**`AWSGlue(definition: dict, tables: str | list[str], auto_create_at: list[str | None] | str | None = None, if_table_exists: Literal['append', 'replace'] = 'append', partitioned_table: bool = False, schema_strategy: Literal['update', 'strict'] = 'update', s3_credentials: S3Credentials = None, s3_region: str = None, **kwargs)`**

</summary>

Bases: `Catalog`

<details>
<summary>`auto_create_at`</summary>

</details>

<details>
<summary>`definition`</summary>

</details>

<details>
<summary>`if_table_exists`</summary>

The strategy to follow when the table already exists.

 **Type:** str

</details>

<details>
<summary>`partitioned_table`</summary>

Whether the table is partitioned or not.

 **Type:** bool

</details>

<details>
<summary>`s3_credentials`</summary>

</details>

<details>
<summary>`s3_region`</summary>

The region where the S3 bucket is located.

 **Type:** str

</details>

<details>
<summary>`schema_strategy`</summary>

The strategy to follow when appending to a table with an existing schema.

 **Type:** str

</details>

<details>
<summary>`tables`</summary>

</details>

</details>

 <details>
<summary>

**`AvroFormat(chunk_size: int = 50000)`**

</summary>

Bases: `FileFormat`

Avro file format.

<details>
<summary>`chunk_size`</summary>

Returns the chunk size for reading/writing AVRO files.

 **Returns:** The chunk size.

**Return type:** int

</details>

</details>

 <details>
<summary>

**`AwsGlueSchemaRegistry(**kwargs)`**

</summary>

Bases: `KafkaSchemaRegistry`

Configuration for AWS Glue Schema Registry.

<details>
<summary>`configs`</summary>

Additional configuration properties for the AWS Glue client.

</details>

<details>
<summary>`credentials`</summary>

Credentials for authenticating with AWS Glue.

</details>

<details>
<summary>`region_name`</summary>

The AWS region of the Schema Registry.

</details>

<details>
<summary>`registry_name`</summary>

Registry name for AWS Glue Schema Registry.

</details>

<details>
<summary>`schema_name`</summary>

Schema name for AWS Glue Schema Registry.

</details>

</details>

 <details>
<summary>`AwsGlueSchemaRegistryCredentialsSpec`</summary>

alias of S3AccessKeyCredentials

</details>

 <details>
<summary>

**`AzureAccountKeyCredentials(account_name: str | Secret, account_key: str | Secret)`**

</summary>

Bases: `AzureCredentials`

Credentials class to access Azure using account key credentials (account name and
account key).

<details>
<summary>`account_key`</summary>

The Azure account key.

 **Type:** Secret

</details>

<details>
<summary>`account_name`</summary>

The Azure account name.

 **Type:** Secret

</details>

<ParamField path="account_name" type="Secret">
  The Azure account name.
</ParamField>

<ParamField path="account_key" type="Secret">
  The Azure account key.
</ParamField>

<ParamField path="dict" type="to_dict">
  Convert the AzureAccountNameKeyCredentials object to a
dictionary
</ParamField>

</details>

 <details>
<summary>

**`AzureConfig(container: str, base_path: str, credentials: AzureAccountKeyCredentials)`**

</summary>

Bases: [object](https://docs.python.org/3/library/functions.html#object)

Configuration for staging files in Azure Blob Storage.

<details>
<summary>`base_path`</summary>

</details>

<details>
<summary>`container`</summary>

</details>

<details>
<summary>`credentials`</summary>

</details>

<ParamField path="container">
  Blob container name.
</ParamField>

<ParamField path="base_path">
  Blob path prefix for staged files
(e.g. "staging/clickhouse").
</ParamField>

<ParamField path="credentials">
  Azure account-key credentials used for uploading
staged files (via azure-storage-blob).
</ParamField>

</details>

 <details>
<summary>

**`AzureDestination(uri: str | list[str], credentials: AzureCredentials, format: str | FileFormat = None)`**

</summary>

Bases: DestinationPlugin

Azure-file-based data outputs.

<details>
<summary>

**`SupportedFormats(*values)`**

</summary>

Bases: [Enum](https://docs.python.org/3/library/enum.html#enum.Enum)

Enum for the supported formats for the AzureDestination.

<details>
<summary>`avro`</summary>

</details>

<details>
<summary>`csv`</summary>

</details>

<details>
<summary>`ndjson`</summary>

</details>

<details>
<summary>`parquet`</summary>

</details>

</details>

<details>
<summary>`allow_fragments`</summary>

Whether to allow fragments in the output.

 **Type:** bool

</details>

<details>
<summary>

**`chunk(working_dir: str, *results)`**

</summary>

Trigger the exporting of the data to local parquet chunks. This method will
receive the resulting data from the user function and must store it in
the local system as parquet files, using the working_dir. Note: This
method should *not* materialize the data, it should only store it in the
local system.

<ParamField path="working_dir" type="str">
  The folder where any files generated must
be stored (this refers to temporary files that will be deleted after
the execution of the plugin, not the final destination of the data)
</ParamField>

<ParamField path="results">
  The data to be exported. It is a list of polars LazyFrames or None.
</ParamField>

**Returns:** A list of the intermediate files created

</details>

<details>
<summary>`credentials`</summary>

The credentials required to access Azure.

 **Type:** AzureCredentials

</details>

<details>
<summary>`format`</summary>

The format of the file. If not provided, it will be inferred from
the file extension of the URI.

 **Type:** FileFormat

</details>

<details>
<summary>

**`stream(working_dir: str, *results)`**

</summary>

Trigger the exporting of the data. This method will receive the resulting data
from the user function and must store it in the desired location.
Note: this method *might* materialize the data provided in a single chunk
generated by the chunk function if invoked, so chunks should be of an
appropriate size.

<ParamField path="working_dir" type="str">
  The folder where any intermediate files generated must
be stored (this refers to temporary files that will be deleted after
the execution of the plugin, not the final destination of the data)
</ParamField>

<ParamField path="results">
  The data to be exported. It is a list of polars LazyFrames or None.
</ParamField>

**Returns:** None

</details>

<details>
<summary>`uri`</summary>

‘az://path/to/files’.

 **Type:** str | list[str]

**Type:** The URI of the files with format

</details>

</details>

 <details>
<summary>

**`chunk(working_dir: str, *results)`**

</summary>

Trigger the exporting of the data to local parquet chunks. This method will
receive the resulting data from the user function and must store it in
the local system as parquet files, using the working_dir. Note: This
method should *not* materialize the data, it should only store it in the
local system.

<ParamField path="working_dir" type="str">
  The folder where any files generated must
be stored (this refers to temporary files that will be deleted after
the execution of the plugin, not the final destination of the data)
</ParamField>

<ParamField path="results">
  The data to be exported. It is a list of polars LazyFrames or None.
</ParamField>

**Returns:** A list of the intermediate files created

</details>

 <details>
<summary>

**`stream(working_dir: str, *results)`**

</summary>

Trigger the exporting of the data. This method will receive the resulting data
from the user function and must store it in the desired location.
Note: this method *might* materialize the data provided in a single chunk
generated by the chunk function if invoked, so chunks should be of an
appropriate size.

<ParamField path="working_dir" type="str">
  The folder where any intermediate files generated must
be stored (this refers to temporary files that will be deleted after
the execution of the plugin, not the final destination of the data)
</ParamField>

<ParamField path="results">
  The data to be exported. It is a list of polars LazyFrames or None.
</ParamField>

**Returns:** None

</details>

 <details>
<summary>

**`AzureSource(uri: str | list[str], credentials: AzureCredentials, format: str | FileFormat = None, initial_last_modified: str | datetime = None, file_info: str | FileInfo | None = None)`**

</summary>

Bases: SourcePlugin

Azure-file-based data inputs.

<details>
<summary>

**`SupportedFormats(*values)`**

</summary>

Bases: [Enum](https://docs.python.org/3/library/enum.html#enum.Enum)

Enum for the supported formats for the AzureSource.

<details>
<summary>`avro`</summary>

</details>

<details>
<summary>`csv`</summary>

</details>

<details>
<summary>`log`</summary>

</details>

<details>
<summary>`ndjson`</summary>

</details>

<details>
<summary>`parquet`</summary>

</details>

</details>

<details>
<summary>

**`chunk(working_dir: str)`**

</summary>

Trigger the import of the data. This must be implemented in any class that
inherits from this class unless directly implementing streaming. The method will
receive a folder where it must store the data as parquet files, and return a
list of the paths of the files created. This files will then be loaded and
mapped to the dataset function in positional order, so if you want file.parquet
to be the first argument of the dataset function, you must return it first. If
you want a parameter to receive multiple files, return a list of the paths. For
example, you would give the following return to provide a first argument with a
single file and a second argument with two files:return
[“file1.parquet”, [“file2.parquet”, “file3.parquet”]]

**Returns:** The path of the file(s) created, inthe order they must be mapped to the dataset function

**Return type:** Union[str, Tuple[str, …], List[str]]

</details>

<details>
<summary>`credentials`</summary>

The credentials required to access Azure.

 **Type:** AzureCredentials

</details>

<details>
<summary>`file_info`</summary>

The file information to add as system columns.

 **Type:** FileInfo | None

</details>

<details>
<summary>`format`</summary>

The format of the file. If not provided, it will be inferred from
the file extension of the data.

 **Type:** FileFormat

</details>

<details>
<summary>`initial_last_modified`</summary>

The date and time after which the files were modified.

 **Type:** str

</details>

<details>
<summary>`initial_values`</summary>

Return a dictionary with the initial values to be stored after execution of
the plugin. They will be accessible in the next execution of the plugin.
The dictionary must have the parameter names as keys and the initial values
as values, all the type string.

 **Returns:** A dictionary with the initial values of the parameters of the plugin.

**Return type:** dict

</details>

<details>
<summary>`uri`</summary>

‘az://path/to/files’.

 **Type:** str | list[str]

**Type:** The URI of the files with format

</details>

</details>

 <details>
<summary>

**`chunk(working_dir: str)`**

</summary>

Trigger the import of the data. This must be implemented in any class that
inherits from this class unless directly implementing streaming. The method will
receive a folder where it must store the data as parquet files, and return a
list of the paths of the files created. This files will then be loaded and
mapped to the dataset function in positional order, so if you want file.parquet
to be the first argument of the dataset function, you must return it first. If
you want a parameter to receive multiple files, return a list of the paths. For
example, you would give the following return to provide a first argument with a
single file and a second argument with two files:return
[“file1.parquet”, [“file2.parquet”, “file3.parquet”]]

**Returns:** The path of the file(s) created, inthe order they must be mapped to the dataset function

**Return type:** Union[str, Tuple[str, …], List[str]]

</details>

 <details>
<summary>

**`BigQueryConn(gcs_folder: str, credentials: GCPCredentials, project: str = None, dataset: str = None, enforce_connection_params: bool = True, cx_dst_configs_gcs: dict = None, cx_dst_configs_bigquery: dict = None)`**

</summary>

Bases: Conn

For connection configuration to BigQueryDest.

<details>
<summary>`credentials`</summary>

</details>

<details>
<summary>`cx_dst_configs_bigquery`</summary>

</details>

<details>
<summary>`cx_dst_configs_gcs`</summary>

</details>

<details>
<summary>`dataset`</summary>

</details>

<details>
<summary>`enforce_connection_params`</summary>

</details>

<details>
<summary>`gcs_folder`</summary>

</details>

<details>
<summary>`project`</summary>

</details>

</details>

 <details>
<summary>

**`BigQueryDest(conn: BigQueryConn, tables: TableSpec | None = None, if_table_exists: IfTableExistStrategySpec = 'append', schema_strategy: SchemaStrategySpec = 'update')`**

</summary>

Bases: DestinationPlugin

BigQuery based data outputs.
The data is first stored in parquet files in a GCS bucket, and then loaded into the
BigQuery tables.

<details>
<summary>`conn`</summary>

</details>

<details>
<summary>`if_table_exists`</summary>

The strategy to follow when the table already exists.

 **Type:** str

</details>

<details>
<summary>`schema_strategy`</summary>

The strategy to follow when appending to a table with an existing schema.

 **Type:** str

</details>

<details>
<summary>

**`stream(working_dir: str, *results: list[LazyFrame | None] | LazyFrame | None)`**

</summary>

Trigger the exporting of the data. This method will receive the resulting data
from the user function and must store it in the desired location.
Note: this method *might* materialize the data provided in a single chunk
generated by the chunk function if invoked, so chunks should be of an
appropriate size.

<ParamField path="working_dir" type="str">
  The folder where any intermediate files generated must
be stored (this refers to temporary files that will be deleted after
the execution of the plugin, not the final destination of the data)
</ParamField>

<ParamField path="results">
  The data to be exported. It is a list of polars LazyFrames or None.
</ParamField>

**Returns:** None

</details>

<details>
<summary>`tables`</summary>

</details>

</details>

 <details>
<summary>

**`stream(working_dir: str, *results: list[LazyFrame | None] | LazyFrame | None)`**

</summary>

Trigger the exporting of the data. This method will receive the resulting data
from the user function and must store it in the desired location.
Note: this method *might* materialize the data provided in a single chunk
generated by the chunk function if invoked, so chunks should be of an
appropriate size.

<ParamField path="working_dir" type="str">
  The folder where any intermediate files generated must
be stored (this refers to temporary files that will be deleted after
the execution of the plugin, not the final destination of the data)
</ParamField>

<ParamField path="results">
  The data to be exported. It is a list of polars LazyFrames or None.
</ParamField>

**Returns:** None

</details>

 <details>
<summary>

**`CSVFormat(separator: str | int = ',', quote_char: str | int = '"', eol_char: str | int = '\n', input_encoding: str = 'Utf8', input_null_values: list | None = None, input_missing_is_null: bool = True, input_truncate_ragged_lines: bool = False, input_comment_prefix: str | int | None = None, input_try_parse_dates: bool = False, input_decimal_comma: bool = False, input_has_header: bool = True, input_skip_rows: int = 0, input_skip_rows_after_header: int = 0, input_raise_if_empty: bool = True, input_ignore_errors: bool = False, output_include_header: bool = True, output_datetime_format: str | None = None, output_date_format: str | None = None, output_time_format: str | None = None, output_float_scientific: bool | None = None, output_float_precision: int | None = None, output_null_value: str | None = None, output_quote_style: str | None = None, output_maintain_order: bool = True)`**

</summary>

Bases: `FileFormat`

CSV file format.

<ParamField path="separator" type="str">
  The separator of the CSV file.
</ParamField>

<ParamField path="quote_char" type="str">
  The quote character of the CSV file.
</ParamField>

<ParamField path="eol_char" type="str">
  The end of line character of the CSV file.
</ParamField>

<ParamField path="input_encoding" type="str">
  The encoding of the CSV file. Only used when importing
data.
</ParamField>

<ParamField path="input_null_values" type="list">
  The null values of the CSV file. Only used
when importing data.
</ParamField>

<ParamField path="input_missing_is_null" type="bool">
  Whether missing values should be marked as null.
Only used when importing data.
</ParamField>

<ParamField path="input_truncate_ragged_lines" type="bool">
  Whether to truncate ragged lines of the
CSV file. Only used when importing data.
</ParamField>

<ParamField path="input_comment_prefix" type="str">
  The comment prefix of the CSV file.
Only used when importing data.
</ParamField>

<ParamField path="input_try_parse_dates" type="bool">
  Whether to try parse dates of the CSV file. Only
used when importing data.
</ParamField>

<ParamField path="input_decimal_comma" type="bool">
  Whether the CSV file uses decimal comma. Only used
when importing data.
</ParamField>

<ParamField path="input_has_header" type="bool">
  If the CSV file has header. Only used when importing
data.
</ParamField>

<ParamField path="input_skip_rows" type="int">
  How many rows should be skipped in the CSV file. Only
used when importing data.
</ParamField>

<ParamField path="input_skip_rows_after_header" type="int">
  How many rows should be skipped after the
header in the CSV file. Only used when importing data.
</ParamField>

<ParamField path="input_raise_if_empty" type="bool">
  If an error should be raised for an empty CSV. Only
used when importing data.
</ParamField>

<ParamField path="input_ignore_errors" type="bool">
  If the errors loading the CSV must be ignored. Only
used when importing data.
</ParamField>

<ParamField path="output_include_header" type="bool">
  Whether to include header in the CSV
output. Only used when exporting data.
</ParamField>

<ParamField path="output_datetime_format" type="str">
  A format string, with the
specifiers defined by the chrono Rust crate. If no format specified,
the default fractional-second precision is inferred from the maximum
timeunit found in the frame’s Datetime cols (if any). Only used when
exporting data.
</ParamField>

<ParamField path="output_date_format" type="str">
  A format string, with the specifiers
defined by the chrono Rust crate. Only used when exporting data.
</ParamField>

<ParamField path="output_time_format" type="str">
  A format string, with the specifiers
defined by the chrono Rust crate. Only used when exporting data.
</ParamField>

<ParamField path="output_float_scientific" type="bool">
  Whether to use scientific form
always (true), never (false), or automatically (None). Only used when
exporting data.
</ParamField>

<ParamField path="output_float_precision" type="int">
  Number of decimal places to write.
Only used when exporting data.
</ParamField>

<ParamField path="output_null_value" type="str">
  A string representing null values
(defaulting to the empty string). Only used when exporting data.
</ParamField>

<ParamField path="output_quote_style" type="str">
  Determines the quoting strategy used.
Only used when exporting data.
* necessary (default): This puts quotes around fields only when
necessary. They are necessary when fields contain a quote, separator
or record terminator. Quotes are also necessary when writing an empty
record (which is indistinguishable from a record with one empty field).
This is the default.
* always: This puts quotes around every field. Always.
* never: This never puts quotes around fields, even if that results
in invalid CSV data (e.g.: by not quoting strings containing the
separator).
* non_numeric: This puts quotes around all fields that are
non-numeric. Namely, when writing a field that does not parse as a
valid float or integer, then quotes will be used even if they aren`t
strictly necessary.
</ParamField>

<ParamField path="output_maintain_order" type="bool">
  Maintain the order in which data is
processed. Setting this to False will be slightly faster. Only used
when exporting data.
</ParamField>

</details>

 <details>
<summary>

**`CdcConn`**

</summary>

Bases: Conn, [ABC](https://docs.python.org/3/library/abc.html#abc.ABC)

Base connection configuration for Change Data Capture (CDC) connectors.

<details>
<summary>`credentials`</summary>

</details>

<details>
<summary>`uri`</summary>

</details>

</details>

 <details>
<summary>

**`CdcTrigger(**kwargs)`**

</summary>

Bases: StageTrigger

Base StageTrigger for Change Data Capture (CDC) connectors.

This abstract trigger provides the common polling loop, buffering, and staging
logic for all CDC connectors. Database-specific subclasses implement the
ingestion details for their respective replication protocols.

<details>
<summary>`buffer_max_bytes`</summary>

Maximum bytes in memory before flushing to the working directory.

</details>

<details>
<summary>`buffer_max_rows`</summary>

Maximum rows in memory before flushing to the working directory.

</details>

<details>
<summary>`buffer_max_secs`</summary>

Maximum seconds before flushing memory data to the working directory.

</details>

<details>
<summary>`cdc_format`</summary>

Output format specification for CDC data.

</details>

<details>
<summary>

**`collect_tables(session: Any, cdc_format: CdcFormat = CdcFormat(values_format='columns', flatten_values=True))`**

</summary>

</details>

<details>
<summary>`conn`</summary>

CDC connection configuration.

</details>

<details>
<summary>

**`harvest_tables(session: Any, cdc_format: CdcFormat = CdcFormat(values_format='columns', flatten_values=True))`**

</summary>

</details>

<details>
<summary>`out`</summary>

Mapping of table indices to qualified table names.

</details>

<details>
<summary>`poll_interval_seconds`</summary>

Interval in seconds between polling for new changes.

</details>

<details>
<summary>

**`run(context: StageTriggerContext)`**

</summary>

Continuously runs the stage trigger logic using the received context to
trigger and monitor executions.
SUCCESS if the StageTrigger.end datetime is reached. exit with 0.
ERROR if any issue stops it from working or recovering. exit with 1.
STOPPED upon a signal. exit with 0.

</details>

<details>
<summary>`tables`</summary>

Qualified table names being tracked for changes.

</details>

<details>
<summary>`trigger_max_bytes`</summary>

Maximum total bytes before staging from working directory to the stage location.

</details>

<details>
<summary>`trigger_max_rows`</summary>

Maximum total rows before staging from working directory to the stage location.

</details>

<details>
<summary>`trigger_max_secs`</summary>

Maximum seconds before staging from working directory to the stage location.

</details>

</details>

 <details>
<summary>

**`collect_tables(session: Any, cdc_format: CdcFormat = CdcFormat(values_format='columns', flatten_values=True))`**

</summary>

</details>

 <details>
<summary>

**`harvest_tables(session: Any, cdc_format: CdcFormat = CdcFormat(values_format='columns', flatten_values=True))`**

</summary>

</details>

 <details>
<summary>

**`run(context: StageTriggerContext)`**

</summary>

Continuously runs the stage trigger logic using the received context to
trigger and monitor executions.
SUCCESS if the StageTrigger.end datetime is reached. exit with 0.
ERROR if any issue stops it from working or recovering. exit with 1.
STOPPED upon a signal. exit with 0.

</details>

 <details>
<summary>

**`ClickHouseConn(**kwargs)`**

</summary>

Bases: Conn

Represents a connection configuration for ClickHouse with cloud-staged bulk
loading.

ClickHouse ingests data via cloud storage (S3, Azure Blob, or GCS): files are
first uploaded to the configured staging area and then loaded into ClickHouse
using the matching table function (`s3()`, `azureBlobStorage()`, or
`s3()` pointed at GCS).

<details>
<summary>`credentials`</summary>

The credentials used to authenticate with ClickHouse.  `None` if no
credentials were provided (ClickHouse `default` user, no password).

</details>

<details>
<summary>`cx_dst_configs_clickhouse`</summary>

Destination-specific configuration parameters for the ClickHouse connection.

</details>

<details>
<summary>`cx_src_configs_clickhouse`</summary>

Source-specific configuration parameters for the ClickHouse connection.

</details>

<details>
<summary>`database`</summary>

The target ClickHouse database name.

</details>

<details>
<summary>`enforce_connection_params`</summary>

Whether to enforce connection parameters for the ClickHouse connection.

</details>

<details>
<summary>`host`</summary>

The ClickHouse server hostname or IP address.

</details>

<details>
<summary>`port`</summary>

The HTTP(S) port of the ClickHouse server.

</details>

<details>
<summary>`secure`</summary>

Whether to connect to ClickHouse over HTTPS/TLS.

</details>

<details>
<summary>`staging`</summary>

The cloud storage staging configuration (S3Config, AzureConfig, or
GCSConfig).

</details>

</details>

 <details>
<summary>

**`ClickHouseDest(**kwargs)`**

</summary>

Bases: DestinationPlugin

Destination plugin for writing data to ClickHouse using cloud-staged bulk loading.

Data is first written as Parquet files to the configured cloud storage staging area
(S3, Azure Blob, or GCS) and then ingested into ClickHouse via its native table
functions (`s3()`, `azureBlobStorage()`).

<details>
<summary>`conn`</summary>

The ClickHouse connection configuration.

</details>

<details>
<summary>`destination_tables`</summary>

The table(s) to load data into.

</details>

<details>
<summary>`if_table_exists`</summary>

The strategy to follow when the table already exists.

</details>

<details>
<summary>`schema_evolution`</summary>

The schema evolution strategy.

</details>

<details>
<summary>

**`stream(working_dir: str, *results: list[LazyFrame | None])`**

</summary>

Write each result as a Parquet file and bulk load it into ClickHouse via
the cloud staging area.

<ParamField path="working_dir">
  Temporary directory for intermediate Parquet files.
</ParamField>

<ParamField path="results">
  One polars LazyFrame (or None) per destination table,
in the same order as destination_tables.
</ParamField>

</details>

</details>

 <details>
<summary>

**`stream(working_dir: str, *results: list[LazyFrame | None])`**

</summary>

Write each result as a Parquet file and bulk load it into ClickHouse via
the cloud staging area.

<ParamField path="working_dir">
  Temporary directory for intermediate Parquet files.
</ParamField>

<ParamField path="results">
  One polars LazyFrame (or None) per destination table,
in the same order as destination_tables.
</ParamField>

</details>

 <details>
<summary>

**`ConfluentSchemaRegistry(**kwargs)`**

</summary>

Bases: `KafkaSchemaRegistry`

Configuration for Confluent Schema Registry.

<details>
<summary>`configs`</summary>

Additional configuration properties for the Schema Registry client.

</details>

<details>
<summary>`credentials`</summary>

Credentials for authenticating with the Schema Registry.

</details>

<details>
<summary>`url`</summary>

The URL of the Schema Registry.

</details>

</details>

 <details>
<summary>`ConfluentSchemaRegistryCredentialsSpec`</summary>

alias of UserPasswordCredentials

</details>

 <details>
<summary>

**`Conn`**

</summary>

Bases: [object](https://docs.python.org/3/library/functions.html#object)

Represents a connection configuration. This is a base class.

</details>

 <details>
<summary>

**`CronTrigger(mask: str, start: datetime | None = None, end: datetime | None = None)`**

</summary>

Bases: [object](https://docs.python.org/3/library/functions.html#object)

Represents a cron-based trigger for scheduling Tabsdata publishers,
subscribers and transformers.

This class defines a trigger based on a cron expression, with optional
start and end times to constrain its activation period. It validates the
cron mask and time boundaries to ensure the trigger is well-formed.

<details>
<summary>`end`</summary>

Gets the end time of the trigger as a timezone-aware ISO 8601 string
with ‘Z’ timezone.

 **Returns:** The end time in ISO format, or None if not set.

</details>

<details>
<summary>`mask`</summary>

Gets the cron expression mask.

 **Returns:** The cron expression string.

</details>

<details>
<summary>`start`</summary>

Gets the start time of the trigger as a timezone-aware ISO 8601 string
with ‘Z’ timezone.

 **Returns:** The start time in ISO format, or None if not set.

</details>

</details>

 <details>
<summary>

**`CustomException(message: str, error_code=None)`**

</summary>

Bases: [Exception](https://docs.python.org/3/library/exceptions.html#Exception)

Custom exception for execution errors.

</details>

 <details>
<summary>

**`DatabricksDestination(host_url: str, token: str | Secret, tables: list[str] | str, volume: str, catalog: str | None = None, schema: str | None = None, warehouse: str | None = None, warehouse_id: str | None = None, if_table_exists: Literal['append', 'replace'] = 'append', schema_strategy: Literal['update', 'strict'] = 'update', **kwargs)`**

</summary>

Bases: DestinationPlugin

Databricks based data outputs.

<details>
<summary>

**`chunk(working_dir: str, *results: LazyFrame | None)`**

</summary>

Trigger the exporting of the data to local parquet chunks. This method will
receive the resulting data from the user function and must store it in
the local system as parquet files, using the working_dir. Note: This
method should *not* materialize the data, it should only store it in the
local system.

<ParamField path="working_dir" type="str">
  The folder where any files generated must
be stored (this refers to temporary files that will be deleted after
the execution of the plugin, not the final destination of the data)
</ParamField>

<ParamField path="results">
  The data to be exported. It is a list of polars LazyFrames or None.
</ParamField>

**Returns:** A list of the intermediate files created

</details>

<details>
<summary>`host_url`</summary>

</details>

<details>
<summary>`if_table_exists`</summary>

The strategy to follow when the table already exists.

 **Type:** str

</details>

<details>
<summary>`schema_strategy`</summary>

The strategy to follow when appending to a table with an existing schema.

 **Type:** str

</details>

<details>
<summary>

**`stream(working_dir: str, *results: List[LazyFrame | None] | LazyFrame | None)`**

</summary>

Trigger the exporting of the data. This method will receive the resulting data
from the user function and must store it in the desired location.
Note: this method *might* materialize the data provided in a single chunk
generated by the chunk function if invoked, so chunks should be of an
appropriate size.

<ParamField path="working_dir" type="str">
  The folder where any intermediate files generated must
be stored (this refers to temporary files that will be deleted after
the execution of the plugin, not the final destination of the data)
</ParamField>

<ParamField path="results">
  The data to be exported. It is a list of polars LazyFrames or None.
</ParamField>

**Returns:** None

</details>

<details>
<summary>`tables`</summary>

</details>

<details>
<summary>`token`</summary>

</details>

<details>
<summary>

**`write(files)`**

</summary>

This method is used to write the files to the databricks. It is called
from the stream method, and it is not intended to be called directly.

</details>

</details>

 <details>
<summary>

**`chunk(working_dir: str, *results: LazyFrame | None)`**

</summary>

Trigger the exporting of the data to local parquet chunks. This method will
receive the resulting data from the user function and must store it in
the local system as parquet files, using the working_dir. Note: This
method should *not* materialize the data, it should only store it in the
local system.

<ParamField path="working_dir" type="str">
  The folder where any files generated must
be stored (this refers to temporary files that will be deleted after
the execution of the plugin, not the final destination of the data)
</ParamField>

<ParamField path="results">
  The data to be exported. It is a list of polars LazyFrames or None.
</ParamField>

**Returns:** A list of the intermediate files created

</details>

 <details>
<summary>

**`stream(working_dir: str, *results: List[LazyFrame | None] | LazyFrame | None)`**

</summary>

Trigger the exporting of the data. This method will receive the resulting data
from the user function and must store it in the desired location.
Note: this method *might* materialize the data provided in a single chunk
generated by the chunk function if invoked, so chunks should be of an
appropriate size.

<ParamField path="working_dir" type="str">
  The folder where any intermediate files generated must
be stored (this refers to temporary files that will be deleted after
the execution of the plugin, not the final destination of the data)
</ParamField>

<ParamField path="results">
  The data to be exported. It is a list of polars LazyFrames or None.
</ParamField>

**Returns:** None

</details>

 <details>
<summary>

**`write(files)`**

</summary>

This method is used to write the files to the databricks. It is called
from the stream method, and it is not intended to be called directly.

</details>

 <details>
<summary>

**`DatabricksSource(host_url: str, token: str | Secret, queries: list[str] | str, initial_values: dict | None = None, warehouse: str | None = None, warehouse_id: str | None = None, **kwargs)`**

</summary>

Bases: SourcePlugin

Databricks based data inputs.

<details>
<summary>

**`chunk(working_dir: str)`**

</summary>

Trigger the import of the data. This must be implemented in any class that
inherits from this class unless directly implementing streaming. The method will
receive a folder where it must store the data as parquet files, and return a
list of the paths of the files created. This files will then be loaded and
mapped to the dataset function in positional order, so if you want file.parquet
to be the first argument of the dataset function, you must return it first. If
you want a parameter to receive multiple files, return a list of the paths. For
example, you would give the following return to provide a first argument with a
single file and a second argument with two files:return
[“file1.parquet”, [“file2.parquet”, “file3.parquet”]]

**Returns:** The path of the file(s) created, inthe order they must be mapped to the dataset function

**Return type:** Union[str, Tuple[str, …], List[str]]

</details>

<details>
<summary>`host_url`</summary>

</details>

<details>
<summary>`initial_values`</summary>

The initial values for the parameters in the SQL queries.

</details>

<details>
<summary>`queries`</summary>

</details>

<details>
<summary>`token`</summary>

</details>

</details>

 <details>
<summary>

**`chunk(working_dir: str)`**

</summary>

Trigger the import of the data. This must be implemented in any class that
inherits from this class unless directly implementing streaming. The method will
receive a folder where it must store the data as parquet files, and return a
list of the paths of the files created. This files will then be loaded and
mapped to the dataset function in positional order, so if you want file.parquet
to be the first argument of the dataset function, you must return it first. If
you want a parameter to receive multiple files, return a list of the paths. For
example, you would give the following return to provide a first argument with a
single file and a second argument with two files:return
[“file1.parquet”, [“file2.parquet”, “file3.parquet”]]

**Returns:** The path of the file(s) created, inthe order they must be mapped to the dataset function

**Return type:** Union[str, Tuple[str, …], List[str]]

</details>

 <details>
<summary>

**`Db2CdcConn(**kwargs)`**

</summary>

Bases: CdcConn, Db2Conn

Db2 CDC connection configuration.

</details>

 <details>
<summary>

**`Db2CdcTrigger(**kwargs)`**

</summary>

Bases: CdcTrigger

A CdcTrigger that captures changes from a Db2 database using SQL Replication.

This trigger connects to a Db2 server, reads change data capture events (inserts,
updates, deletes) on specified tables via SQL Replication, and stages them.

<details>
<summary>`blocking_timeout_seconds`</summary>

Timeout in seconds for blocking reads from the replication stream.

</details>

<details>
<summary>

**`collect_tables(session: Any, cdc_format: CdcFormat = CdcFormat(values_format='columns', flatten_values=True))`**

</summary>

</details>

<details>
<summary>

**`harvest_tables(session: Any, cdc_format: CdcFormat = CdcFormat(values_format='columns', flatten_values=True))`**

</summary>

</details>

<details>
<summary>`start_from`</summary>

Position to start reading changes from.

</details>

</details>

 <details>
<summary>

**`collect_tables(session: Any, cdc_format: CdcFormat = CdcFormat(values_format='columns', flatten_values=True))`**

</summary>

</details>

 <details>
<summary>

**`harvest_tables(session: Any, cdc_format: CdcFormat = CdcFormat(values_format='columns', flatten_values=True))`**

</summary>

</details>

 <details>
<summary>

**`Db2Conn(**kwargs)`**

</summary>

Bases: Conn

Represents a connection configuration to Db2.

<details>
<summary>`credentials`</summary>

The credentials required to access the Db2 database. If no credentials were
provided, it will return None.

</details>

<details>
<summary>`cx_dst_configs_db2`</summary>

Destination-specific configuration parameters for the Db2 connection.

</details>

<details>
<summary>`cx_src_configs_db2`</summary>

Source-specific configuration parameters for the Db2 connection.

</details>

<details>
<summary>`enforce_connection_params`</summary>

Whether to enforce connection parameters for the Db2 connection.

</details>

<details>
<summary>`uri`</summary>

The URI of the database where the data is located.

</details>

</details>

 <details>
<summary>

**`Db2Src(**kwargs)`**

</summary>

Bases: SourcePlugin

Source plugin for reading data from Db2.

<details>
<summary>`chunk_size`</summary>

The chunk size in records for reading large queries in batches.

</details>

<details>
<summary>`conn`</summary>

The Db2 connection configuration.

</details>

<details>
<summary>`initial_values`</summary>

The initial values for the parameters in the SQL queries.

</details>

<details>
<summary>`loader`</summary>

The data processing loader to use for executing SQL imports.

</details>

<details>
<summary>`queries`</summary>

The SQL queries to execute to read data.

</details>

<details>
<summary>`schema_overrides`</summary>

The schema overrides for the tables being read.

</details>

<details>
<summary>

**`stream(working_dir: str)`**

</summary>

Execute the query and load the results as a list of TableFrames.

**Returns:** A list of TableFrames containing the query results.

</details>

<details>
<summary>`transactional`</summary>

Whether to use transactions for reading to ensure consistency.

</details>

</details>

 <details>
<summary>

**`stream(working_dir: str)`**

</summary>

Execute the query and load the results as a list of TableFrames.

**Returns:** A list of TableFrames containing the query results.

</details>

 <details>
<summary>

**`DestinationPlugin`**

</summary>

Bases: [object](https://docs.python.org/3/library/functions.html#object)

Parent class for subscriber connectors.

  trigger_output(working_dir, `*args`, `**kwargs`):Trigger the exporting of the data. This function will
receive the resulting data from the dataset function
and must store it in the desired location.

   <details>
<summary>

**`chunk(working_dir: str, *results: VALID_PLUGIN_RESULT)`**

</summary>

Trigger the exporting of the data to local parquet chunks. This method will
receive the resulting data from the user function and must store it in
the local system as parquet files, using the working_dir. Note: This
method should *not* materialize the data, it should only store it in the
local system.

<ParamField path="working_dir" type="str">
  The folder where any files generated must
be stored (this refers to temporary files that will be deleted after
the execution of the plugin, not the final destination of the data)
</ParamField>

<ParamField path="results">
  The data to be exported. It is a list of polars LazyFrames or None.
</ParamField>

**Returns:** A list of the intermediate files created

</details>

<details>
<summary>

**`stream(working_dir: str, *results: VALID_PLUGIN_RESULT)`**

</summary>

Trigger the exporting of the data. This method will receive the resulting data
from the user function and must store it in the desired location.
Note: this method *might* materialize the data provided in a single chunk
generated by the chunk function if invoked, so chunks should be of an
appropriate size.

<ParamField path="working_dir" type="str">
  The folder where any intermediate files generated must
be stored (this refers to temporary files that will be deleted after
the execution of the plugin, not the final destination of the data)
</ParamField>

<ParamField path="results">
  The data to be exported. It is a list of polars LazyFrames or None.
</ParamField>

**Returns:** None

</details>

<details>
<summary>

**`write(files: List[str | List[str] | List[List[str]]])`**

</summary>

Given a file or a list of files, write to the desired destination. Note: this
method *might* materialize the data in the files it receives, so chunks
should be of an appropriate size.

</details>

</details>

 <details>
<summary>

**`chunk(working_dir: str, *results: VALID_PLUGIN_RESULT)`**

</summary>

Trigger the exporting of the data to local parquet chunks. This method will
receive the resulting data from the user function and must store it in
the local system as parquet files, using the working_dir. Note: This
method should *not* materialize the data, it should only store it in the
local system.

<ParamField path="working_dir" type="str">
  The folder where any files generated must
be stored (this refers to temporary files that will be deleted after
the execution of the plugin, not the final destination of the data)
</ParamField>

<ParamField path="results">
  The data to be exported. It is a list of polars LazyFrames or None.
</ParamField>

**Returns:** A list of the intermediate files created

</details>

 <details>
<summary>

**`stream(working_dir: str, *results: VALID_PLUGIN_RESULT)`**

</summary>

Trigger the exporting of the data. This method will receive the resulting data
from the user function and must store it in the desired location.
Note: this method *might* materialize the data provided in a single chunk
generated by the chunk function if invoked, so chunks should be of an
appropriate size.

<ParamField path="working_dir" type="str">
  The folder where any intermediate files generated must
be stored (this refers to temporary files that will be deleted after
the execution of the plugin, not the final destination of the data)
</ParamField>

<ParamField path="results">
  The data to be exported. It is a list of polars LazyFrames or None.
</ParamField>

**Returns:** None

</details>

 <details>
<summary>

**`write(files: List[str | List[str] | List[List[str]]])`**

</summary>

Given a file or a list of files, write to the desired destination. Note: this
method *might* materialize the data in the files it receives, so chunks
should be of an appropriate size.

</details>

 <details>
<summary>

**`DruidConn(**kwargs)`**

</summary>

Bases: Conn

Represents a connection configuration for Apache Druid with cloud-staged
bulk loading.

Data is first uploaded to the configured cloud staging area (S3, Azure
Blob, or GCS) and then ingested into Druid via the Overlord REST API
using a native batch `index_parallel` ingestion task.

<details>
<summary>`credentials`</summary>

The credentials used for HTTP Basic authentication.  `None` if no
credentials were provided.

</details>

<details>
<summary>`http_timeout_sec`</summary>

Timeout in seconds for HTTP requests to the Overlord API.

</details>

<details>
<summary>`max_concurrent_subtasks`</summary>

Maximum number of parallel sub-tasks inside the ingestion task.

</details>

<details>
<summary>`max_rows_in_memory`</summary>

In-memory buffer size during ingestion.

</details>

<details>
<summary>`max_rows_per_segment`</summary>

Maximum number of rows per segment (dynamic partitioning threshold).

</details>

<details>
<summary>`overlord_url`</summary>

The base URL of the Druid Overlord REST API.

</details>

<details>
<summary>`segment_granularity`</summary>

Time-based partitioning granularity for new segments.

</details>

<details>
<summary>`staging`</summary>

The cloud storage staging configuration (S3Config, AzureConfig, or
GCSConfig).

</details>

<details>
<summary>`time_column`</summary>

The PyArrow column to use as Druid’s `__time` primary timestamp.
`None` if a constant value should be used.

</details>

</details>

 <details>
<summary>

**`DruidDest(**kwargs)`**

</summary>

Bases: DestinationPlugin

Destination plugin for writing data to Apache Druid using cloud-staged
bulk loading.

Data is first written as Parquet files to the configured cloud storage
staging area (S3, Azure Blob, or GCS) and then ingested into Druid via
a native batch `index_parallel` task submitted to the Overlord REST API.

<details>
<summary>`conn`</summary>

The Druid connection configuration.

</details>

<details>
<summary>`destination_tables`</summary>

The datasource(s) to load data into.

</details>

<details>
<summary>`if_table_exists`</summary>

The strategy to follow when the datasource already exists.

</details>

<details>
<summary>

**`stream(working_dir: str, *results: list[LazyFrame | None])`**

</summary>

Write each result as a Parquet file and bulk load it into Druid via
the cloud staging area.

<ParamField path="working_dir">
  Temporary directory for intermediate Parquet files.
</ParamField>

<ParamField path="results">
  One polars LazyFrame (or None) per destination
datasource, in the same order as destination_tables.
</ParamField>

</details>

</details>

 <details>
<summary>

**`stream(working_dir: str, *results: list[LazyFrame | None])`**

</summary>

Write each result as a Parquet file and bulk load it into Druid via
the cloud staging area.

<ParamField path="working_dir">
  Temporary directory for intermediate Parquet files.
</ParamField>

<ParamField path="results">
  One polars LazyFrame (or None) per destination
datasource, in the same order as destination_tables.
</ParamField>

</details>

 <details>
<summary>

**`EnvironmentSecret(environment_variable_name: str)`**

</summary>

Bases: `Secret`

Secrets class representing a secret obtained from an environment variable in the
server.

<details>
<summary>`secret_value`</summary>

Get the secret value pointed at by the secret. To be used only during execution
in the backend.

 **Returns:** The secret value.

**Return type:** str

</details>

<ParamField path="environment_variable_name" type="str">
  Name of the environment variable from which
we will obtain the secret value.
</ParamField>

<ParamField path="dict" type="to_dict">
  Convert the EnvironmentSecret object to a dictionary.
</ParamField>

<ParamField path="str" type="secret_value">
  Get the secret value.
</ParamField>

</details>

 <details>
<summary>

**`FileInfo(*values)`**

</summary>

Bases: [StrEnum](https://docs.python.org/3/library/enum.html#enum.StrEnum)

</details>

 <details>
<summary>

**`GCPServiceAccountKeyCredentials(service_account_key: str | Secret)`**

</summary>

Bases: `GCPCredentials`

Credentials class to store the credentials needed to access GCS
using account key credentials (service account key).

<details>
<summary>`service_account_key`</summary>

The GCS service account key.

 **Type:** Secret

</details>

<ParamField path="service_account_key" type="Secret">
  The GCS service account key.
</ParamField>

<ParamField path="dict" type="to_dict">
  Convert the GCPServiceAccountKeyCredentials object to a
dictionary
</ParamField>

</details>

 <details>
<summary>

**`GCSConfig(bucket: str, base_path: str, upload_credentials: GCPServiceAccountKeyCredentials | None = None, hmac_credentials: HMACCredentials | None = None)`**

</summary>

Bases: [object](https://docs.python.org/3/library/functions.html#object)

Configuration for staging files in Google Cloud Storage.

<details>
<summary>`base_path`</summary>

</details>

<details>
<summary>`bucket`</summary>

</details>

<details>
<summary>`hmac_credentials`</summary>

</details>

<details>
<summary>`upload_credentials`</summary>

</details>

<ParamField path="bucket">
  GCS bucket name.
</ParamField>

<ParamField path="base_path">
  Object path prefix for staged files
(e.g. "staging/clickhouse").
</ParamField>

<ParamField path="upload_credentials">
  Optional GCP service-account credentials used for
uploading staged files via the GCS SDK. When None, application
default credentials (ADC) are used.
</ParamField>

<ParamField path="hmac_credentials">
  Optional HMACCredentials
carrying GCS HMAC access key and secret for S3-compatible access.
When None, the storage backend operates without explicit
credentials (suitable for public buckets or server-side IAM).
</ParamField>

</details>

 <details>
<summary>

**`GCSDestination(uri: str | list[str], credentials: GCPCredentials, format: str | FileFormat = None)`**

</summary>

Bases: DestinationPlugin

GCS-file-based data outputs.

<details>
<summary>

**`SupportedFormats(*values)`**

</summary>

Bases: [Enum](https://docs.python.org/3/library/enum.html#enum.Enum)

Enum for the supported formats for the GCSDestination.

<details>
<summary>`avro`</summary>

</details>

<details>
<summary>`csv`</summary>

</details>

<details>
<summary>`ndjson`</summary>

</details>

<details>
<summary>`parquet`</summary>

</details>

</details>

<details>
<summary>`allow_fragments`</summary>

Whether to allow fragments in the output.

 **Type:** bool

</details>

<details>
<summary>

**`chunk(working_dir: str, *results)`**

</summary>

Trigger the exporting of the data to local parquet chunks. This method will
receive the resulting data from the user function and must store it in
the local system as parquet files, using the working_dir. Note: This
method should *not* materialize the data, it should only store it in the
local system.

<ParamField path="working_dir" type="str">
  The folder where any files generated must
be stored (this refers to temporary files that will be deleted after
the execution of the plugin, not the final destination of the data)
</ParamField>

<ParamField path="results">
  The data to be exported. It is a list of polars LazyFrames or None.
</ParamField>

**Returns:** A list of the intermediate files created

</details>

<details>
<summary>`credentials`</summary>

The credentials required to access GCS.

 **Type:** GCPCredentials

</details>

<details>
<summary>`format`</summary>

The format of the file. If not provided, it will be inferred from
the file extension of the URI.

 **Type:** FileFormat

</details>

<details>
<summary>

**`stream(working_dir: str, *results)`**

</summary>

Trigger the exporting of the data. This method will receive the resulting data
from the user function and must store it in the desired location.
Note: this method *might* materialize the data provided in a single chunk
generated by the chunk function if invoked, so chunks should be of an
appropriate size.

<ParamField path="working_dir" type="str">
  The folder where any intermediate files generated must
be stored (this refers to temporary files that will be deleted after
the execution of the plugin, not the final destination of the data)
</ParamField>

<ParamField path="results">
  The data to be exported. It is a list of polars LazyFrames or None.
</ParamField>

**Returns:** None

</details>

<details>
<summary>`uri`</summary>

‘gs://path/to/files’.

 **Type:** str | list[str]

**Type:** The URI of the files with format

</details>

</details>

 <details>
<summary>

**`chunk(working_dir: str, *results)`**

</summary>

Trigger the exporting of the data to local parquet chunks. This method will
receive the resulting data from the user function and must store it in
the local system as parquet files, using the working_dir. Note: This
method should *not* materialize the data, it should only store it in the
local system.

<ParamField path="working_dir" type="str">
  The folder where any files generated must
be stored (this refers to temporary files that will be deleted after
the execution of the plugin, not the final destination of the data)
</ParamField>

<ParamField path="results">
  The data to be exported. It is a list of polars LazyFrames or None.
</ParamField>

**Returns:** A list of the intermediate files created

</details>

 <details>
<summary>

**`stream(working_dir: str, *results)`**

</summary>

Trigger the exporting of the data. This method will receive the resulting data
from the user function and must store it in the desired location.
Note: this method *might* materialize the data provided in a single chunk
generated by the chunk function if invoked, so chunks should be of an
appropriate size.

<ParamField path="working_dir" type="str">
  The folder where any intermediate files generated must
be stored (this refers to temporary files that will be deleted after
the execution of the plugin, not the final destination of the data)
</ParamField>

<ParamField path="results">
  The data to be exported. It is a list of polars LazyFrames or None.
</ParamField>

**Returns:** None

</details>

 <details>
<summary>

**`GCSSource(uri: str | list[str], credentials: GCPCredentials, format: str | FileFormat = None, initial_last_modified: str | datetime = None, file_info: str | FileInfo | None = None)`**

</summary>

Bases: SourcePlugin

GCS-file-based data inputs.

<details>
<summary>

**`SupportedFormats(*values)`**

</summary>

Bases: [Enum](https://docs.python.org/3/library/enum.html#enum.Enum)

Enum for the supported formats for the GCSSource.

<details>
<summary>`avro`</summary>

</details>

<details>
<summary>`csv`</summary>

</details>

<details>
<summary>`log`</summary>

</details>

<details>
<summary>`ndjson`</summary>

</details>

<details>
<summary>`parquet`</summary>

</details>

</details>

<details>
<summary>

**`chunk(working_dir: str)`**

</summary>

Trigger the import of the data. This must be implemented in any class that
inherits from this class unless directly implementing streaming. The method will
receive a folder where it must store the data as parquet files, and return a
list of the paths of the files created. This files will then be loaded and
mapped to the dataset function in positional order, so if you want file.parquet
to be the first argument of the dataset function, you must return it first. If
you want a parameter to receive multiple files, return a list of the paths. For
example, you would give the following return to provide a first argument with a
single file and a second argument with two files:return
[“file1.parquet”, [“file2.parquet”, “file3.parquet”]]

**Returns:** The path of the file(s) created, inthe order they must be mapped to the dataset function

**Return type:** Union[str, Tuple[str, …], List[str]]

</details>

<details>
<summary>`credentials`</summary>

The credentials required to access GCS.

 **Type:** GCPCredentials

</details>

<details>
<summary>`file_info`</summary>

The file information to add as system columns.

 **Type:** FileInfo | None

</details>

<details>
<summary>`format`</summary>

The format of the file. If not provided, it will be inferred from
the file extension of the data.

 **Type:** FileFormat

</details>

<details>
<summary>`initial_last_modified`</summary>

The date and time after which the files were modified.

 **Type:** str

</details>

<details>
<summary>`initial_values`</summary>

Return a dictionary with the initial values to be stored after execution of
the plugin. They will be accessible in the next execution of the plugin.
The dictionary must have the parameter names as keys and the initial values
as values, all the type string.

 **Returns:** A dictionary with the initial values of the parameters of the plugin.

**Return type:** dict

</details>

<details>
<summary>`uri`</summary>

‘gs://path/to/files’.

 **Type:** str | list[str]

**Type:** The URI of the files with format

</details>

</details>

 <details>
<summary>

**`chunk(working_dir: str)`**

</summary>

Trigger the import of the data. This must be implemented in any class that
inherits from this class unless directly implementing streaming. The method will
receive a folder where it must store the data as parquet files, and return a
list of the paths of the files created. This files will then be loaded and
mapped to the dataset function in positional order, so if you want file.parquet
to be the first argument of the dataset function, you must return it first. If
you want a parameter to receive multiple files, return a list of the paths. For
example, you would give the following return to provide a first argument with a
single file and a second argument with two files:return
[“file1.parquet”, [“file2.parquet”, “file3.parquet”]]

**Returns:** The path of the file(s) created, inthe order they must be mapped to the dataset function

**Return type:** Union[str, Tuple[str, …], List[str]]

</details>

 <details>
<summary>

**`HMACCredentials(access_key: str | Secret, secret_key: str | Secret)`**

</summary>

Bases: `Credentials`

Generic HMAC credentials consisting of an access key and a secret key.

These credentials follow the same shape as AWS S3 access keys and are
compatible with any service that uses HMAC-based authentication over the
S3-compatible API, such as GCS HMAC keys or MinIO credentials.

<details>
<summary>`access_key`</summary>

The HMAC access key id.

 **Type:** Secret

</details>

<details>
<summary>`secret_key`</summary>

The HMAC secret key.

 **Type:** Secret

</details>

<ParamField path="access_key" type="Secret">
  The HMAC access key id.
</ParamField>

<ParamField path="secret_key" type="Secret">
  The HMAC secret key.
</ParamField>

<ParamField path="dict" type="to_dict">
  Convert the HMACCredentials object to a dictionary.
</ParamField>

</details>

 <details>
<summary>

**`HashiCorpSecret(path: str, name: str, vault: str = 'HASHICORP')`**

</summary>

Bases: `Secret`

Secrets class representing a secret stored in Hashicorp Vault.

<details>
<summary>`secret_value`</summary>

Get the secret value pointed at by the secret. To be used only during execution
in the backend.

 **Returns:** The secret value.

**Return type:** str

</details>

<details>
<summary>`vault`</summary>

</details>

<ParamField path="path" type="str">
  The path to the secret in Hashicorp Vault.
</ParamField>

<ParamField path="name" type="str">
  The name of the secret in Hashicorp Vault.
</ParamField>

<ParamField path="vault" type="str">
  If multiple vaults exist in the system, the name of the vault to
use. When executing in the server, the URL and token associated to that
specific vault will be used. The name can only contain uppercase letters,
numbers and underscores, and can’t begin with a number. Defaults to
“HASHICORP”.
</ParamField>

<ParamField path="dict" type="to_dict">
  Convert the HashiCorpSecret object to a dictionary.
</ParamField>

<ParamField path="str" type="secret_value">
  Get the secret value.
</ParamField>

</details>

 <details>
<summary>

**`KafkaConn(**kwargs)`**

</summary>

Bases: Conn

Kafka Consumer Connection configuration.

<details>
<summary>`credentials`</summary>

Kafka credentials.

</details>

<details>
<summary>`cx_src_configs`</summary>

Source-specific configuration parameters for the Kafka connection.

</details>

<details>
<summary>`enforce_connection_params`</summary>

Whether to enforce connection parameters.

</details>

<details>
<summary>`group_id`</summary>

Kafka consumer group ID.

</details>

<details>
<summary>`schema_registry`</summary>

Kafka schema registry configuration.

</details>

<details>
<summary>`servers`</summary>

Kafka bootstrap servers.

</details>

</details>

 <details>
<summary>`KafkaCredentialsSpec`</summary>

alias of UserPasswordCredentials

</details>

 <details>
<summary>

**`KafkaTrigger(**kwargs)`**

</summary>

Bases: StageTrigger

A StageTrigger that consumes messages from a Kafka topic and stages them.

This trigger connects to a Kafka cluster, consumes messages from a specified
topic, and uses rollover conditions (time, size, and message count) to
buffer and stage the data.

<details>
<summary>`conn`</summary>

Kafka consumer connection configuration.

</details>

<details>
<summary>`data_format`</summary>

Data format of the messages (“avro”, “json”, “protobuf”).

</details>

<details>
<summary>`dlq_topic`</summary>

Dead-letter queue topic.

</details>

<details>
<summary>`group_id`</summary>

Kafka consumer group ID.

</details>

<details>
<summary>`messages_rollover`</summary>

Message count-based rollover configuration.

</details>

<details>
<summary>

**`run(context: StageTriggerContext)`**

</summary>

Run the Kafka stage trigger to consume messages, buffer them, and stage them
based on rollover conditions.

</details>

<details>
<summary>`schema`</summary>

Schema string for deserializing messages.

</details>

<details>
<summary>`size_rollover_mb`</summary>

Size-based rollover configuration. Size is specified in megabytes.

</details>

<details>
<summary>`time_rollover_secs`</summary>

Timeout in seconds for consuming each message.

</details>

<details>
<summary>`topic`</summary>

Kafka topic to consume from.

</details>

</details>

 <details>
<summary>

**`run(context: StageTriggerContext)`**

</summary>

Run the Kafka stage trigger to consume messages, buffer them, and stage them
based on rollover conditions.

</details>

 <details>
<summary>

**`LocalFileDestination(path: str | list[str], format: str | FileFormat = None)`**

</summary>

Bases: DestinationPlugin

LocalFile-based data outputs.

<details>
<summary>

**`SupportedFormats(*values)`**

</summary>

Bases: [Enum](https://docs.python.org/3/library/enum.html#enum.Enum)

Enum for the supported formats for the LocalFileDestination.

<details>
<summary>`avro`</summary>

</details>

<details>
<summary>`csv`</summary>

</details>

<details>
<summary>`ndjson`</summary>

</details>

<details>
<summary>`parquet`</summary>

</details>

</details>

<details>
<summary>`allow_fragments`</summary>

Whether to allow fragments in the output.

 **Type:** bool

</details>

<details>
<summary>

**`chunk(working_dir: str, *results)`**

</summary>

Trigger the exporting of the data to local parquet chunks. This method will
receive the resulting data from the user function and must store it in
the local system as parquet files, using the working_dir. Note: This
method should *not* materialize the data, it should only store it in the
local system.

<ParamField path="working_dir" type="str">
  The folder where any files generated must
be stored (this refers to temporary files that will be deleted after
the execution of the plugin, not the final destination of the data)
</ParamField>

<ParamField path="results">
  The data to be exported. It is a list of polars LazyFrames or None.
</ParamField>

**Returns:** A list of the intermediate files created

</details>

<details>
<summary>`format`</summary>

The format of the file or files. If not provided, it will be
inferred from the file extension in the path.

 **Type:** FileFormat

</details>

<details>
<summary>`path`</summary>

The path or paths to store the files.

 **Type:** str | list[str]

</details>

<details>
<summary>

**`stream(working_dir: str, *results)`**

</summary>

Trigger the exporting of the data. This method will receive the resulting data
from the user function and must store it in the desired location.
Note: this method *might* materialize the data provided in a single chunk
generated by the chunk function if invoked, so chunks should be of an
appropriate size.

<ParamField path="working_dir" type="str">
  The folder where any intermediate files generated must
be stored (this refers to temporary files that will be deleted after
the execution of the plugin, not the final destination of the data)
</ParamField>

<ParamField path="results">
  The data to be exported. It is a list of polars LazyFrames or None.
</ParamField>

**Returns:** None

</details>

</details>

 <details>
<summary>

**`chunk(working_dir: str, *results)`**

</summary>

Trigger the exporting of the data to local parquet chunks. This method will
receive the resulting data from the user function and must store it in
the local system as parquet files, using the working_dir. Note: This
method should *not* materialize the data, it should only store it in the
local system.

<ParamField path="working_dir" type="str">
  The folder where any files generated must
be stored (this refers to temporary files that will be deleted after
the execution of the plugin, not the final destination of the data)
</ParamField>

<ParamField path="results">
  The data to be exported. It is a list of polars LazyFrames or None.
</ParamField>

**Returns:** A list of the intermediate files created

</details>

 <details>
<summary>

**`stream(working_dir: str, *results)`**

</summary>

Trigger the exporting of the data. This method will receive the resulting data
from the user function and must store it in the desired location.
Note: this method *might* materialize the data provided in a single chunk
generated by the chunk function if invoked, so chunks should be of an
appropriate size.

<ParamField path="working_dir" type="str">
  The folder where any intermediate files generated must
be stored (this refers to temporary files that will be deleted after
the execution of the plugin, not the final destination of the data)
</ParamField>

<ParamField path="results">
  The data to be exported. It is a list of polars LazyFrames or None.
</ParamField>

**Returns:** None

</details>

 <details>
<summary>

**`LocalFileSource(path: str | list[str], format: str | FileFormat = None, initial_last_modified: str | datetime = None, file_info: str | FileInfo | None = None)`**

</summary>

Bases: SourcePlugin

Local-file-based data inputs.

<details>
<summary>

**`SupportedFormats(*values)`**

</summary>

Bases: [Enum](https://docs.python.org/3/library/enum.html#enum.Enum)

Enum for the supported formats for the LocalFileSource.

<details>
<summary>`avro`</summary>

</details>

<details>
<summary>`csv`</summary>

</details>

<details>
<summary>`log`</summary>

</details>

<details>
<summary>`ndjson`</summary>

</details>

<details>
<summary>`parquet`</summary>

</details>

</details>

<details>
<summary>

**`chunk(working_dir: str)`**

</summary>

Trigger the import of the data. This must be implemented in any class that
inherits from this class unless directly implementing streaming. The method will
receive a folder where it must store the data as parquet files, and return a
list of the paths of the files created. This files will then be loaded and
mapped to the dataset function in positional order, so if you want file.parquet
to be the first argument of the dataset function, you must return it first. If
you want a parameter to receive multiple files, return a list of the paths. For
example, you would give the following return to provide a first argument with a
single file and a second argument with two files:return
[“file1.parquet”, [“file2.parquet”, “file3.parquet”]]

**Returns:** The path of the file(s) created, inthe order they must be mapped to the dataset function

**Return type:** Union[str, Tuple[str, …], List[str]]

</details>

<details>
<summary>`file_info`</summary>

The file information to add as system columns.

 **Type:** FileInfo | None

</details>

<details>
<summary>`format`</summary>

The format of the file or files. If not provided, it will be
inferred  from the file extension in the path.

 **Type:** FileFormat

</details>

<details>
<summary>`initial_last_modified`</summary>

The date and time after which the files were modified.

 **Type:** str

</details>

<details>
<summary>`initial_values`</summary>

Return a dictionary with the initial values to be stored after execution of
the plugin. They will be accessible in the next execution of the plugin.
The dictionary must have the parameter names as keys and the initial values
as values, all the type string.

 **Returns:** A dictionary with the initial values of the parameters of the plugin.

**Return type:** dict

</details>

<details>
<summary>`path`</summary>

The path or paths to the files to load.

 **Type:** str | list[str]

</details>

</details>

 <details>
<summary>

**`chunk(working_dir: str)`**

</summary>

Trigger the import of the data. This must be implemented in any class that
inherits from this class unless directly implementing streaming. The method will
receive a folder where it must store the data as parquet files, and return a
list of the paths of the files created. This files will then be loaded and
mapped to the dataset function in positional order, so if you want file.parquet
to be the first argument of the dataset function, you must return it first. If
you want a parameter to receive multiple files, return a list of the paths. For
example, you would give the following return to provide a first argument with a
single file and a second argument with two files:return
[“file1.parquet”, [“file2.parquet”, “file3.parquet”]]

**Returns:** The path of the file(s) created, inthe order they must be mapped to the dataset function

**Return type:** Union[str, Tuple[str, …], List[str]]

</details>

 <details>
<summary>

**`LogFormat`**

</summary>

Bases: `FileFormat`

Log file format.

</details>

 <details>
<summary>

**`MSSQLConn(connection_string_or_uri: str, credentials: UserPasswordCredentials | None = None, enforce_connection_params: bool = True, cx_src_configs_mssql: dict | None = None, cx_dst_configs_mssql: dict | None = None)`**

</summary>

Bases: Conn

Represents a connection configuration to Microsoft SQL Server (MSSQL).

<details>
<summary>`connection_string_or_uri`</summary>

The connection string or URI for the MSSQL database.

</details>

<details>
<summary>`credentials`</summary>

The credentials used to authenticate with the database.

</details>

<details>
<summary>`cx_dst_configs_mssql`</summary>

The destination configuration parameters for the MSSQL connection.

</details>

<details>
<summary>`cx_src_configs_mssql`</summary>

The source configuration parameters for the MSSQL connection.

</details>

<details>
<summary>`enforce_connection_params`</summary>

Whether to enforce connection parameters for the MSSQL connection.
Applicable only for a connection to a MSSQL Destination.

</details>

<details>
<summary>`uri`</summary>

The URI for the MSSQL database.

</details>

</details>

 <details>
<summary>

**`MSSQLDestination(connection_string: str, destination_table: str | list[str], credentials: dict | UserPasswordCredentials | None = None, server: str | Secret = None, database: str | Secret = None, driver: str | Secret = None, if_table_exists: Literal['append', 'replace'] = 'append', chunk_size: int = 50000, **kwargs)`**

</summary>

Bases: DestinationPlugin

MSSQL-based data outputs.

<details>
<summary>

**`chunk(working_dir: str, *results: LazyFrame | None)`**

</summary>

Trigger the exporting of the data to local parquet chunks. This method will
receive the resulting data from the user function and must store it in
the local system as parquet files, using the working_dir. Note: This
method should *not* materialize the data, it should only store it in the
local system.

<ParamField path="working_dir" type="str">
  The folder where any files generated must
be stored (this refers to temporary files that will be deleted after
the execution of the plugin, not the final destination of the data)
</ParamField>

<ParamField path="results">
  The data to be exported. It is a list of polars LazyFrames or None.
</ParamField>

**Returns:** A list of the intermediate files created

</details>

<details>
<summary>`connection_string`</summary>

Get the connection string for the database.

 **Returns:** The connection string.

**Return type:** str

</details>

<details>
<summary>`credentials`</summary>

The credentials required to access
Microsoft SQL Server. If no credentials were provided, it will return None.

 **Type:** UserPasswordCredentials | None

</details>

<details>
<summary>`destination_table`</summary>

Get the destination table(s) where the data will be stored.

 **Returns:** The destination table(s).

**Return type:** list[str]

</details>

<details>
<summary>`if_table_exists`</summary>

Returns the value of the if_table_exists property.
This property determines what to do if the table already exists.

</details>

<details>
<summary>

**`stream(working_dir: str, *results: list[LazyFrame | None] | LazyFrame | None)`**

</summary>

Trigger the exporting of the data. This method will receive the resulting data
from the user function and must store it in the desired location.
Note: this method *might* materialize the data provided in a single chunk
generated by the chunk function if invoked, so chunks should be of an
appropriate size.

<ParamField path="working_dir" type="str">
  The folder where any intermediate files generated must
be stored (this refers to temporary files that will be deleted after
the execution of the plugin, not the final destination of the data)
</ParamField>

<ParamField path="results">
  The data to be exported. It is a list of polars LazyFrames or None.
</ParamField>

**Returns:** None

</details>

<details>
<summary>

**`write(files)`**

</summary>

This method is used to write the files to the database. It is called
from the stream method, and it is not intended to be called directly.

</details>

</details>

 <details>
<summary>

**`chunk(working_dir: str, *results: LazyFrame | None)`**

</summary>

Trigger the exporting of the data to local parquet chunks. This method will
receive the resulting data from the user function and must store it in
the local system as parquet files, using the working_dir. Note: This
method should *not* materialize the data, it should only store it in the
local system.

<ParamField path="working_dir" type="str">
  The folder where any files generated must
be stored (this refers to temporary files that will be deleted after
the execution of the plugin, not the final destination of the data)
</ParamField>

<ParamField path="results">
  The data to be exported. It is a list of polars LazyFrames or None.
</ParamField>

**Returns:** A list of the intermediate files created

</details>

 <details>
<summary>

**`stream(working_dir: str, *results: list[LazyFrame | None] | LazyFrame | None)`**

</summary>

Trigger the exporting of the data. This method will receive the resulting data
from the user function and must store it in the desired location.
Note: this method *might* materialize the data provided in a single chunk
generated by the chunk function if invoked, so chunks should be of an
appropriate size.

<ParamField path="working_dir" type="str">
  The folder where any intermediate files generated must
be stored (this refers to temporary files that will be deleted after
the execution of the plugin, not the final destination of the data)
</ParamField>

<ParamField path="results">
  The data to be exported. It is a list of polars LazyFrames or None.
</ParamField>

**Returns:** None

</details>

 <details>
<summary>

**`write(files)`**

</summary>

This method is used to write the files to the database. It is called
from the stream method, and it is not intended to be called directly.

</details>

 <details>
<summary>

**`MSSQLSource(**kwargs)`**

</summary>

Bases: SourcePlugin

Microsoft SQL Server based data inputs.

<details>
<summary>

**`chunk(working_dir: str)`**

</summary>

Execute the query and yield chunks of data.

**Yields:** pd.DataFrame – A chunk of data from the query result.

</details>

<details>
<summary>`connection_string`</summary>

Get the connection string for the database.

 **Returns:** The connection string.

**Return type:** str

</details>

<details>
<summary>`credentials`</summary>

The credentials required to access
Microsoft SQL Server. If no credentials were provided, it will return None.

 **Type:** UserPasswordCredentials | None

</details>

<details>
<summary>`initial_values`</summary>

The initial values for the parameters in the SQL queries.

 **Type:** dict

</details>

<details>
<summary>`query`</summary>

Get the SQL query or queries to execute.

 **Returns:** The SQL query or queries.

**Return type:** str | list[str]

</details>

</details>

 <details>
<summary>

**`chunk(working_dir: str)`**

</summary>

Execute the query and yield chunks of data.

**Yields:** pd.DataFrame – A chunk of data from the query result.

</details>

 <details>
<summary>

**`MSSQLSrc(conn: MSSQLConn, queries: str | list[str], initial_values: dict[str, Any] | None = None, transactional: bool = True, chunk_size: Annotated[int, Gt(gt=0)] = 100000, loader: Literal['polars_sqlalchemy', 'pandas'] = 'polars_sqlalchemy', schema_overrides: dict[str, Boolean | Categorical | Date | Datetime | Decimal | Duration | Enum | Float16 | Float32 | Float64 | Int8 | Int16 | Int64 | Int32 | Int128 | Null | String | Time | UInt8 | UInt16 | UInt32 | UInt64] | list[dict[str, Boolean | Categorical | Date | Datetime | Decimal | Duration | Enum | Float16 | Float32 | Float64 | Int8 | Int16 | Int64 | Int32 | Int128 | Null | String | Time | UInt8 | UInt16 | UInt32 | UInt64]] | None = None)`**

</summary>

Bases: SourcePlugin

Source Plugin for reading data from a Microsoft SQL Server (MSSQL) database.

<details>
<summary>`chunk_size`</summary>

The chunk size in records for reading large queries in batches.

</details>

<details>
<summary>`conn`</summary>

The MSSQL connection configuration.

</details>

<details>
<summary>`initial_values`</summary>

The initial values for the parameters in the SQL queries.

</details>

<details>
<summary>`loader`</summary>

The data processing loader to use for executing SQL imports.

</details>

<details>
<summary>`queries`</summary>

The SQL queries to execute to read data.

</details>

<details>
<summary>`schema_overrides`</summary>

The schema overrides for the tables being read.

</details>

<details>
<summary>

**`stream(working_dir: str)`**

</summary>

Execute the query and load the results as a list of TableFrames.

**Returns:** A list of TableFrames containing the query results.

</details>

<details>
<summary>`transactional`</summary>

Whether to use transactions for reading to ensure consistency.

</details>

</details>

 <details>
<summary>

**`stream(working_dir: str)`**

</summary>

Execute the query and load the results as a list of TableFrames.

**Returns:** A list of TableFrames containing the query results.

</details>

 <details>
<summary>

**`MariaDBConn(uri: str, credentials: UserPasswordCredentials | None = None, enforce_connection_params: bool = True, cx_src_configs_mariadb: dict | None = None, cx_dst_configs_mariadb: dict | None = None)`**

</summary>

Bases: Conn

Represents a connection configuration to MariaDB.

<details>
<summary>`credentials`</summary>

The credentials required to access the MariaDB database. If no credentials were
provided, it will return None.

</details>

<details>
<summary>`cx_dst_configs_mariadb`</summary>

Destination-specific configuration parameters for the MariaDB connection.

</details>

<details>
<summary>`cx_src_configs_mariadb`</summary>

Source-specific configuration parameters for the MariaDB connection.

</details>

<details>
<summary>`enforce_connection_params`</summary>

Whether to enforce connection parameters for the MariaDB connection.

</details>

<details>
<summary>`uri`</summary>

The URI of the database where the data is located.

</details>

</details>

 <details>
<summary>

**`MariaDBDest(conn: MariaDBConn, destination_tables: str | list[str], if_table_exists: Literal['append', 'replace'] = 'append', transactional: bool = True, chunk_size: Annotated[int, Gt(gt=0)] = 100000, loader: Literal['polars_sqlalchemy'] = 'polars_sqlalchemy')`**

</summary>

Bases: DestinationPlugin

Destination plugin for writing data to MySQL.

<details>
<summary>`chunk_size`</summary>

The chunk size for writing large results in batches.

</details>

<details>
<summary>`conn`</summary>

The MariaDB connection configuration.

</details>

<details>
<summary>`destination_tables`</summary>

The table(s) to create. If multiple tables are provided,
they must be provided as a list.

</details>

<details>
<summary>`if_table_exists`</summary>

The strategy to follow when the table already exists.

</details>

<details>
<summary>`loader`</summary>

The data processing loader to use for executing DB write operation.

</details>

<details>
<summary>

**`stream(working_dir: str, *results: list[LazyFrame | None])`**

</summary>

Store the results into the MariaDB database.

<ParamField path="working_dir">
  The working directory where the results are stored.
</ParamField>

<ParamField path="results">
  The results to store in the SQL destination.
</ParamField>

</details>

<details>
<summary>`transactional`</summary>

Whether to use transactions for writing data to ensure consistency.

</details>

</details>

 <details>
<summary>

**`stream(working_dir: str, *results: list[LazyFrame | None])`**

</summary>

Store the results into the MariaDB database.

<ParamField path="working_dir">
  The working directory where the results are stored.
</ParamField>

<ParamField path="results">
  The results to store in the SQL destination.
</ParamField>

</details>

 <details>
<summary>

**`MariaDBDestination(**kwargs)`**

</summary>

Bases: DestinationPlugin

MariaDB-based data outputs.

<details>
<summary>

**`chunk(working_dir: str, *results: LazyFrame | None)`**

</summary>

Store the results in the SQL destination.

<ParamField path="working_dir">
  The working directory where the results will be stored.
</ParamField>

<ParamField path="results" type="list">
  The results to store in the SQL
destination.
</ParamField>

</details>

<details>
<summary>`credentials`</summary>

The credentials required to access the MariaDB
database.

 **Type:** UserPasswordCredentials

</details>

<details>
<summary>`destination_table`</summary>

The table(s) to create. If multiple tables are provided,
they must be provided as a list.

 **Type:** str | List[str]

</details>

<details>
<summary>`if_table_exists`</summary>

The strategy to follow when the table already exists.

 **Type:** str

</details>

<details>
<summary>`uri`</summary>

The URI of the database where the data is going to be stored.

 **Type:** str

</details>

<details>
<summary>

**`write(files: list[str | None])`**

</summary>

Given a file or a list of files, write to the desired destination. Note: this
method *might* materialize the data in the files it receives, so chunks
should be of an appropriate size.

</details>

</details>

 <details>
<summary>

**`chunk(working_dir: str, *results: LazyFrame | None)`**

</summary>

Store the results in the SQL destination.

<ParamField path="working_dir">
  The working directory where the results will be stored.
</ParamField>

<ParamField path="results" type="list">
  The results to store in the SQL
destination.
</ParamField>

</details>

 <details>
<summary>

**`write(files: list[str | None])`**

</summary>

Given a file or a list of files, write to the desired destination. Note: this
method *might* materialize the data in the files it receives, so chunks
should be of an appropriate size.

</details>

 <details>
<summary>

**`MariaDBSource(**kwargs)`**

</summary>

Bases: SourcePlugin

MariaDB-based data inputs.

<details>
<summary>

**`chunk(working_dir: str)`**

</summary>

Trigger the import of the data. This must be implemented in any class that
inherits from this class unless directly implementing streaming. The method will
receive a folder where it must store the data as parquet files, and return a
list of the paths of the files created. This files will then be loaded and
mapped to the dataset function in positional order, so if you want file.parquet
to be the first argument of the dataset function, you must return it first. If
you want a parameter to receive multiple files, return a list of the paths. For
example, you would give the following return to provide a first argument with a
single file and a second argument with two files:return
[“file1.parquet”, [“file2.parquet”, “file3.parquet”]]

**Returns:** The path of the file(s) created, inthe order they must be mapped to the dataset function

**Return type:** Union[str, Tuple[str, …], List[str]]

</details>

<details>
<summary>`credentials`</summary>

The credentials required to access
MariaDB. If no credentials were provided, it will return None.

 **Type:** UserPasswordCredentials | None

</details>

<details>
<summary>`initial_values`</summary>

The initial values for the parameters in the SQL queries.

 **Type:** dict

</details>

<details>
<summary>`query`</summary>

The SQL query(s) to execute.

 **Type:** str | List[str]

</details>

<details>
<summary>`uri`</summary>

The URI of the database where the data is located.

 **Type:** str

</details>

</details>

 <details>
<summary>

**`chunk(working_dir: str)`**

</summary>

Trigger the import of the data. This must be implemented in any class that
inherits from this class unless directly implementing streaming. The method will
receive a folder where it must store the data as parquet files, and return a
list of the paths of the files created. This files will then be loaded and
mapped to the dataset function in positional order, so if you want file.parquet
to be the first argument of the dataset function, you must return it first. If
you want a parameter to receive multiple files, return a list of the paths. For
example, you would give the following return to provide a first argument with a
single file and a second argument with two files:return
[“file1.parquet”, [“file2.parquet”, “file3.parquet”]]

**Returns:** The path of the file(s) created, inthe order they must be mapped to the dataset function

**Return type:** Union[str, Tuple[str, …], List[str]]

</details>

 <details>
<summary>

**`MariaDBSrc(conn: MariaDBConn, queries: str | list[str], initial_values: dict[str, Any] | None = None, transactional: bool = True, chunk_size: Annotated[int, Gt(gt=0)] = 100000, loader: Literal['polars_sqlalchemy', 'pandas'] = 'polars_sqlalchemy', schema_overrides: dict[str, Boolean | Categorical | Date | Datetime | Decimal | Duration | Enum | Float16 | Float32 | Float64 | Int8 | Int16 | Int64 | Int32 | Int128 | Null | String | Time | UInt8 | UInt16 | UInt32 | UInt64] | list[dict[str, Boolean | Categorical | Date | Datetime | Decimal | Duration | Enum | Float16 | Float32 | Float64 | Int8 | Int16 | Int64 | Int32 | Int128 | Null | String | Time | UInt8 | UInt16 | UInt32 | UInt64]] | None = None)`**

</summary>

Bases: SourcePlugin

Source plugin for reading data from MariaDB.

<details>
<summary>`chunk_size`</summary>

The chunk size in records for reading large queries in batches.

</details>

<details>
<summary>`conn`</summary>

The MariaDB connection configuration.

</details>

<details>
<summary>`initial_values`</summary>

The initial values for the parameters in the SQL queries.

</details>

<details>
<summary>`loader`</summary>

The data processing loader to use for executing SQL imports.

</details>

<details>
<summary>`queries`</summary>

The SQL queries to execute to read data.

</details>

<details>
<summary>`schema_overrides`</summary>

</details>

<details>
<summary>

**`stream(working_dir: str)`**

</summary>

Execute the query and load the results as a list of TableFrames.

**Returns:** A list of TableFrames containing the query results.

</details>

<details>
<summary>`transactional`</summary>

Whether to use transactions for reading to ensure consistency.

</details>

</details>

 <details>
<summary>

**`stream(working_dir: str)`**

</summary>

Execute the query and load the results as a list of TableFrames.

**Returns:** A list of TableFrames containing the query results.

</details>

 <details>
<summary>

**`MongoDBDestination(uri: str, collections_with_ids: tuple[str, str | None] | List[tuple[str, str | None]], credentials: UserPasswordCredentials = None, connection_options: dict = None, if_collection_exists: Literal['append', 'replace'] = 'append', use_trxs: bool = False, docs_per_trx: int = 1000, maintain_order: bool = False, update_existing: bool = True, fail_on_duplicate_key: bool = True, log_intermediate_files: bool = False, **kwargs)`**

</summary>

Bases: DestinationPlugin

MongoDB-based data outputs.

<details>
<summary>

**`chunk(working_dir: str, *results: LazyFrame | None)`**

</summary>

Trigger the exporting of the data to local parquet chunks. This method will
receive the resulting data from the user function and must store it in
the local system as parquet files, using the working_dir. Note: This
method should *not* materialize the data, it should only store it in the
local system.

<ParamField path="working_dir" type="str">
  The folder where any files generated must
be stored (this refers to temporary files that will be deleted after
the execution of the plugin, not the final destination of the data)
</ParamField>

<ParamField path="results">
  The data to be exported. It is a list of polars LazyFrames or None.
</ParamField>

**Returns:** A list of the intermediate files created

</details>

<details>
<summary>`collections_with_ids`</summary>

</details>

<details>
<summary>`connection_options`</summary>

</details>

<details>
<summary>`credentials`</summary>

</details>

<details>
<summary>`if_collection_exists`</summary>

</details>

<details>
<summary>

**`stream(working_dir: str, *results: List[LazyFrame | None] | LazyFrame | None)`**

</summary>

Trigger the exporting of the data. This method will receive the resulting data
from the user function and must store it in the desired location.
Note: this method *might* materialize the data provided in a single chunk
generated by the chunk function if invoked, so chunks should be of an
appropriate size.

<ParamField path="working_dir" type="str">
  The folder where any intermediate files generated must
be stored (this refers to temporary files that will be deleted after
the execution of the plugin, not the final destination of the data)
</ParamField>

<ParamField path="results">
  The data to be exported. It is a list of polars LazyFrames or None.
</ParamField>

**Returns:** None

</details>

<details>
<summary>`uri`</summary>

</details>

<details>
<summary>

**`write(files)`**

</summary>

This method is used to write the files to the database. It is called
from the stream method, and it is not intended to be called directly.

</details>

</details>

 <details>
<summary>

**`chunk(working_dir: str, *results: LazyFrame | None)`**

</summary>

Trigger the exporting of the data to local parquet chunks. This method will
receive the resulting data from the user function and must store it in
the local system as parquet files, using the working_dir. Note: This
method should *not* materialize the data, it should only store it in the
local system.

<ParamField path="working_dir" type="str">
  The folder where any files generated must
be stored (this refers to temporary files that will be deleted after
the execution of the plugin, not the final destination of the data)
</ParamField>

<ParamField path="results">
  The data to be exported. It is a list of polars LazyFrames or None.
</ParamField>

**Returns:** A list of the intermediate files created

</details>

 <details>
<summary>

**`stream(working_dir: str, *results: List[LazyFrame | None] | LazyFrame | None)`**

</summary>

Trigger the exporting of the data. This method will receive the resulting data
from the user function and must store it in the desired location.
Note: this method *might* materialize the data provided in a single chunk
generated by the chunk function if invoked, so chunks should be of an
appropriate size.

<ParamField path="working_dir" type="str">
  The folder where any intermediate files generated must
be stored (this refers to temporary files that will be deleted after
the execution of the plugin, not the final destination of the data)
</ParamField>

<ParamField path="results">
  The data to be exported. It is a list of polars LazyFrames or None.
</ParamField>

**Returns:** None

</details>

 <details>
<summary>

**`write(files)`**

</summary>

This method is used to write the files to the database. It is called
from the stream method, and it is not intended to be called directly.

</details>

 <details>
<summary>

**`MySQLCdcConn(**kwargs)`**

</summary>

Bases: CdcConn, MySQLConn

MySQL CDC connection configuration.

</details>

 <details>
<summary>

**`MySQLCdcTrigger(**kwargs)`**

</summary>

Bases: CdcTrigger

A CdcTrigger that captures changes from a MySQL database using binlog replication.

This trigger connects to a MySQL server, reads the binary log for change data
capture events (inserts, updates, deletes) on specified tables, and stages them.

<details>
<summary>`blocking_timeout_seconds`</summary>

Timeout in seconds for blocking reads from the binlog stream.

</details>

<details>
<summary>

**`collect_tables(session: Any, cdc_format: CdcFormat = CdcFormat(values_format='columns', flatten_values=True))`**

</summary>

</details>

<details>
<summary>

**`harvest_tables(session: Any, cdc_format: CdcFormat = CdcFormat(values_format='columns', flatten_values=True))`**

</summary>

</details>

<details>
<summary>`server_id`</summary>

MySQL server ID for binlog replication.

</details>

<details>
<summary>`start_from`</summary>

Position to start reading changes from.

</details>

</details>

 <details>
<summary>

**`collect_tables(session: Any, cdc_format: CdcFormat = CdcFormat(values_format='columns', flatten_values=True))`**

</summary>

</details>

 <details>
<summary>

**`harvest_tables(session: Any, cdc_format: CdcFormat = CdcFormat(values_format='columns', flatten_values=True))`**

</summary>

</details>

 <details>
<summary>

**`MySQLConn(uri: str, credentials: UserPasswordCredentials | None = None, enforce_connection_params: bool = True, cx_src_configs_mysql: dict | None = None, cx_dst_configs_mysql: dict | None = None)`**

</summary>

Bases: Conn

Represents a connection configuration to MySQL.

<details>
<summary>`credentials`</summary>

The credentials required to access the MySQL database. If no credentials were
provided, it will return None.

</details>

<details>
<summary>`cx_dst_configs_mysql`</summary>

The destination configuration parameters for the MySQL connection.

</details>

<details>
<summary>`cx_src_configs_mysql`</summary>

The source configuration parameters for the MySQL connection.

</details>

<details>
<summary>`enforce_connection_params`</summary>

Whether to enforce connection parameters for the MySQL connection.

</details>

<details>
<summary>`uri`</summary>

The URI of the database where the data is located.

</details>

</details>

 <details>
<summary>

**`MySQLDest(conn: MySQLConn, destination_tables: str | list[str], if_table_exists: Literal['append', 'replace'] = 'append', transactional: bool = True, chunk_size: Annotated[int, Gt(gt=0)] = 100000, loader: Literal['polars_sqlalchemy'] = 'polars_sqlalchemy')`**

</summary>

Bases: DestinationPlugin

Destination plugin for writing data to MySQL.

<details>
<summary>`chunk_size`</summary>

The chunk size for writing large results in batches.

</details>

<details>
<summary>`conn`</summary>

The MySQL connection configuration.

</details>

<details>
<summary>`destination_tables`</summary>

The table(s) to create. If multiple tables are provided,
they must be provided as a list.

</details>

<details>
<summary>`if_table_exists`</summary>

The strategy to follow when the table already exists.

</details>

<details>
<summary>`loader`</summary>

The data processing loader to use for executing DB write operation.

</details>

<details>
<summary>

**`stream(working_dir: str, *results: list[LazyFrame | None])`**

</summary>

Store the results into the MySQL database.

<ParamField path="working_dir">
  The working directory where the results are stored.
</ParamField>

<ParamField path="results">
  The results to store in the SQL destination.
</ParamField>

</details>

<details>
<summary>`transactional`</summary>

Whether to use transactions for writing data to ensure consistency.

</details>

</details>

 <details>
<summary>

**`stream(working_dir: str, *results: list[LazyFrame | None])`**

</summary>

Store the results into the MySQL database.

<ParamField path="working_dir">
  The working directory where the results are stored.
</ParamField>

<ParamField path="results">
  The results to store in the SQL destination.
</ParamField>

</details>

 <details>
<summary>

**`MySQLDestination(**kwargs)`**

</summary>

Bases: DestinationPlugin

MySQL-based data outputs.

<details>
<summary>

**`chunk(working_dir: str, *results: LazyFrame | None)`**

</summary>

Store the results in the SQL destination.

<ParamField path="working_dir" type="str">
  The working directory where the results will be stored.
</ParamField>

<ParamField path="results" type="list">
  The results to store in the SQL
destination.
</ParamField>

</details>

<details>
<summary>`credentials`</summary>

The credentials required to access the MySQLDatabase.

 **Type:** UserPasswordCredentials

</details>

<details>
<summary>`destination_table`</summary>

The table(s) to create. If multiple tables are provided,
they must be provided as a list.

 **Type:** str | List[str]

</details>

<details>
<summary>`if_table_exists`</summary>

The strategy to follow when the table already exists.

 **Type:** str

</details>

<details>
<summary>`uri`</summary>

The URI of the database where the data is going to be stored.

 **Type:** str

</details>

<details>
<summary>

**`write(files: list[str | None])`**

</summary>

Given a file or a list of files, write to the desired destination. Note: this
method *might* materialize the data in the files it receives, so chunks
should be of an appropriate size.

</details>

</details>

 <details>
<summary>

**`chunk(working_dir: str, *results: LazyFrame | None)`**

</summary>

Store the results in the SQL destination.

<ParamField path="working_dir" type="str">
  The working directory where the results will be stored.
</ParamField>

<ParamField path="results" type="list">
  The results to store in the SQL
destination.
</ParamField>

</details>

 <details>
<summary>

**`write(files: list[str | None])`**

</summary>

Given a file or a list of files, write to the desired destination. Note: this
method *might* materialize the data in the files it receives, so chunks
should be of an appropriate size.

</details>

 <details>
<summary>

**`MySQLSource(**kwargs)`**

</summary>

Bases: SourcePlugin

MySQL-based data inputs.

<details>
<summary>

**`chunk(working_dir: str)`**

</summary>

Trigger the import of the data. This must be implemented in any class that
inherits from this class unless directly implementing streaming. The method will
receive a folder where it must store the data as parquet files, and return a
list of the paths of the files created. This files will then be loaded and
mapped to the dataset function in positional order, so if you want file.parquet
to be the first argument of the dataset function, you must return it first. If
you want a parameter to receive multiple files, return a list of the paths. For
example, you would give the following return to provide a first argument with a
single file and a second argument with two files:return
[“file1.parquet”, [“file2.parquet”, “file3.parquet”]]

**Returns:** The path of the file(s) created, inthe order they must be mapped to the dataset function

**Return type:** Union[str, Tuple[str, …], List[str]]

</details>

<details>
<summary>`credentials`</summary>

The credentials required to access the
MySQLDatabase. If no credentials were provided, it will return None.

 **Type:** UserPasswordCredentials | None

</details>

<details>
<summary>`initial_values`</summary>

The initial values for the parameters in the SQL queries.

 **Type:** dict

</details>

<details>
<summary>`query`</summary>

The SQL query(s) to execute.

 **Type:** str | List[str]

</details>

<details>
<summary>`uri`</summary>

The URI of the database where the data is located.

 **Type:** str

</details>

</details>

 <details>
<summary>

**`chunk(working_dir: str)`**

</summary>

Trigger the import of the data. This must be implemented in any class that
inherits from this class unless directly implementing streaming. The method will
receive a folder where it must store the data as parquet files, and return a
list of the paths of the files created. This files will then be loaded and
mapped to the dataset function in positional order, so if you want file.parquet
to be the first argument of the dataset function, you must return it first. If
you want a parameter to receive multiple files, return a list of the paths. For
example, you would give the following return to provide a first argument with a
single file and a second argument with two files:return
[“file1.parquet”, [“file2.parquet”, “file3.parquet”]]

**Returns:** The path of the file(s) created, inthe order they must be mapped to the dataset function

**Return type:** Union[str, Tuple[str, …], List[str]]

</details>

 <details>
<summary>

**`MySQLSrc(conn: MySQLConn, queries: str | list[str], initial_values: dict[str, Any] | None = None, transactional: bool = True, chunk_size: Annotated[int, Gt(gt=0)] = 100000, loader: Literal['polars_sqlalchemy', 'pandas'] = 'polars_sqlalchemy', schema_overrides: dict[str, Boolean | Categorical | Date | Datetime | Decimal | Duration | Enum | Float16 | Float32 | Float64 | Int8 | Int16 | Int64 | Int32 | Int128 | Null | String | Time | UInt8 | UInt16 | UInt32 | UInt64] | list[dict[str, Boolean | Categorical | Date | Datetime | Decimal | Duration | Enum | Float16 | Float32 | Float64 | Int8 | Int16 | Int64 | Int32 | Int128 | Null | String | Time | UInt8 | UInt16 | UInt32 | UInt64]] | None = None)`**

</summary>

Bases: SourcePlugin

Source plugin for reading data from MySQL.

<details>
<summary>`chunk_size`</summary>

The chunk size in records for reading large queries in batches.

</details>

<details>
<summary>`conn`</summary>

The MySQL connection configuration.

</details>

<details>
<summary>`initial_values`</summary>

The initial values for the parameters in the SQL queries.

</details>

<details>
<summary>`loader`</summary>

The data processing loader to use for executing SQL imports.

</details>

<details>
<summary>`queries`</summary>

The SQL queries to execute to read data.

</details>

<details>
<summary>`schema_overrides`</summary>

The schema overrides for the tables being read.

</details>

<details>
<summary>

**`stream(working_dir: str)`**

</summary>

Execute the query and load the results as a list of TableFrames.

**Returns:** A list of TableFrames containing the query results.

</details>

<details>
<summary>`transactional`</summary>

Whether to use transactions for reading to ensure consistency.

</details>

</details>

 <details>
<summary>

**`stream(working_dir: str)`**

</summary>

Execute the query and load the results as a list of TableFrames.

**Returns:** A list of TableFrames containing the query results.

</details>

 <details>
<summary>

**`NDJSONFormat`**

</summary>

Bases: `FileFormat`

JSON file format.

</details>

 <details>
<summary>

**`OracleConn(uri: str, credentials: UserPasswordCredentials | None = None, enforce_connection_params: bool = True, cx_src_configs_oracle: dict | None = None, cx_dst_configs_oracle: dict | None = None)`**

</summary>

Bases: Conn

Represents a connection configuration to Oracle.

<details>
<summary>`credentials`</summary>

The credentials required to access the Oracle database. If no credentials were
provided, it will return None.

</details>

<details>
<summary>`cx_dst_configs_oracle`</summary>

Destination-specific configuration parameters for the Oracle connection.

</details>

<details>
<summary>`cx_src_configs_oracle`</summary>

Source-specific configuration parameters for the Oracle connection.

</details>

<details>
<summary>`enforce_connection_params`</summary>

Whether to enforce connection parameters for the Oracle connection.

</details>

<details>
<summary>`uri`</summary>

The URI of the database where the data is located.

</details>

</details>

 <details>
<summary>

**`OracleDestination(uri: str, destination_table: List[str] | str, credentials: UserPasswordCredentials = None, if_table_exists: Literal['append', 'replace'] = 'append')`**

</summary>

Bases: DestinationPlugin

Oracle-based data outputs.

<details>
<summary>

**`chunk(working_dir: str, *results: LazyFrame | None)`**

</summary>

Store the results in the SQL destination.

<ParamField path="working_dir" type="str">
  The working directory where the results will be stored.
</ParamField>

<ParamField path="results" type="list">
  The results to store in the SQL
destination.
</ParamField>

</details>

<details>
<summary>`credentials`</summary>

The credentials required to access the Oracle
database.

 **Type:** UserPasswordCredentials

</details>

<details>
<summary>`destination_table`</summary>

The table(s) to create. If multiple tables are provided,
they must be provided as a list.

 **Type:** str | List[str]

</details>

<details>
<summary>`if_table_exists`</summary>

The strategy to follow when the table already exists.

 **Type:** str

</details>

<details>
<summary>`uri`</summary>

The URI of the database where the data is going to be stored.

 **Type:** str

</details>

<details>
<summary>

**`write(files: list[str | None])`**

</summary>

Given a file or a list of files, write to the desired destination. Note: this
method *might* materialize the data in the files it receives, so chunks
should be of an appropriate size.

</details>

</details>

 <details>
<summary>

**`chunk(working_dir: str, *results: LazyFrame | None)`**

</summary>

Store the results in the SQL destination.

<ParamField path="working_dir" type="str">
  The working directory where the results will be stored.
</ParamField>

<ParamField path="results" type="list">
  The results to store in the SQL
destination.
</ParamField>

</details>

 <details>
<summary>

**`write(files: list[str | None])`**

</summary>

Given a file or a list of files, write to the desired destination. Note: this
method *might* materialize the data in the files it receives, so chunks
should be of an appropriate size.

</details>

 <details>
<summary>

**`OracleSource(**kwargs)`**

</summary>

Bases: SourcePlugin

Oracle-based data inputs.

<details>
<summary>

**`chunk(working_dir: str)`**

</summary>

Trigger the import of the data. This must be implemented in any class that
inherits from this class unless directly implementing streaming. The method will
receive a folder where it must store the data as parquet files, and return a
list of the paths of the files created. This files will then be loaded and
mapped to the dataset function in positional order, so if you want file.parquet
to be the first argument of the dataset function, you must return it first. If
you want a parameter to receive multiple files, return a list of the paths. For
example, you would give the following return to provide a first argument with a
single file and a second argument with two files:return
[“file1.parquet”, [“file2.parquet”, “file3.parquet”]]

**Returns:** The path of the file(s) created, inthe order they must be mapped to the dataset function

**Return type:** Union[str, Tuple[str, …], List[str]]

</details>

<details>
<summary>`credentials`</summary>

The credentials required to access
Oracle. If no credentials were provided, it will return None.

 **Type:** UserPasswordCredentials | None

</details>

<details>
<summary>`initial_values`</summary>

The initial values for the parameters in the SQL queries.

 **Type:** dict

</details>

<details>
<summary>`query`</summary>

The SQL query(s) to execute.

 **Type:** str | List[str]

</details>

<details>
<summary>`uri`</summary>

The URI of the database where the data is located.

 **Type:** str

</details>

</details>

 <details>
<summary>

**`chunk(working_dir: str)`**

</summary>

Trigger the import of the data. This must be implemented in any class that
inherits from this class unless directly implementing streaming. The method will
receive a folder where it must store the data as parquet files, and return a
list of the paths of the files created. This files will then be loaded and
mapped to the dataset function in positional order, so if you want file.parquet
to be the first argument of the dataset function, you must return it first. If
you want a parameter to receive multiple files, return a list of the paths. For
example, you would give the following return to provide a first argument with a
single file and a second argument with two files:return
[“file1.parquet”, [“file2.parquet”, “file3.parquet”]]

**Returns:** The path of the file(s) created, inthe order they must be mapped to the dataset function

**Return type:** Union[str, Tuple[str, …], List[str]]

</details>

 <details>
<summary>

**`OracleSrc(conn: OracleConn, queries: str | list[str], initial_values: dict[str, Any] | None = None, transactional: bool = True, chunk_size: Annotated[int, Gt(gt=0)] = 100000, loader: Literal['polars_sqlalchemy', 'pandas'] = 'polars_sqlalchemy', schema_overrides: dict[str, Boolean | Categorical | Date | Datetime | Decimal | Duration | Enum | Float16 | Float32 | Float64 | Int8 | Int16 | Int64 | Int32 | Int128 | Null | String | Time | UInt8 | UInt16 | UInt32 | UInt64] | list[dict[str, Boolean | Categorical | Date | Datetime | Decimal | Duration | Enum | Float16 | Float32 | Float64 | Int8 | Int16 | Int64 | Int32 | Int128 | Null | String | Time | UInt8 | UInt16 | UInt32 | UInt64]] | None = None)`**

</summary>

Bases: SourcePlugin

Source plugin for reading data from Oracle.

<details>
<summary>`chunk_size`</summary>

The chunk size in records for reading large queries in batches.

</details>

<details>
<summary>`conn`</summary>

The Oracle connection configuration.

</details>

<details>
<summary>`initial_values`</summary>

The initial values for the parameters in the SQL queries.

</details>

<details>
<summary>`loader`</summary>

The data processing loader to use for executing SQL imports.

</details>

<details>
<summary>`queries`</summary>

The SQL queries to execute to read data.

</details>

<details>
<summary>`schema_overrides`</summary>

The schema overrides for the tables being read.

</details>

<details>
<summary>

**`stream(working_dir: str)`**

</summary>

Execute the query and load the results as a list of TableFrames.

**Returns:** A list of TableFrames containing the query results.

</details>

<details>
<summary>`transactional`</summary>

Whether to use transactions for reading to ensure consistency.

</details>

</details>

 <details>
<summary>

**`stream(working_dir: str)`**

</summary>

Execute the query and load the results as a list of TableFrames.

**Returns:** A list of TableFrames containing the query results.

</details>

 <details>
<summary>

**`ParquetFormat`**

</summary>

Bases: `FileFormat`

Parquet file format.

</details>

 <details>
<summary>

**`PostgresCdcConn(**kwargs)`**

</summary>

Bases: CdcConn, PostgresConn

PostgreSQL CDC connection configuration.

</details>

 <details>
<summary>

**`PostgresCdcTrigger(**kwargs)`**

</summary>

Bases: CdcTrigger

A CdcTrigger that captures changes from a PostgreSQL database using WAL
logical replication.

This trigger connects to a PostgreSQL server, reads the write-ahead log via
the wal2json output plugin for change data capture events (inserts, updates,
deletes) on specified tables, and stages them.

<details>
<summary>`blocking_timeout_seconds`</summary>

Timeout in seconds for blocking reads from the replication stream.

</details>

<details>
<summary>

**`collect_tables(session: Any, cdc_format: CdcFormat = CdcFormat(values_format='columns', flatten_values=True))`**

</summary>

</details>

<details>
<summary>

**`harvest_tables(session: Any, cdc_format: CdcFormat = CdcFormat(values_format='columns', flatten_values=True))`**

</summary>

</details>

<details>
<summary>`publication_name`</summary>

Name of the PostgreSQL publication to subscribe to.

</details>

<details>
<summary>`replication_slot`</summary>

Name of the logical replication slot.

</details>

<details>
<summary>`slot_behavior`</summary>

Whether to create a new replication slot or reuse an existing one.

</details>

<details>
<summary>`start_from`</summary>

Position to start reading changes from.

</details>

</details>

 <details>
<summary>

**`collect_tables(session: Any, cdc_format: CdcFormat = CdcFormat(values_format='columns', flatten_values=True))`**

</summary>

</details>

 <details>
<summary>

**`harvest_tables(session: Any, cdc_format: CdcFormat = CdcFormat(values_format='columns', flatten_values=True))`**

</summary>

</details>

 <details>
<summary>

**`PostgresConn(uri: str, credentials: UserPasswordCredentials | None = None, enforce_connection_params: bool = True, cx_src_configs_postgres: dict | None = None, cx_dst_configs_postgres: dict | None = None)`**

</summary>

Bases: Conn

Represents a connection configuration to Postgres.

<details>
<summary>`credentials`</summary>

The credentials required to access the Postgres database. If no credentials were
provided, it will return None.

</details>

<details>
<summary>`cx_dst_configs_postgres`</summary>

Destination-specific configuration parameters for the Postgres connection.

</details>

<details>
<summary>`cx_src_configs_postgres`</summary>

Source-specific configuration parameters for the Postgres connection.

</details>

<details>
<summary>`enforce_connection_params`</summary>

Whether to enforce connection parameters for the Postgres connection.

</details>

<details>
<summary>`uri`</summary>

The URI of the database where the data is located.

</details>

</details>

 <details>
<summary>

**`PostgresDest(conn: PostgresConn, destination_tables: str | list[str], if_table_exists: Literal['append', 'replace'] = 'append', transactional: bool = True, chunk_size: Annotated[int, Gt(gt=0)] = 100000, loader: Literal['polars_sqlalchemy'] = 'polars_sqlalchemy')`**

</summary>

Bases: DestinationPlugin

Destination plugin for writing data to PostgreSQL.

<details>
<summary>`chunk_size`</summary>

The chunk size for writing large results in batches.

</details>

<details>
<summary>`conn`</summary>

The Postgres connection configuration.

</details>

<details>
<summary>`destination_tables`</summary>

The table(s) to create. If multiple tables are provided,
they must be provided as a list.

</details>

<details>
<summary>`if_table_exists`</summary>

The strategy to follow when the table already exists.

</details>

<details>
<summary>`loader`</summary>

The data processing loader to use for executing DB write operation.

</details>

<details>
<summary>

**`stream(working_dir: str, *results: list[LazyFrame | None])`**

</summary>

Store the results into the PostgreSQL database.

<ParamField path="working_dir">
  The working directory where the results are stored.
</ParamField>

<ParamField path="results">
  The results to store in the SQL destination.
</ParamField>

</details>

<details>
<summary>`transactional`</summary>

Whether to use transactions for writing data to ensure consistency.

</details>

</details>

 <details>
<summary>

**`stream(working_dir: str, *results: list[LazyFrame | None])`**

</summary>

Store the results into the PostgreSQL database.

<ParamField path="working_dir">
  The working directory where the results are stored.
</ParamField>

<ParamField path="results">
  The results to store in the SQL destination.
</ParamField>

</details>

 <details>
<summary>

**`PostgresDestination(**kwargs)`**

</summary>

Bases: DestinationPlugin

Postgres-based data outputs.

<details>
<summary>

**`chunk(working_dir: str, *results: LazyFrame | None)`**

</summary>

Store the results in the SQL destination.

<ParamField path="working_dir" type="str">
  The working directory where the results will be stored.
</ParamField>

<ParamField path="results" type="list">
  The results to store in the SQL
destination.
</ParamField>

</details>

<details>
<summary>`credentials`</summary>

The credentials required to access the
Postgres database.

 **Type:** UserPasswordCredentials

</details>

<details>
<summary>`destination_table`</summary>

The table(s) to create. If multiple tables are provided,
they must be provided as a list.

 **Type:** str | List[str]

</details>

<details>
<summary>`if_table_exists`</summary>

The strategy to follow when the table already exists.

 **Type:** str

</details>

<details>
<summary>`uri`</summary>

The URI of the database where the data is going to be stored.

 **Type:** str

</details>

<details>
<summary>

**`write(files: list[str | None])`**

</summary>

Given a file or a list of files, write to the desired destination. Note: this
method *might* materialize the data in the files it receives, so chunks
should be of an appropriate size.

</details>

</details>

 <details>
<summary>

**`chunk(working_dir: str, *results: LazyFrame | None)`**

</summary>

Store the results in the SQL destination.

<ParamField path="working_dir" type="str">
  The working directory where the results will be stored.
</ParamField>

<ParamField path="results" type="list">
  The results to store in the SQL
destination.
</ParamField>

</details>

 <details>
<summary>

**`write(files: list[str | None])`**

</summary>

Given a file or a list of files, write to the desired destination. Note: this
method *might* materialize the data in the files it receives, so chunks
should be of an appropriate size.

</details>

 <details>
<summary>

**`PostgresSource(**kwargs)`**

</summary>

Bases: SourcePlugin

Postgres-based data inputs.

<details>
<summary>

**`chunk(working_dir: str)`**

</summary>

Trigger the import of the data. This must be implemented in any class that
inherits from this class unless directly implementing streaming. The method will
receive a folder where it must store the data as parquet files, and return a
list of the paths of the files created. This files will then be loaded and
mapped to the dataset function in positional order, so if you want file.parquet
to be the first argument of the dataset function, you must return it first. If
you want a parameter to receive multiple files, return a list of the paths. For
example, you would give the following return to provide a first argument with a
single file and a second argument with two files:return
[“file1.parquet”, [“file2.parquet”, “file3.parquet”]]

**Returns:** The path of the file(s) created, inthe order they must be mapped to the dataset function

**Return type:** Union[str, Tuple[str, …], List[str]]

</details>

<details>
<summary>`credentials`</summary>

The credentials required to access the
PostgresDatabase. If no credentials were provided, it will return None.

 **Type:** UserPasswordCredentials | None

</details>

<details>
<summary>`initial_values`</summary>

The initial values for the parameters in the SQL queries.

 **Type:** dict

</details>

<details>
<summary>`query`</summary>

The SQL query(s) to execute.

 **Type:** str | List[str]

</details>

<details>
<summary>`uri`</summary>

The URI of the database where the data is located.

 **Type:** str

</details>

</details>

 <details>
<summary>

**`chunk(working_dir: str)`**

</summary>

Trigger the import of the data. This must be implemented in any class that
inherits from this class unless directly implementing streaming. The method will
receive a folder where it must store the data as parquet files, and return a
list of the paths of the files created. This files will then be loaded and
mapped to the dataset function in positional order, so if you want file.parquet
to be the first argument of the dataset function, you must return it first. If
you want a parameter to receive multiple files, return a list of the paths. For
example, you would give the following return to provide a first argument with a
single file and a second argument with two files:return
[“file1.parquet”, [“file2.parquet”, “file3.parquet”]]

**Returns:** The path of the file(s) created, inthe order they must be mapped to the dataset function

**Return type:** Union[str, Tuple[str, …], List[str]]

</details>

 <details>
<summary>

**`PostgresSrc(conn: PostgresConn, queries: str | list[str], initial_values: dict[str, Any] | None = None, transactional: bool = True, chunk_size: Annotated[int, Gt(gt=0)] = 100000, loader: Literal['polars_sqlalchemy', 'pandas'] = 'polars_sqlalchemy', schema_overrides: dict[str, Boolean | Categorical | Date | Datetime | Decimal | Duration | Enum | Float16 | Float32 | Float64 | Int8 | Int16 | Int64 | Int32 | Int128 | Null | String | Time | UInt8 | UInt16 | UInt32 | UInt64] | list[dict[str, Boolean | Categorical | Date | Datetime | Decimal | Duration | Enum | Float16 | Float32 | Float64 | Int8 | Int16 | Int64 | Int32 | Int128 | Null | String | Time | UInt8 | UInt16 | UInt32 | UInt64]] | None = None)`**

</summary>

Bases: SourcePlugin

Source plugin for reading data from Postgres.

<details>
<summary>`chunk_size`</summary>

The chunk size in records for reading large queries in batches.

</details>

<details>
<summary>`conn`</summary>

The Postgres connection configuration.

</details>

<details>
<summary>`initial_values`</summary>

The initial values for the parameters in the SQL queries.

</details>

<details>
<summary>`loader`</summary>

The data processing loader to use for executing SQL imports.

</details>

<details>
<summary>`queries`</summary>

The SQL queries to execute to read data.

</details>

<details>
<summary>`schema_overrides`</summary>

The schema overrides for the tables being read.

</details>

<details>
<summary>

**`stream(working_dir: str)`**

</summary>

Execute the query and load the results as a list of TableFrames.

**Returns:** A list of TableFrames containing the query results.

</details>

<details>
<summary>`transactional`</summary>

Whether to use transactions for reading to ensure consistency.

</details>

</details>

 <details>
<summary>

**`stream(working_dir: str)`**

</summary>

Execute the query and load the results as a list of TableFrames.

**Returns:** A list of TableFrames containing the query results.

</details>

 <details>
<summary>

**`S3AccessKeyCredentials(aws_access_key_id: str | Secret, aws_secret_access_key: str | Secret)`**

</summary>

Bases: `S3Credentials`

Credentials class to access an S3 bucket using access key credentials (access key
id and secret access key).

<details>
<summary>`aws_access_key_id`</summary>

The AWS access key id.

 **Type:** Secret

</details>

<details>
<summary>`aws_secret_access_key`</summary>

The AWS secret access key.

 **Type:** Secret

</details>

<ParamField path="aws_access_key_id" type="Secret">
  The AWS access key id.
</ParamField>

<ParamField path="aws_secret_access_key" type="Secret">
  The AWS secret access key.
</ParamField>

<ParamField path="dict" type="to_dict">
  Convert the S3AccessKeyCredentials object to a dictionary
</ParamField>

</details>

 <details>
<summary>

**`S3Config(bucket: str, region: str, base_path: str, credentials: S3AccessKeyCredentials)`**

</summary>

Bases: [object](https://docs.python.org/3/library/functions.html#object)

Configuration for staging files in Amazon S3.

<details>
<summary>`base_path`</summary>

</details>

<details>
<summary>`bucket`</summary>

</details>

<details>
<summary>`credentials`</summary>

</details>

<details>
<summary>`region`</summary>

</details>

<ParamField path="bucket">
  S3 bucket name.
</ParamField>

<ParamField path="region">
  AWS region (e.g. "us-east-1").
</ParamField>

<ParamField path="base_path">
  Key prefix under which staged files are stored
(e.g. "staging/clickhouse").
</ParamField>

<ParamField path="credentials">
  AWS access-key credentials used for uploading staged
files (via boto3).
</ParamField>

</details>

 <details>
<summary>

**`S3Destination(uri: str | list[str], credentials: S3Credentials, format: str | FileFormat = None, region: str = None, catalog: AWSGlue = None)`**

</summary>

Bases: DestinationPlugin

S3-file-based data outputs.

<details>
<summary>

**`SupportedFormats(*values)`**

</summary>

Bases: [Enum](https://docs.python.org/3/library/enum.html#enum.Enum)

Enum for the supported formats for the S3Destination.

<details>
<summary>`avro`</summary>

</details>

<details>
<summary>`csv`</summary>

</details>

<details>
<summary>`ndjson`</summary>

</details>

<details>
<summary>`parquet`</summary>

</details>

</details>

<details>
<summary>`allow_fragments`</summary>

Whether to allow fragments in the output.

 **Type:** bool

</details>

<details>
<summary>`catalog`</summary>

The catalog to store the data in.

 **Type:** Catalog

</details>

<details>
<summary>

**`chunk(working_dir: str, *results)`**

</summary>

Trigger the exporting of the data to local parquet chunks. This method will
receive the resulting data from the user function and must store it in
the local system as parquet files, using the working_dir. Note: This
method should *not* materialize the data, it should only store it in the
local system.

<ParamField path="working_dir" type="str">
  The folder where any files generated must
be stored (this refers to temporary files that will be deleted after
the execution of the plugin, not the final destination of the data)
</ParamField>

<ParamField path="results">
  The data to be exported. It is a list of polars LazyFrames or None.
</ParamField>

**Returns:** A list of the intermediate files created

</details>

<details>
<summary>`credentials`</summary>

The credentials required to access the S3 bucket.

 **Type:** S3Credentials

</details>

<details>
<summary>`format`</summary>

The format of the file. If not provided, it will be inferred from
the file.

 **Type:** FileFormat

</details>

<details>
<summary>`region`</summary>

The region where the S3 bucket is located.

 **Type:** str

</details>

<details>
<summary>

**`stream(working_dir: str, *results)`**

</summary>

Trigger the exporting of the data. This method will receive the resulting data
from the user function and must store it in the desired location.
Note: this method *might* materialize the data provided in a single chunk
generated by the chunk function if invoked, so chunks should be of an
appropriate size.

<ParamField path="working_dir" type="str">
  The folder where any intermediate files generated must
be stored (this refers to temporary files that will be deleted after
the execution of the plugin, not the final destination of the data)
</ParamField>

<ParamField path="results">
  The data to be exported. It is a list of polars LazyFrames or None.
</ParamField>

**Returns:** None

</details>

<details>
<summary>`uri`</summary>

‘s3://path/to/files’.

 **Type:** str | list[str]

**Type:** The URI of the files with format

</details>

</details>

 <details>
<summary>

**`chunk(working_dir: str, *results)`**

</summary>

Trigger the exporting of the data to local parquet chunks. This method will
receive the resulting data from the user function and must store it in
the local system as parquet files, using the working_dir. Note: This
method should *not* materialize the data, it should only store it in the
local system.

<ParamField path="working_dir" type="str">
  The folder where any files generated must
be stored (this refers to temporary files that will be deleted after
the execution of the plugin, not the final destination of the data)
</ParamField>

<ParamField path="results">
  The data to be exported. It is a list of polars LazyFrames or None.
</ParamField>

**Returns:** A list of the intermediate files created

</details>

 <details>
<summary>

**`stream(working_dir: str, *results)`**

</summary>

Trigger the exporting of the data. This method will receive the resulting data
from the user function and must store it in the desired location.
Note: this method *might* materialize the data provided in a single chunk
generated by the chunk function if invoked, so chunks should be of an
appropriate size.

<ParamField path="working_dir" type="str">
  The folder where any intermediate files generated must
be stored (this refers to temporary files that will be deleted after
the execution of the plugin, not the final destination of the data)
</ParamField>

<ParamField path="results">
  The data to be exported. It is a list of polars LazyFrames or None.
</ParamField>

**Returns:** None

</details>

 <details>
<summary>

**`S3Source(uri: str | list[str], credentials: S3Credentials, format: str | FileFormat = None, initial_last_modified: str | datetime = None, region: str = None, file_info: str | FileInfo | None = None)`**

</summary>

Bases: SourcePlugin

S3-file-based data inputs.

<details>
<summary>

**`SupportedFormats(*values)`**

</summary>

Bases: [Enum](https://docs.python.org/3/library/enum.html#enum.Enum)

Enum for the supported formats for the S3Source.

<details>
<summary>`avro`</summary>

</details>

<details>
<summary>`csv`</summary>

</details>

<details>
<summary>`log`</summary>

</details>

<details>
<summary>`ndjson`</summary>

</details>

<details>
<summary>`parquet`</summary>

</details>

</details>

<details>
<summary>

**`chunk(working_dir: str)`**

</summary>

Trigger the import of the data. This must be implemented in any class that
inherits from this class unless directly implementing streaming. The method will
receive a folder where it must store the data as parquet files, and return a
list of the paths of the files created. This files will then be loaded and
mapped to the dataset function in positional order, so if you want file.parquet
to be the first argument of the dataset function, you must return it first. If
you want a parameter to receive multiple files, return a list of the paths. For
example, you would give the following return to provide a first argument with a
single file and a second argument with two files:return
[“file1.parquet”, [“file2.parquet”, “file3.parquet”]]

**Returns:** The path of the file(s) created, inthe order they must be mapped to the dataset function

**Return type:** Union[str, Tuple[str, …], List[str]]

</details>

<details>
<summary>`credentials`</summary>

The credentials required to access the S3 bucket.

 **Type:** S3Credentials

</details>

<details>
<summary>`file_info`</summary>

The file information to add as system columns.

 **Type:** FileInfo | None

</details>

<details>
<summary>`format`</summary>

The format of the file. If not provided, it will be inferred from
the file.

 **Type:** FileFormat

</details>

<details>
<summary>`initial_last_modified`</summary>

The date and time after which the files were modified.

 **Type:** str

</details>

<details>
<summary>`initial_values`</summary>

Return a dictionary with the initial values to be stored after execution of
the plugin. They will be accessible in the next execution of the plugin.
The dictionary must have the parameter names as keys and the initial values
as values, all the type string.

 **Returns:** A dictionary with the initial values of the parameters of the plugin.

**Return type:** dict

</details>

<details>
<summary>`region`</summary>

The region where the S3 bucket is located.

 **Type:** str

</details>

<details>
<summary>`uri`</summary>

‘s3://path/to/files’.

 **Type:** str | list[str]

**Type:** The URI of the files with format

</details>

</details>

 <details>
<summary>

**`chunk(working_dir: str)`**

</summary>

Trigger the import of the data. This must be implemented in any class that
inherits from this class unless directly implementing streaming. The method will
receive a folder where it must store the data as parquet files, and return a
list of the paths of the files created. This files will then be loaded and
mapped to the dataset function in positional order, so if you want file.parquet
to be the first argument of the dataset function, you must return it first. If
you want a parameter to receive multiple files, return a list of the paths. For
example, you would give the following return to provide a first argument with a
single file and a second argument with two files:return
[“file1.parquet”, [“file2.parquet”, “file3.parquet”]]

**Returns:** The path of the file(s) created, inthe order they must be mapped to the dataset function

**Return type:** Union[str, Tuple[str, …], List[str]]

</details>

 <details>
<summary>

**`SalesforceReportSource(credentials: SalesforceCredentials, report: str | list[str], column_name_strategy: Literal['columnName', 'label'], find_report_by: Literal['id', 'name'] = None, filter: tuple[str, str, str] | list[tuple[str, str, str]] = None, filter_logic: str = None, instance_url: str = None, last_modified_column: str = None, initial_last_modified: str = None, **kwargs)`**

</summary>

Bases: SourcePlugin

Salesforce Reports based data inputs.

<details>
<summary>

**`chunk(working_dir: str)`**

</summary>

Trigger the import of the data. This must be implemented in any class that
inherits from this class unless directly implementing streaming. The method will
receive a folder where it must store the data as parquet files, and return a
list of the paths of the files created. This files will then be loaded and
mapped to the dataset function in positional order, so if you want file.parquet
to be the first argument of the dataset function, you must return it first. If
you want a parameter to receive multiple files, return a list of the paths. For
example, you would give the following return to provide a first argument with a
single file and a second argument with two files:return
[“file1.parquet”, [“file2.parquet”, “file3.parquet”]]

**Returns:** The path of the file(s) created, inthe order they must be mapped to the dataset function

**Return type:** Union[str, Tuple[str, …], List[str]]

</details>

<details>
<summary>`column_name_strategy`</summary>

</details>

<details>
<summary>`credentials`</summary>

</details>

<details>
<summary>`filter`</summary>

</details>

<details>
<summary>`filter_logic`</summary>

</details>

<details>
<summary>`find_report_by`</summary>

</details>

<details>
<summary>`report`</summary>

</details>

</details>

 <details>
<summary>

**`chunk(working_dir: str)`**

</summary>

Trigger the import of the data. This must be implemented in any class that
inherits from this class unless directly implementing streaming. The method will
receive a folder where it must store the data as parquet files, and return a
list of the paths of the files created. This files will then be loaded and
mapped to the dataset function in positional order, so if you want file.parquet
to be the first argument of the dataset function, you must return it first. If
you want a parameter to receive multiple files, return a list of the paths. For
example, you would give the following return to provide a first argument with a
single file and a second argument with two files:return
[“file1.parquet”, [“file2.parquet”, “file3.parquet”]]

**Returns:** The path of the file(s) created, inthe order they must be mapped to the dataset function

**Return type:** Union[str, Tuple[str, …], List[str]]

</details>

 <details>
<summary>

**`SalesforceSource(credentials: SalesforceCredentials, query: str | list[str], instance_url: str = None, include_deleted: bool = False, initial_last_modified: str = None, **kwargs)`**

</summary>

Bases: SourcePlugin

Salesforce (SOQL query) based data inputs (not Salesforce Reports).

<details>
<summary>

**`chunk(working_dir: str)`**

</summary>

Trigger the import of the data. This must be implemented in any class that
inherits from this class unless directly implementing streaming. The method will
receive a folder where it must store the data as parquet files, and return a
list of the paths of the files created. This files will then be loaded and
mapped to the dataset function in positional order, so if you want file.parquet
to be the first argument of the dataset function, you must return it first. If
you want a parameter to receive multiple files, return a list of the paths. For
example, you would give the following return to provide a first argument with a
single file and a second argument with two files:return
[“file1.parquet”, [“file2.parquet”, “file3.parquet”]]

**Returns:** The path of the file(s) created, inthe order they must be mapped to the dataset function

**Return type:** Union[str, Tuple[str, …], List[str]]

</details>

<details>
<summary>`credentials`</summary>

</details>

<details>
<summary>`query`</summary>

</details>

</details>

 <details>
<summary>

**`chunk(working_dir: str)`**

</summary>

Trigger the import of the data. This must be implemented in any class that
inherits from this class unless directly implementing streaming. The method will
receive a folder where it must store the data as parquet files, and return a
list of the paths of the files created. This files will then be loaded and
mapped to the dataset function in positional order, so if you want file.parquet
to be the first argument of the dataset function, you must return it first. If
you want a parameter to receive multiple files, return a list of the paths. For
example, you would give the following return to provide a first argument with a
single file and a second argument with two files:return
[“file1.parquet”, [“file2.parquet”, “file3.parquet”]]

**Returns:** The path of the file(s) created, inthe order they must be mapped to the dataset function

**Return type:** Union[str, Tuple[str, …], List[str]]

</details>

 <details>
<summary>

**`SalesforceTokenCredentials(username: str | Secret, password: str | Secret, security_token: str | Secret)`**

</summary>

Bases: `SalesforceCredentials`

Credentials class to store the credentials needed to access a Salesforce
using a username, a password and a token.

<details>
<summary>`password`</summary>

The password to access Salesforce.

 **Type:** Secret

</details>

<details>
<summary>`security_token`</summary>

The security token to access Salesforce.

 **Type:** Secret

</details>

<details>
<summary>`username`</summary>

The username to access Salesforce.

 **Type:** Secret

</details>

<ParamField path="username" type="Secret">
  The username to access Salesforce.
</ParamField>

<ParamField path="password" type="Secret">
  The password to access Salesforce.
</ParamField>

<ParamField path="security_token" type="Secret">
  The security token to access Salesforce.
</ParamField>

<ParamField path="dict" type="to_dict">
  Convert the AzureAccountNameKeyCredentials object to a
dictionary
</ParamField>

</details>

 <details>
<summary>

**`SnowflakeDestination(connection_parameters: dict, destination_table: List[str] | str, if_table_exists: Literal['append', 'replace'] = 'append', stage: str | None = None, **kwargs)`**

</summary>

Bases: DestinationPlugin

Snowflake based data outputs.

<details>
<summary>

**`chunk(working_dir: str, *results: LazyFrame | None)`**

</summary>

Trigger the exporting of the data to local parquet chunks. This method will
receive the resulting data from the user function and must store it in
the local system as parquet files, using the working_dir. Note: This
method should *not* materialize the data, it should only store it in the
local system.

<ParamField path="working_dir" type="str">
  The folder where any files generated must
be stored (this refers to temporary files that will be deleted after
the execution of the plugin, not the final destination of the data)
</ParamField>

<ParamField path="results">
  The data to be exported. It is a list of polars LazyFrames or None.
</ParamField>

**Returns:** A list of the intermediate files created

</details>

<details>
<summary>`destination_table`</summary>

Get the destination table(s) where the data will be stored.

 **Returns:** The destination table(s).

**Return type:** str | list[str]

</details>

<details>
<summary>`if_table_exists`</summary>

Returns the value of the if_table_exists property.
This property determines what to do if the table already exists.

</details>

<details>
<summary>

**`stream(working_dir: str, *results: List[LazyFrame | None] | LazyFrame | None)`**

</summary>

Trigger the exporting of the data. This method will receive the resulting data
from the user function and must store it in the desired location.
Note: this method *might* materialize the data provided in a single chunk
generated by the chunk function if invoked, so chunks should be of an
appropriate size.

<ParamField path="working_dir" type="str">
  The folder where any intermediate files generated must
be stored (this refers to temporary files that will be deleted after
the execution of the plugin, not the final destination of the data)
</ParamField>

<ParamField path="results">
  The data to be exported. It is a list of polars LazyFrames or None.
</ParamField>

**Returns:** None

</details>

<details>
<summary>

**`write(files)`**

</summary>

This method is used to write the files to the database. It is called
from the stream method, and it is not intended to be called directly.

</details>

</details>

 <details>
<summary>

**`chunk(working_dir: str, *results: LazyFrame | None)`**

</summary>

Trigger the exporting of the data to local parquet chunks. This method will
receive the resulting data from the user function and must store it in
the local system as parquet files, using the working_dir. Note: This
method should *not* materialize the data, it should only store it in the
local system.

<ParamField path="working_dir" type="str">
  The folder where any files generated must
be stored (this refers to temporary files that will be deleted after
the execution of the plugin, not the final destination of the data)
</ParamField>

<ParamField path="results">
  The data to be exported. It is a list of polars LazyFrames or None.
</ParamField>

**Returns:** A list of the intermediate files created

</details>

 <details>
<summary>

**`stream(working_dir: str, *results: List[LazyFrame | None] | LazyFrame | None)`**

</summary>

Trigger the exporting of the data. This method will receive the resulting data
from the user function and must store it in the desired location.
Note: this method *might* materialize the data provided in a single chunk
generated by the chunk function if invoked, so chunks should be of an
appropriate size.

<ParamField path="working_dir" type="str">
  The folder where any intermediate files generated must
be stored (this refers to temporary files that will be deleted after
the execution of the plugin, not the final destination of the data)
</ParamField>

<ParamField path="results">
  The data to be exported. It is a list of polars LazyFrames or None.
</ParamField>

**Returns:** None

</details>

 <details>
<summary>

**`write(files)`**

</summary>

This method is used to write the files to the database. It is called
from the stream method, and it is not intended to be called directly.

</details>

 <details>
<summary>

**`SourcePlugin`**

</summary>

Bases: [object](https://docs.python.org/3/library/functions.html#object)

Parent class for publisher connectors.

  def chunk(working_dir) -&gt; Union[str, Tuple[str, …], List[str]]Trigger the import of the data. The method will receive a folder where it
must store the data as parquet files, and return a list of the paths of
the files created. This files will then be loaded and mapped to the
dataset function in positional order, so if you want file.parquet to be
the first argument of the dataset function, you must return it first. If
you want a parameter to receive multiple files, return a list of the paths.
For example, you would give the following return to provide a first argument
with a single file and a second argument with two files:
return [“file1.parquet”, [“file2.parquet”, “file3.parquet”]]

   <details>
<summary>

**`chunk(working_dir: str)`**

</summary>

Trigger the import of the data. This must be implemented in any class that
inherits from this class unless directly implementing streaming. The method will
receive a folder where it must store the data as parquet files, and return a
list of the paths of the files created. This files will then be loaded and
mapped to the dataset function in positional order, so if you want file.parquet
to be the first argument of the dataset function, you must return it first. If
you want a parameter to receive multiple files, return a list of the paths. For
example, you would give the following return to provide a first argument with a
single file and a second argument with two files:return
[“file1.parquet”, [“file2.parquet”, “file3.parquet”]]

**Returns:** The path of the file(s) created, inthe order they must be mapped to the dataset function

**Return type:** Union[str, Tuple[str, …], List[str]]

</details>

<details>
<summary>`initial_values`</summary>

Return a dictionary with the initial values to be stored after execution of
the plugin. They will be accessible in the next execution of the plugin.
The dictionary must have the parameter names as keys and the initial values
as values, all the type string.

 **Returns:** A dictionary with the initial values of the parameters of the plugin.

**Return type:** dict

</details>

<details>
<summary>

**`stream(working_dir: str)`**

</summary>

</details>

</details>

 <details>
<summary>

**`chunk(working_dir: str)`**

</summary>

Trigger the import of the data. This must be implemented in any class that
inherits from this class unless directly implementing streaming. The method will
receive a folder where it must store the data as parquet files, and return a
list of the paths of the files created. This files will then be loaded and
mapped to the dataset function in positional order, so if you want file.parquet
to be the first argument of the dataset function, you must return it first. If
you want a parameter to receive multiple files, return a list of the paths. For
example, you would give the following return to provide a first argument with a
single file and a second argument with two files:return
[“file1.parquet”, [“file2.parquet”, “file3.parquet”]]

**Returns:** The path of the file(s) created, inthe order they must be mapped to the dataset function

**Return type:** Union[str, Tuple[str, …], List[str]]

</details>

 <details>
<summary>

**`stream(working_dir: str)`**

</summary>

</details>

 <details>
<summary>

**`Stage(use_existing_data: bool = True)`**

</summary>

Bases: SourcePlugin

Inputs that have been placed in a stage, usually from a StageTrigger.

<details>
<summary>

**`stream(working_dir: str)`**

</summary>

</details>

<details>
<summary>`use_existing_data`</summary>

</details>

</details>

 <details>
<summary>

**`stream(working_dir: str)`**

</summary>

</details>

 <details>
<summary>

**`StageTrigger(**kwargs)`**

</summary>

Bases: [ABC](https://docs.python.org/3/library/abc.html#abc.ABC)

<details>
<summary>`end`</summary>

</details>

<details>
<summary>`out`</summary>

Returns a mapping of positional index to table name for the outputs
produced by this trigger, or None when the StageTrigger implementation only
supports generating a single table.

</details>

<details>
<summary>

**`run(ctx: StageTriggerContext)`**

</summary>

Continuously runs the stage trigger logic using the received context to
trigger and monitor executions.
SUCCESS if the StageTrigger.end datetime is reached. exit with 0.
ERROR if any issue stops it from working or recovering. exit with 1.
STOPPED upon a signal. exit with 0.

</details>

<details>
<summary>`start`</summary>

</details>

</details>

 <details>
<summary>

**`run(ctx: StageTriggerContext)`**

</summary>

Continuously runs the stage trigger logic using the received context to
trigger and monitor executions.
SUCCESS if the StageTrigger.end datetime is reached. exit with 0.
ERROR if any issue stops it from working or recovering. exit with 1.
STOPPED upon a signal. exit with 0.

</details>

 <details>
<summary>

**`StarRocksConn(**kwargs)`**

</summary>

Bases: Conn

Represents a connection configuration for StarRocks with cloud-staged bulk
loading.

StarRocks ingests data via cloud storage (S3, Azure Blob, or GCS): files are
first uploaded to the configured staging area and then loaded into StarRocks
using the `FILES()` table function in an `INSERT INTO … SELECT * FROM
FILES(...)` statement.

<details>
<summary>`credentials`</summary>

The credentials used to authenticate with StarRocks.  `None` if no
credentials were provided (StarRocks `root` user, no password).

</details>

<details>
<summary>`cx_dst_configs_starrocks`</summary>

Destination-specific configuration parameters for the StarRocks connection.

</details>

<details>
<summary>`cx_src_configs_starrocks`</summary>

Source-specific configuration parameters for the StarRocks connection.

</details>

<details>
<summary>`database`</summary>

The target StarRocks database name.

</details>

<details>
<summary>`enforce_connection_params`</summary>

Whether to enforce connection parameters for the StarRocks connection.

</details>

<details>
<summary>`host`</summary>

The StarRocks FE hostname or IP address.

</details>

<details>
<summary>`port`</summary>

The MySQL protocol port of the StarRocks FE server.

</details>

<details>
<summary>`staging`</summary>

The cloud storage staging configuration (S3Config, AzureConfig, or
GCSConfig).

</details>

</details>

 <details>
<summary>

**`StarRocksDest(**kwargs)`**

</summary>

Bases: DestinationPlugin

Destination plugin for writing data to StarRocks using cloud-staged bulk loading.

Data is first written as Parquet files to the configured cloud storage staging area
(S3, Azure Blob, or GCS) and then ingested into StarRocks via its `FILES()`
table function in an `INSERT INTO … SELECT * FROM FILES(...)` statement.

<details>
<summary>`conn`</summary>

The StarRocks connection configuration.

</details>

<details>
<summary>`destination_tables`</summary>

The table(s) to load data into.

</details>

<details>
<summary>`if_table_exists`</summary>

The strategy to follow when the table already exists.

</details>

<details>
<summary>`schema_evolution`</summary>

The schema evolution strategy.

</details>

<details>
<summary>

**`stream(working_dir: str, *results: list[LazyFrame | None])`**

</summary>

Write each result as a Parquet file and bulk load it into StarRocks via
the cloud staging area.

<ParamField path="working_dir">
  Temporary directory for intermediate Parquet files.
</ParamField>

<ParamField path="results">
  One polars LazyFrame (or None) per destination table,
in the same order as destination_tables.
</ParamField>

</details>

</details>

 <details>
<summary>

**`stream(working_dir: str, *results: list[LazyFrame | None])`**

</summary>

Write each result as a Parquet file and bulk load it into StarRocks via
the cloud staging area.

<ParamField path="working_dir">
  Temporary directory for intermediate Parquet files.
</ParamField>

<ParamField path="results">
  One polars LazyFrame (or None) per destination table,
in the same order as destination_tables.
</ParamField>

</details>

 <details>
<summary>

**`TableFrame(df: Mapping[str, Sequence[object] | Mapping[str, Sequence[object]]] | TableFrame | None = None, *, origin: TableFrameOrigin | None = TableFrameOrigin.INIT, properties: TableFrameProperties | None = None)`**

</summary>

Bases: [object](https://docs.python.org/3/library/functions.html#object)

&gt; Private Functions

<details>
<summary>

**`assert_has_cols(cols: str | list[str], exact: bool | None = False)`**

</summary>

Ensures that the (non-system) columns in the TableFrame match the expected
columns.

Raises an exception if the expectation is not met.

If exact is True, the check verifies that the TableFrame contains exactly the
expected columns, with no extra or missing ones.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>> tf.assert_has_cols("a")
>>> tf.assert_has_cols(["a", "b"], exact=True)
```

<ParamField path="cols">
  The expected column name(s). Can be a string or a list of strings.
</ParamField>

<ParamField path="exact">
  If True, checks that the TableFrame contains exactly the specified
columns.
</ParamField>

**Raises:** ValueError – If expected columns are missing or unexpected columns are
    present in the TableFrame.

</details>

<details>
<summary>

**`cast(dtypes: Mapping[td_typing.ColumnNameOrSelector | td_typing.DataType, td_typing.DataType] | td_typing.DataType, *, strict: bool = True)`**

</summary>

Cast columns to a new data type.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌──────┬──────┐
│ a    ┆ b    │
│ ---  ┆ ---  │
│ str  ┆ i64  │
╞══════╪══════╡
│ A    ┆ 1    │
│ X    ┆ 10   │
│ C    ┆ 3    │
│ D    ┆ 5    │
│ M    ┆ 9    │
│ A    ┆ 100  │
│ M    ┆ 50   │
│ null ┆ 20   │
│ F    ┆ null │
└──────┴──────┘
>>>
>>> tf.cast({"b":td.Float32}).collect()
>>>
┌──────┬───────┐
│ a    ┆ b     │
│ ---  ┆ ---   │
│ str  ┆ f32   │
╞══════╪═══════╡
│ A    ┆ 1.0   │
│ X    ┆ 10.0  │
│ C    ┆ 3.0   │
│ D    ┆ 5.0   │
│ M    ┆ 9.0   │
│ A    ┆ 100.0 │
│ M    ┆ 50.0  │
│ null ┆ 20.0  │
│ F    ┆ null  │
└──────┴───────┘
```

<ParamField path="dtypes">
  Mapping of the column name(s) to the new data type(s).
</ParamField>

<ParamField path="strict">
  If True, raises an error if the cast cannot be performed.
</ParamField>

</details>

<details>
<summary>

**`clear(n: int = 0)`**

</summary>

Clears all rows of the TableFrame preserving the schema.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌──────┬──────┐
│ a    ┆ b    │
│ ---  ┆ ---  │
│ str  ┆ i64  │
╞══════╪══════╡
│ A    ┆ 1    │
│ X    ┆ 10   │
│ C    ┆ 3    │
│ D    ┆ 5    │
│ M    ┆ 9    │
│ A    ┆ 100  │
│ M    ┆ 50   │
│ null ┆ 20   │
│ F    ┆ null │
└──────┴──────┘
>>>
>>> tf.cast({"b":td.Float32}).collect()
>>>
┌──────┬───────┐
│ a    ┆ b     │
│ ---  ┆ ---   │
│ str  ┆ f32   │
╞══════╪═══════╡
└──────┴───────┘
```

</details>

<details>
<summary>

**`columns(kind: Literal['all', 'user', 'system'] | None = 'user')`**

</summary>

</details>

<details>
<summary>

**`drop(*columns: td_typing.ColumnNameOrSelector | Iterable[td_typing.ColumnNameOrSelector], strict: bool = True)`**

</summary>

Discard columns from the TableFrame.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌──────┬──────┐
│ x    ┆ y    │
│ ---  ┆ ---  │
│ f64  ┆ f64  │
╞══════╪══════╡
│ 1.0  ┆ 2.0  │
│ 2.0  ┆ 2.0  │
│ 10.0 ┆ 10.0 │
│ 4.0  ┆ 10.0 │
│ 5.0  ┆ null │
│ null ┆ null │
└──────┴──────┘
>>>
>>> tf.drop("y")
>>>
┌──────┐
│ x    │
│ ---  │
│ f64  │
╞══════╡
│ 1.0  │
│ 2.0  │
│ NaN  │
│ 4.0  │
│ 5.0  │
│ null │
└──────┘
```

<ParamField path="columns">
  Columns to drop.
</ParamField>

<ParamField path="strict">
  If True, raises an error if a column does not exist.
</ParamField>

</details>

<details>
<summary>

**`drop_nans(subset: td_typing.ColumnNameOrSelector | Collection[td_typing.ColumnNameOrSelector] | None = None)`**

</summary>

Drop rows with NaN values.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌──────┬──────┬──────┐
│ ss   ┆ u    ┆ ff   │
│ ---  ┆ ---  ┆ ---  │
│ str  ┆ i64  ┆ f64  │
╞══════╪══════╪══════╡
│ A    ┆ 1    ┆ 1.1  │
│ B    ┆ 0    ┆ 0.0  │
│ A    ┆ 2    ┆ 2.2  │
│ B    ┆ 3    ┆ 3.3  │
│ B    ┆ 4    ┆ 4.4  │
│ C    ┆ 5    ┆ -1.1 │
│ C    ┆ 6    ┆ -2.2 │
│ C    ┆ 7    ┆ -3.3 │
│ D    ┆ 8    ┆ inf  │
│ F    ┆ 9    ┆ NaN  │
│ null ┆ null ┆ null │
└──────┴──────┴──────┘
>>>
>>> tf.unique("a", keep="last")
┌─────┬─────┬──────┐
│ ss  ┆ u   ┆ ff   │
│ --- ┆ --- ┆ ---  │
│ str ┆ i64 ┆ f64  │
╞═════╪═════╪══════╡
│ A   ┆ 1   ┆ 1.1  │
│ B   ┆ 0   ┆ 0.0  │
│ A   ┆ 2   ┆ 2.2  │
│ B   ┆ 3   ┆ 3.3  │
│ B   ┆ 4   ┆ 4.4  │
│ C   ┆ 5   ┆ -1.1 │
│ C   ┆ 6   ┆ -2.2 │
│ C   ┆ 7   ┆ -3.3 │
│ D   ┆ 8   ┆ inf  │
└─────┴─────┴──────┘
>>>
```

</details>

<details>
<summary>

**`drop_nulls(subset: td_typing.ColumnNameOrSelector | Collection[td_typing.ColumnNameOrSelector] | None = None)`**

</summary>

Drop rows with null values.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌──────┬──────┬──────┐
│ ss   ┆ u    ┆ ff   │
│ ---  ┆ ---  ┆ ---  │
│ str  ┆ i64  ┆ f64  │
╞══════╪══════╪══════╡
│ A    ┆ 1    ┆ 1.1  │
│ B    ┆ 0    ┆ 0.0  │
│ A    ┆ 2    ┆ 2.2  │
│ B    ┆ 3    ┆ 3.3  │
│ B    ┆ 4    ┆ 4.4  │
│ C    ┆ 5    ┆ -1.1 │
│ C    ┆ 6    ┆ -2.2 │
│ C    ┆ 7    ┆ -3.3 │
│ D    ┆ 8    ┆ inf  │
│ F    ┆ 9    ┆ NaN  │
│ G    ┆ null ┆ 2.3  │
└──────┴──────┴──────┘
>>>
>>> tf.drop_nulls("a")
>>>
┌─────┬─────┬──────┐
│ ss  ┆ u   ┆ ff   │
│ --- ┆ --- ┆ ---  │
│ str ┆ i64 ┆ f64  │
╞═════╪═════╪══════╡
│ A   ┆ 1   ┆ 1.1  │
│ B   ┆ 0   ┆ 0.0  │
│ A   ┆ 2   ┆ 2.2  │
│ B   ┆ 3   ┆ 3.3  │
│ B   ┆ 4   ┆ 4.4  │
│ C   ┆ 5   ┆ -1.1 │
│ C   ┆ 6   ┆ -2.2 │
│ C   ┆ 7   ┆ -3.3 │
│ D   ┆ 8   ┆ inf  │
│ F   ┆ 9   ┆ NaN  │
└─────┴─────┴──────┘
```

</details>

<details>
<summary>`dtypes`</summary>

</details>

<details>
<summary>

**`empty(schema: SimpleSchema = None)`**

</summary>

Creates an empty (no column - no row) TableFrame.

</details>

<details>
<summary>

**`explain(**kwargs)`**

</summary>

Provide private accessibility level wrapper.

</details>

<details>
<summary>

**`extract_as_columns(offset: int, length: int)`**

</summary>

Extract a slice of rows from the table as a column-oriented dictionary.

The result is a mapping of column names to lists of values from the selected
rows.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌─────┬─────┐
│ a   ┆ b   │
│ --- ┆ --- │
│ str ┆ i64 │
╞═════╪═════╡
│ A   ┆ 1   │
│ X   ┆ 10  │
│ C   ┆ 3   │
│ D   ┆ 5   │
│ M   ┆ 9   │
└─────┴─────┘
>>>
>>> tf.extract_as_columns(offset=0, length=2)
    {
        "a": ["A", 1],
        "b": ["X", 10]
    }
```

<ParamField path="offset" type="int">
  The starting row index of the slice.
</ParamField>

<ParamField path="length" type="int">
  The number of rows to include in the slice.
</ParamField>

**Returns:** A dictionary where each key is a column name,
and its value is a list of values from the selected slice.

**Return type:** dict[str, list[Any]]

</details>

<details>
<summary>

**`extract_as_rows(offset: int, length: int)`**

</summary>

Extract a slice of rows from the TableFrame as a list of dictionaries.

Each dictionary represents one row, where keys are column names
and values are the corresponding cell values.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌─────┬─────┐
│ a   ┆ b   │
│ --- ┆ --- │
│ str ┆ i64 │
╞═════╪═════╡
│ A   ┆ 1   │
│ X   ┆ 10  │
│ C   ┆ 3   │
│ D   ┆ 5   │
│ M   ┆ 9   │
└─────┴─────┘
>>>
>>> tf.extract_as_rows(offset=0, length=2)
[
>>>
>>> tf.extract_as_rows(offset=0, length=2)
    [
        {"a": "A", "b": 1},
        {"a": "X", "b": 10},
    ]
```

<ParamField path="offset" type="int">
  The starting row index of the slice.
</ParamField>

<ParamField path="length" type="int">
  The number of rows to include in the slice.
</ParamField>

**Returns:** A list of row dictionaries.

**Return type:** list[dict[str, Any]]

</details>

<details>
<summary>

**`fill_nan(value: int | float | Expr | None)`**

</summary>

Replace all NaN values in the TableFrame with the given value.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌──────┬──────┐
│ x    ┆ y    │
│ ---  ┆ ---  │
│ f64  ┆ f64  │
╞══════╪══════╡
│ 1.0  ┆ 2.0  │
│ 2.0  ┆ 2.0  │
│ NaN  ┆ NaN  │
│ 4.0  ┆ NaN  │
│ 5.0  ┆ null │
│ null ┆ null │
└──────┴──────┘
>>>
>>> tf.fill_nan(10)
>>>
┌──────┬──────┐
│ x    ┆ y    │
│ ---  ┆ ---  │
│ f64  ┆ f64  │
╞══════╪══════╡
│ 1.0  ┆ 2.0  │
│ 2.0  ┆ 2.0  │
│ 10.0 ┆ 10.0 │
│ 4.0  ┆ 10.0 │
│ 5.0  ┆ null │
│ null ┆ null │
└──────┴──────┘
```

</details>

<details>
<summary>

**`fill_null(value: Any | Expr | None = None)`**

</summary>

Replace all null values in the TableFrame with the given value.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌──────┬──────┐
│ x    ┆ y    │
│ ---  ┆ ---  │
│ f64  ┆ f64  │
╞══════╪══════╡
│ 1.0  ┆ 2.0  │
│ 2.0  ┆ 2.0  │
│ NaN  ┆ NaN  │
│ 4.0  ┆ NaN  │
│ 5.0  ┆ null │
│ null ┆ null │
└──────┴──────┘
>>>
>>> tf.fill_null(20)
>>>
┌──────┬──────┐
│ x    ┆ y    │
│ ---  ┆ ---  │
│ f64  ┆ f64  │
╞══════╪══════╡
│ 1.0  ┆ 2.0  │
│ 2.0  ┆ 2.0  │
│ NaN  ┆ NaN  │
│ 4.0  ┆ NaN  │
│ 5.0  ┆ 20.0 │
│ 20.0 ┆ 20.0 │
└──────┴──────┘
```

</details>

<details>
<summary>

**`filter(*predicates: td_typing.IntoExprColumn | Iterable[td_typing.IntoExprColumn] | bool | list[bool] | np.ndarray[Any, Any])`**

</summary>

Filter the TableFrame based on the given predicates.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌─────┬─────┐
│ a   ┆ b   │
│ --- ┆ --- │
│ str ┆ i64 │
╞═════╪═════╡
│ A   ┆ 1   │
│ X   ┆ 10  │
│ C   ┆ 3   │
│ D   ┆ 5   │
│ M   ┆ 9   │
│ A   ┆ 100 │
│ M   ┆ 50  │
└─────┴─────┘
>>>
>>> tf.filter(td.col("a").is_in(["A", "C"]).or_(td.col("b").eq(10)))
>>>
┌─────┬─────┐
│ a   ┆ b   │
│ --- ┆ --- │
│ str ┆ i64 │
╞═════╪═════╡
│ A   ┆ 1   │
│ X   ┆ 10  │
│ C   ┆ 3   │
│ A   ┆ 100 │
└─────┴─────┘
```

</details>

<details>
<summary>

**`first`**

</summary>

Return a TableFrame with the first row.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌─────┬─────┐
│ a   ┆ b   │
│ --- ┆ --- │
│ str ┆ i64 │
╞═════╪═════╡
│ A   ┆ 1   │
│ X   ┆ 10  │
│ C   ┆ 3   │
│ D   ┆ 5   │
│ M   ┆ 9   │
└─────┴─────┘
>>>
>>> tf.first()
>>>
┌─────┬─────┐
│ a   ┆ b   │
│ --- ┆ --- │
│ str ┆ i64 │
╞═════╪═════╡
│ A   ┆ 1   │
└─────┴─────┘
```

</details>

<details>
<summary>

**`first_row(named: bool = False)`**

</summary>

Return a tuple or dictionary with the first row, or None if no row.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌─────┬─────┐
│ A   ┆ B   │
│ --- ┆ --- │
│ str ┆ i64 │
╞═════╪═════╡
│ a   ┆ 1   │
│ b   ┆ 2   │
│ c   ┆ 3   │
└─────┴─────┘
>>>
>>> tf.last_row()
>>>
('a', 1)
>>>
>>> tf.last_row(named=True)
>>>
{'A': 'a', 'B': '1'}
```

</details>

<details>
<summary>

**`from_dict(data: Mapping[str, Sequence[object] | Mapping[str, Sequence[object]]] | None = None)`**

</summary>

Creates tableframe from a dictionary, or None.
None produces as an empty (no column - no row) tableframe.

</details>

<details>
<summary>

**`from_pandas(data: pd.DataFrame | None = None)`**

</summary>

Creates tableframe from a pandas dataframe, or None.
None produces as an empty (no column - no row) tableframe.

</details>

<details>
<summary>

**`from_polars(data: LazyFrame | DataFrame | None = None)`**

</summary>

Creates tableframe from a polars dataframe or lazyframe, or None.
None produces as an empty (no column - no row) tableframe.

</details>

<details>
<summary>

**`grok(expr: td_typing.IntoExpr, pattern: str, schema: dict[str, td_col.Column])`**

</summary>

Parse log text into structured fields using a Grok pattern.

Applies a Grok pattern to the given column or expression and directly appends
**one new column per named capture** in the pattern to the output TableFrame.
Rows that do not match the pattern will contain null values for the
extracted columns.

Example

```text
>>> import tabsdata as td
>>> tf = td.TableFrame({"logs": [
...     "alice-login-2023",
...     "bob-logout-2024",
... ]})
>>>
>>> # Capture 3 fields: user, action, year
>>> log_pattern = r"%{WORD:user}-%{WORD:action}-%{INT:year}"
>>> log_schema = {
>>>     "word": td_col.Column("user", td.String),
>>>     "action": td_col.Column("action", td.String),
>>>     "year": td_col.Column("year", td.Int8),
>>> }
>>> out = tf.grok("logs", log_pattern, log_schema)
>>> out.collect()
┌──────────────────┬───────┬────────┬──────┐
│ logs             ┆ user  ┆ action ┆ year │
│ ---              ┆ ---   ┆ ---    ┆ ---  │
│ str              ┆ str   ┆ str    ┆ i64  │
╞══════════════════╪═══════╪════════╪══════╡
│ alice-login-2023 ┆ alice ┆ login  ┆ 2023 │
│ bob-logout-2024  ┆ bob   ┆ logout ┆ 2024 │
└──────────────────┴───────┴────────┴──────┘
```

Notes

- The function automatically expands the Grok captures into separate
columns.
- Non-matching rows will show null for the extracted columns.
- If a pattern defines duplicate capture names, numeric suffixes like
field, field[1] will be used to disambiguate them.

<ParamField path="expr" type="IntoExpr">
  Column name or expression that resolves to a single
string column containing log lines.
</ParamField>

<ParamField path="pattern" type="str">
  Grok pattern with named captures (e.g., %&#123;WORD:user&#125;).
</ParamField>

<ParamField path="schema" type="dict">
  A mapping where each capture name
is associated with its corresponding column definition, specifying
both the column name and its data type.
</ParamField>

**Returns:** A new TableFrame with one column per Grok capture added.

**Return type:** TableFrame

</details>

<details>
<summary>

**`group_by(*by: td_typing.IntoExpr | Iterable[td_typing.IntoExpr])`**

</summary>

Perform a group by on the TableFrame.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌─────┬─────┐
│ a   ┆ b   │
│ --- ┆ --- │
│ str ┆ i64 │
╞═════╪═════╡
│ A   ┆ 1   │
│ X   ┆ 10  │
│ C   ┆ 3   │
│ D   ┆ 5   │
│ M   ┆ 9   │
│ A   ┆ 100 │
│ M   ┆ 50  │
└─────┴─────┘
>>>
>>> tf.group_by(td.col("a")).agg(td.col("b").sum())
>>>
┌─────┬─────┐
│ a   ┆ b   │
│ --- ┆ --- │
│ str ┆ i64 │
╞═════╪═════╡
│ M   ┆ 59  │
│ A   ┆ 101 │
│ C   ┆ 3   │
│ D   ┆ 5   │
│ X   ┆ 10  │
└─────┴─────┘
```

</details>

<details>
<summary>

**`has_cols(cols: str | list[str], exact: bool | None = False)`**

</summary>

Verifies the presence of (non-system) columns in the TableFrame.

If exact is True, the check ensures that the TableFrame contains exactly the
specified columns (excluding system columns), with no extras or omissions.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌──────┬──────┐
│ a    ┆ b    │
│ ---  ┆ ---  │
│ str  ┆ i64  │
╞══════╪══════╡
│ A    ┆ 1    │
└──────┴──────┘
>>>
>>> tf.has_cols("a")
>>>
(True, {}, {"b"})
>>>
>>> tf.has_cols(["a", "c", "d"])
>>>
(False, {"c", "d"}, {"b"})
>>>
>>> tf.has_cols("a", exact=True)
>>>
(False, {}, {"b"})
>>>
>>> tf.has_cols(["a", "b"], exact=True)
>>>
(True, {}, {})
```

<ParamField path="cols">
  The column name(s) to verify. Can be a string or a list of strings.
</ParamField>

<ParamField path="exact">
  If True, checks that the TableFrame contains exactly the specified
columns.
</ParamField>

**Returns:** A boolean indicating whether the check was successful.
A set of columns in cols missing in the TableFrame.
A set of columns in the TableFrame missing in cols.

**Return type:** tuple[bool, set[str], set[str]]

</details>

<details>
<summary>

**`has_same_schema(tf: TableFrame)`**

</summary>

Verifies if the schema of the current TableFrame is same than the provided
TableFrame.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf1: td.TableFrame ...
>>>
┌──────┬──────┐
│ a    ┆ b    │
│ ---  ┆ ---  │
│ str  ┆ i64  │
╞══════╪══════╡
│ A    ┆ 1    │
└──────┴──────┘
>>>
>>> tf2: td.TableFrame ...
>>>
┌──────┬──────┐
│ a    ┆ c    │
│ ---  ┆ ---  │
│ str  ┆ i64  │
╞══════╪══════╡
│ A    ┆ 1    │
└──────┴──────┘
>>> tf1.has_same_schema(tf2)
>>>
False
>>>
>>> tf1: td.TableFrame ...
>>>
┌──────┬──────┐
│ a    ┆ b    │
│ ---  ┆ ---  │
│ str  ┆ i64  │
╞══════╪══════╡
│ A    ┆ 1    │
└──────┴──────┘
>>>
>>> tf2: td.TableFrame ...
>>>
┌──────┬──────┐
│ a    ┆ b    │
│ ---  ┆ ---  │
│ str  ┆ str  │
╞══════╪══════╡
│ A    ┆ 1    │
└──────┴──────┘
>>> tf1.has_same_schema(tf2)
>>>
False
```

**Returns:** Whether the condition is met or not.

**Return type:** bool

</details>

<details>
<summary>

**`head(n: int = 5)`**

</summary>

Return a TableFrame with the first n rows.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌─────┬─────┐
│ a   ┆ b   │
│ --- ┆ --- │
│ str ┆ i64 │
╞═════╪═════╡
│ A   ┆ 1   │
│ X   ┆ 10  │
│ C   ┆ 3   │
│ D   ┆ 5   │
│ M   ┆ 9   │
└─────┴─────┘
>>>
>>> tf.head(2)
>>>
┌─────┬─────┐
│ a   ┆ b   │
│ --- ┆ --- │
│ str ┆ i64 │
╞═════╪═════╡
│ A   ┆ 1   │
│ X   ┆ 10  │
└─────┴─────┘
```

</details>

<details>
<summary>

**`inspect(**kwargs)`**

</summary>

Provide private accessibility level wrapper.

</details>

<details>
<summary>

**`is_empty`**

</summary>

Checks if a TableFrame has no rows.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌──────┬──────┐
│ a    ┆ b    │
│ ---  ┆ ---  │
│ str  ┆ i64  │
╞══════╪══════╡
│ A    ┆ 1    │
└──────┴──────┘
>>>
>>> tf.is_empty()
>>>
False
>>>
>>> tf: td.TableFrame ...
>>>
┌──────┬──────┐
│ a    ┆ b    │
│ ---  ┆ ---  │
│ str  ┆ i64  │
╞══════╪══════╡
└──────┴──────┘
>>>
>>> tf.is_empty()
>>>
True
```

</details>

<details>
<summary>

**`item`**

</summary>

Returns a scalar value if the TableFrame contains exactly one user column and
one row.

Raises an exception if there is more than one user column or more than one row.

Returns None if the TableFrame is empty.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌─────┐
│ a   │
│ --- │
│ str │
╞═════╡
│ A   │
└─────┘
>>>
>>> tf.python_version()
>>>
A
```

</details>

<details>
<summary>

**`join(other: TableFrame, on: str | Expr | Sequence[str | Expr] | None = None, how: Literal['inner', 'left', 'right', 'full', 'semi', 'anti', 'cross', 'outer'] = 'inner', *, left_on: str | Expr | Sequence[str | Expr] | None = None, right_on: str | Expr | Sequence[str | Expr] | None = None, suffix: str = '_right', join_nulls: bool = False, coalesce: bool | None = None)`**

</summary>

Join the TableFrame with another TableFrame.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf1: td.TableFrame ...
>>>
┌──────┬──────┐
│ a    ┆ b    │
│ ---  ┆ ---  │
│ str  ┆ i64  │
╞══════╪══════╡
│ A    ┆ 1    │
│ X    ┆ 10   │
│ C    ┆ 3    │
│ D    ┆ 5    │
│ M    ┆ 9    │
│ A    ┆ 100  │
│ M    ┆ 50   │
│ null ┆ 20   │
│ F    ┆ null │
└──────┴──────┘
>>>
>>> tf2: td.TableFrame ...
>>>
┌──────┬──────┐
│ a    ┆ b    │
│ ---  ┆ ---  │
│ str  ┆ i64  │
╞══════╪══════╡
│ A    ┆ 3    │
│ Y    ┆ 4    │
│ Z    ┆ 5    │
│ A    ┆ 0    │
│ M    ┆ 6    │
│ null ┆ 8    │
│ F    ┆ null │
└──────┴──────┘
>>>
An inner join:
>>>
>>> tf1.join(tf2, on="a", how="inner")
>>>
┌─────┬──────┬─────────┐
│ a   ┆ b    ┆ b_right │
│ --- ┆ ---  ┆ ---     │
│ str ┆ i64  ┆ i64     │
╞═════╪══════╪═════════╡
│ A   ┆ 1    ┆ 3       │
│ A   ┆ 1    ┆ 0       │
│ M   ┆ 9    ┆ 6       │
│ A   ┆ 100  ┆ 3       │
│ A   ┆ 100  ┆ 0       │
│ M   ┆ 50   ┆ 6       │
│ F   ┆ null ┆ null    │
└─────┴──────┴─────────┘
>>>
A left join:
>>>
>>> tf1.join(tf2, on="a", how="left")
>>>
┌──────┬──────┬─────────┐
│ a    ┆ b    ┆ b_right │
│ ---  ┆ ---  ┆ ---     │
│ str  ┆ i64  ┆ i64     │
╞══════╪══════╪═════════╡
│ A    ┆ 1    ┆ 3       │
│ A    ┆ 1    ┆ 0       │
│ X    ┆ 10   ┆ null    │
│ C    ┆ 3    ┆ null    │
│ D    ┆ 5    ┆ null    │
│ …    ┆ …    ┆ …       │
│ A    ┆ 100  ┆ 3       │
│ A    ┆ 100  ┆ 0       │
│ M    ┆ 50   ┆ 6       │
│ null ┆ 20   ┆ null    │
│ F    ┆ null ┆ null    │
└──────┴──────┴─────────┘
>>>
Turning off column coalesce:
>>>
>>> tf1.join(tf2, on="a", coalesce=False)
>>>
┌─────┬──────┬─────────┬─────────┐
│ a   ┆ b    ┆ a_right ┆ b_right │
│ --- ┆ ---  ┆ ---     ┆ ---     │
│ str ┆ i64  ┆ str     ┆ i64     │
╞═════╪══════╪═════════╪═════════╡
│ A   ┆ 1    ┆ A       ┆ 3       │
│ A   ┆ 1    ┆ A       ┆ 0       │
│ M   ┆ 9    ┆ M       ┆ 6       │
│ A   ┆ 100  ┆ A       ┆ 3       │
│ A   ┆ 100  ┆ A       ┆ 0       │
│ M   ┆ 50   ┆ M       ┆ 6       │
│ F   ┆ null ┆ F       ┆ null    │
└─────┴──────┴─────────┴─────────┘
```

<ParamField path="other">
  The TableFrame to join.
</ParamField>

<ParamField path="on">
  Name(s) of the columns to join on. The column name(s) must be in
both TableFrame’s. Don’t use this parameter if using `left_on
and right_on parameters, or if how=”cross”.
</ParamField>

<ParamField path="how">
  Join strategy:

inner: An inner join.
left: A left join.
right: A rigth join.
full: A full join.
cross: The cartesian product.
semi: An inner join but only returning the columns from left
TableFrame.
anti: Rows from the left TableFrame that have no match
in the right TableFrame.
</ParamField>

<ParamField path="">
  
</ParamField>

<ParamField path="">
  
</ParamField>

<ParamField path="">
  
</ParamField>

<ParamField path="">
  
</ParamField>

<ParamField path="">
  
</ParamField>

<ParamField path="">
  
</ParamField>

<ParamField path="">
  
</ParamField>

<ParamField path="left_on">
  Name(s) of the columns to join on from the left TableFrame.
It must be used together wit the right_on parameter.
It cannot be used with the on parameter.
</ParamField>

<ParamField path="right_on">
  Name(s) of the columns to join on from the right TableFrame.
It must be used together wit the left_on parameter.
It cannot be used with the on parameter.
</ParamField>

<ParamField path="suffix">
  Duplicate columns on the right Table are appended this suffix.
</ParamField>

<ParamField path="join_nulls">
  If null value matches should produce join rows or not.
</ParamField>

<ParamField path="coalesce">
  Collapse join columns into a single column.
</ParamField>

</details>

<details>
<summary>

**`last`**

</summary>

Return a TableFrame with the last row.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌─────┬─────┐
│ a   ┆ b   │
│ --- ┆ --- │
│ str ┆ i64 │
╞═════╪═════╡
│ A   ┆ 1   │
│ X   ┆ 10  │
│ C   ┆ 3   │
│ D   ┆ 5   │
│ M   ┆ 9   │
└─────┴─────┘
>>>
>>> tf.last()
>>>
┌─────┬─────┐
│ a   ┆ b   │
│ --- ┆ --- │
│ str ┆ i64 │
╞═════╪═════╡
│ M   ┆ 9   │
└─────┴─────┘
```

</details>

<details>
<summary>

**`last_row(named: bool = False)`**

</summary>

Return a tuple or dictionary with the last row, or None if no row.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌─────┬─────┐
│ A   ┆ B   │
│ --- ┆ --- │
│ str ┆ i64 │
╞═════╪═════╡
│ a   ┆ 1   │
│ b   ┆ 2   │
│ c   ┆ 3   │
└─────┴─────┘
>>>
>>> tf.last_row()
>>>
('c', 3)
>>>
>>> tf.last_row(named=True)
>>>
{'A': 'c', 'B': 3}
```

</details>

<details>
<summary>

**`limit(n: int = 5)`**

</summary>

Return a TableFrame with the first n rows.
This is equivalent to head.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌─────┬─────┐
│ a   ┆ b   │
│ --- ┆ --- │
│ str ┆ i64 │
╞═════╪═════╡
│ A   ┆ 1   │
│ X   ┆ 10  │
│ C   ┆ 3   │
│ D   ┆ 5   │
│ M   ┆ 9   │
└─────┴─────┘
>>>
>>> tf.limit(2)
>>>
┌─────┬─────┐
│ a   ┆ b   │
│ --- ┆ --- │
│ str ┆ i64 │
╞═════╪═════╡
│ A   ┆ 1   │
│ X   ┆ 10  │
└─────┴─────┘
```

</details>

<details>
<summary>

**`rename(mapping: dict[str, str])`**

</summary>

Rename columns from the TableFrame.

Examples:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌──────┬──────┐
│ i    ┆ f    │
│ ---  ┆ ---  │
│ i32  ┆ f64  │
╞══════╪══════╡
│ 1    ┆ 3.1  │
│ 2    ┆ 4.1  │
│ 3    ┆ 5.9  │
│ 4    ┆ 2.6  │
│ 5    ┆ 53.5 │
│ 6    ┆ 8.97 │
└──────┴──────┘
>>>
>>> tf.{"i": "index", "f": "amount"})
>>>
┌───────┬────────┐
│ index ┆ amount │
│ ----- ┆ ------ │
│ i32   ┆ f64    │
╞═══════╪════════╡
│ 1     ┆ 3.1    │
│ 2     ┆ 4.1    │
│ 3     ┆ 5.9    │
│ 4     ┆ 2.6    │
│ 5     ┆ 53.5   │
│ 6     ┆ 8.97   │
└───────┴────────┘
```

</details>

<details>
<summary>`schema`</summary>

</details>

<details>
<summary>

**`select(*exprs: td_typing.IntoExpr | Iterable[td_typing.IntoExpr], **named_exprs: td_typing.IntoExpr)`**

</summary>

Select column(s) from the TableFrame.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌─────┬─────┐
│ a   ┆ b   │
│ --- ┆ --- │
│ str ┆ i64 │
╞═════╪═════╡
│ A   ┆ 1   │
│ X   ┆ 10  │
│ C   ┆ 3   │
│ D   ┆ 5   │
│ M   ┆ 9   │
│ A   ┆ 100 │
│ M   ┆ 50  │
└─────┴─────┘
>>>
>>> tf.select(td.col("a"), td.col("b").mul(2).alias("bx2"),)
>>>
┌─────┬─────┐
│ a   ┆ bx2 │
│ --- ┆ --- │
│ str ┆ i64 │
╞═════╪═════╡
│ A   ┆ 2   │
│ X   ┆ 20  │
│ C   ┆ 6   │
│ D   ┆ 10  │
│ M   ┆ 18  │
│ A   ┆ 200 │
│ M   ┆ 100 │
└─────┴─────┘
```

<ParamField path="exprs">
  Columns or expressions to select.
</ParamField>

<ParamField path="named_exprs">
  Named expressions to select.
</ParamField>

</details>

<details>
<summary>

**`show_graph(**kwargs)`**

</summary>

Provide private accessibility level wrapper.

</details>

<details>
<summary>

**`slice(offset: int, length: int | None = None)`**

</summary>

Return a TableFrame with a slice of the original TableFrame

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌─────┬─────┐
│ a   ┆ b   │
│ --- ┆ --- │
│ str ┆ i64 │
╞═════╪═════╡
│ A   ┆ 1   │
│ X   ┆ 10  │
│ C   ┆ 3   │
│ D   ┆ 5   │
│ M   ┆ 9   │
└─────┴─────┘
>>>
>>> tf.slice(2,2)
>>>
┌─────┬─────┐
│ a   ┆ b   │
│ --- ┆ --- │
│ str ┆ i64 │
╞═════╪═════╡
│ C   ┆ 3   │
│ D   ┆ 5   │
└─────┴─────┘
```

<ParamField path="offset">
  Slice starting index.
</ParamField>

<ParamField path="length">
  The length of the slice. None means all the way to the end.
</ParamField>

</details>

<details>
<summary>

**`sort(by: td_typing.IntoExpr | Iterable[td_typing.IntoExpr], *more_by: td_typing.IntoExpr, descending: bool | Sequence[bool] = False, nulls_last: bool | Sequence[bool] = False, maintain_order: bool = False)`**

</summary>

Sort the TableFrame by the given column(s) or expression(s).

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌──────┬──────┐
│ a    ┆ b    │
│ ---  ┆ ---  │
│ str  ┆ i64  │
╞══════╪══════╡
│ A    ┆ 1    │
│ X    ┆ 10   │
│ C    ┆ 3    │
│ D    ┆ 5    │
│ M    ┆ 9    │
│ A    ┆ 100  │
│ M    ┆ 50   │
│ null ┆ 20   │
│ F    ┆ null │
└──────┴──────┘
>>>
>>> tf.sort(td.col("a"), descending = True)
>>>
┌──────┬───────┐
│ a    ┆ b     │
│ ---  ┆ ---   │
│ str  ┆ f32   │
╞══════╪═══════╡
│ A    ┆ 1.0   │
│ X    ┆ 10.0  │
│ C    ┆ 3.0   │
│ D    ┆ 5.0   │
│ M    ┆ 9.0   │
│ A    ┆ 100.0 │
│ M    ┆ 50.0  │
│ null ┆ 20.0  │
│ F    ┆ null  │
└──────┴───────┘
```

<ParamField path="by">
  Column(s) or expression(s) to sort by.
</ParamField>

<ParamField path="more_by">
  Additional colums to sort by.
</ParamField>

<ParamField path="descending">
  Specifies if the sorting should be descending or not.
</ParamField>

<ParamField path="nulls_last">
  Specifies if null values should be placed last.
</ParamField>

<ParamField path="maintain_order">
  Preserve the order of equal rows.
</ParamField>

</details>

<details>
<summary>

**`tail(n: int = 5)`**

</summary>

Return a TableFrame with the last n rows.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌─────┬─────┐
│ a   ┆ b   │
│ --- ┆ --- │
│ str ┆ i64 │
╞═════╪═════╡
│ A   ┆ 1   │
│ X   ┆ 10  │
│ C   ┆ 3   │
│ D   ┆ 5   │
│ M   ┆ 9   │
└─────┴─────┘
>>>
>>> tf.tail(2)
>>>
┌─────┬─────┐
│ a   ┆ b   │
│ --- ┆ --- │
│ str ┆ i64 │
╞═════╪═════╡
│ D   ┆ 5   │
│ M   ┆ 9   │
└─────┴─────┘
```

</details>

<details>
<summary>

**`to_pandas`**

</summary>

Creates a pandas dataframe from this tableframe.

</details>

<details>
<summary>

**`to_polars_df`**

</summary>

Creates a polars dataframe from this tableframe.

</details>

<details>
<summary>

**`to_polars_lf`**

</summary>

Creates a polars lazyframe from this tableframe.

</details>

<details>
<summary>

**`udf(on: td_typing.IntoExpr | list[td_typing.IntoExpr], function: td_udf.UDF)`**

</summary>

Apply a user-defined function (UDF) to the columns resolved by expr.

The selected columns are supplied to function, which can implement either
on_batch or on_element. An on_batch implementation receives a list of
Polars series representing the selected columns and must return a list of
Polars series with matching length. An on_element implementation receives
a list of Python scalars for each row and returns a list of scalars; the
framework wraps this in an efficient batch executor, so data still flows in
batches even when authoring row-wise logic. In both cases the returned series
become new columns appended to the original TableFrame.

By default, both methods receive their inputs as a list. Override the
signature property in your UDF class to return “unpacked” to have each column
passed as a separate argument instead.

 Creating UDFs:1. Subclass [tabsdata.tableframe.udf.function.UDF](bymod-tabsdata.tableframe.udf.function#tabsdata.tableframe.udf.function.UDF).
2. Implement `__init__` to call `super().__init__(output_columns)` where
`output_columns` is a tuple or list of tuples `(name, data type)`
specifying the UDF default output schema (column names and data types).
Each tuple must contain a column name (string) and a data type
(DataType).
3. Override exactly one of on_batch or on_element, to implement the UDF
function logic.
4. Return a list of TabsData Series (for on_batch) or TabsData supported
scalars (for on_element) with the same length as specified in the
output schema.
5. If overriding the on_batch method, the return type must be a list of
TabsData Series. If overriding the on_element method, the return type
must be a list of supported TabsData scalar values. For both cases, the
number of elements in the returned lists must match the number of
elements in the output_columns list provided to the UDF constructor.

  Using UDFs:

 1. Instantiate a function created as above.
2. Pass it to TableFrame method udf().
3. Optionally use `UDF.output_columns()` to override output column names
or data types after instantiation.

 Examples

```text
>>> import tabsdata as td
>>> import tabsdata.tableframe as tdf
>>>
>>> class SumUDF(tdf.UDF):
...     def __init__(self):
...         super().__init__(("total", tdf.Int64))
...
...     def on_batch(self, series):
...         return [series[0] + series[1]]
>>>
>>> tf = td.TableFrame({"a": [1, 2, 3], "b": [10, 20, 30]})
>>> print(tf)
┌─────┬─────┐
│ a   ┆ b   │
│ --- ┆ --- │
│ i64 ┆ i64 │
╞═════╪═════╡
│ 1   ┆ 10  │
│ 2   ┆ 20  │
│ 3   ┆ 30  │
└─────┴─────┘
>>> tf.udf(td.col("a", "b"), SumUDF())
>>> print(tf)
┌─────┬─────┬───────┐
│ a   ┆ b   ┆ total │
│ --- ┆ --- ┆ ---   │
│ i64 ┆ i64 ┆ i64   │
╞═════╪═════╪═══════╡
│ 1   ┆ 10  ┆ 11    │
│ 2   ┆ 20  ┆ 22    │
│ 3   ┆ 30  ┆ 33    │
└─────┴─────┴───────┘
```

```text
>>> class RatioUDF(tdf.UDF):
...     def __init__(self):
...         super().__init__(("ratio", tdf.Float64))
...
...     def on_element(self, values):
...         return [values[0] / values[1]]
>>>
>>> tf = td.TableFrame({"numerator": [10, 20, 30],
>>>                     "denominator": [2, 5, 10],})
>>> print(tf)
┌───────────┬──────────────┐
│ numerator ┆ denominator  │
│ ---       ┆ ---          │
│ i64       ┆ i64          │
╞═══════════╪══════════════╡
│ 10        ┆ 2            │
│ 20        ┆ 5            │
│ 30        ┆ 10           │
└───────────┴──────────────┘
>>> tf.udf(td.col("numerator", "denominator"), RatioUDF()).collect()
>>> print(tf)
┌───────────┬──────────────┬──────┐
│ numerator ┆ denominator  ┆ ratio│
│ ---       ┆ ---          ┆ ---  │
│ i64       ┆ i64          ┆ f64  │
╞═══════════╪══════════════╪══════╡
│ 10        ┆ 2            ┆ 5.0  │
│ 20        ┆ 5            ┆ 4.0  │
│ 30        ┆ 10           ┆ 3.0  │
└───────────┴──────────────┴──────┘
```

Using signature property to receive individual arguments:

```text
>>> class RatioUnpackedUDF(tdf.UDF):
...     def __init__(self):
...         super().__init__(("ratio", tdf.Float64))
...
...     @property
...     def signature(self):
...         return "unpacked"
...
...     def on_element(self, numerator, denominator):
...         return [numerator / denominator]
>>>
>>> tf = td.TableFrame({"numerator": [10, 20, 30],
>>>                     "denominator": [2, 5, 10],})
>>> tf.udf(td.col("numerator", "denominator"), RatioUnpackedUDF()).collect()
>>> print(tf)
┌───────────┬──────────────┬──────┐
│ numerator ┆ denominator  ┆ ratio│
│ ---       ┆ ---          ┆ ---  │
│ i64       ┆ i64          ┆ f64  │
╞═══════════╪══════════════╪══════╡
│ 10        ┆ 2            ┆ 5.0  │
│ 20        ┆ 5            ┆ 4.0  │
│ 30        ┆ 10           ┆ 3.0  │
└───────────┴──────────────┴──────┘
```

<ParamField path="on">
  Expression selecting the input column(s) of the UDF.
</ParamField>

<ParamField path="function">
  Instance of tabsdata.tableframe.udf.function.UDF
defining on_batch or on_element to produce the output series.
</ParamField>

</details>

<details>
<summary>

**`unique(subset: td_typing.ColumnNameOrSelector | Collection[td_typing.ColumnNameOrSelector] | None = None, *, keep: td_typing.UniqueKeepStrategy = 'any', maintain_order: bool = False)`**

</summary>

Deduplicate rows from the TableFrame.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌──────┬──────┐
│ a    ┆ b    │
│ ---  ┆ ---  │
│ str  ┆ i64  │
╞══════╪══════╡
│ A    ┆ 1    │
│ X    ┆ 10   │
│ C    ┆ 3    │
│ D    ┆ 5    │
│ M    ┆ 9    │
│ A    ┆ 100  │
│ M    ┆ 50   │
│ null ┆ 20   │
│ F    ┆ null │
└──────┴──────┘
>>>
>>> tf.unique("a", keep="last")
>>>
┌──────┬──────┐
│ a    ┆ b    │
│ ---  ┆ ---  │
│ str  ┆ i64  │
╞══════╪══════╡
│ D    ┆ 5    │
│ C    ┆ 3    │
│ X    ┆ 10   │
│ A    ┆ 100  │
│ M    ┆ 50   │
│ F    ┆ null │
│ null ┆ 20   │
└──────┴──────┘
```

<ParamField path="subset">
  Columns to evaluate for duplicate values. If None, all columns are
considered.
</ParamField>

<ParamField path="keep">
  Strategy to keep duplicates: first, last, any, none (
eliminate duplicate rows).
</ParamField>

<ParamField path="maintain_order">
  Preserve the order of the rows.
</ParamField>

</details>

<details>
<summary>

**`unnest(columns: td_typing.ColumnNameOrSelector | Collection[td_typing.ColumnNameOrSelector], *more_columns: td_typing.ColumnNameOrSelector)`**

</summary>

Expand one or more struct columns so that each field within the struct becomes
a separate column in the TableFrame.

The resulting TableFrame will place these new columns in the same position
as the original struct column, effectively replacing it. This makes it easier
to work directly with the inner fields as standard columns.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf = td.TableFrame({
...     "id": [1, 2],
...     "info": [
...         {"name": "Alice", "age": 30},
...         {"name": "Bob", "age": None},
...     ],
...     "status": ["active", "inactive"],
... })
>>>
>>> tf
>>>
┌─────┬───────────────┬───────────┐
│ id  ┆ info          ┆ status    │
│ --- ┆ ---           ┆ ---       │
│ i64 ┆ struct[2]     ┆ str       │
╞═════╪═══════════════╪═══════════╡
│ 1   ┆ {"Alice",30}  ┆ active    │
│ 2   ┆ {"Bob",null}  ┆ inactive  │
└─────┴───────────────┴───────────┘
>>>
>>> tf.unnest("info")
>>>
┌─────┬───────┬──────┬───────────┐
│ id  ┆ name  ┆ age  ┆ status    │
│ --- ┆ ---   ┆ ---  ┆ ---       │
│ i64 ┆ str   ┆ i64  ┆ str       │
╞═════╪═══════╪══════╪═══════════╡
│ 1   ┆ Alice ┆ 30   ┆ active    │
│ 2   ┆ Bob   ┆ null ┆ inactive  │
└─────┴───────┴──────┴───────────┘
```

<ParamField path="columns">
  Name of the struct column(s) to expand.
</ParamField>

<ParamField path="more_columns">
  Additional struct columns to expand, provided as positional
arguments.
</ParamField>

</details>

<details>
<summary>`width`</summary>

</details>

<details>
<summary>

**`with_columns(*exprs: td_typing.IntoExpr | Iterable[td_typing.IntoExpr], **named_exprs: td_typing.IntoExpr)`**

</summary>

Add columns to the TableFrame.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌──────┬──────┐
│ x    ┆ y    │
│ ---  ┆ ---  │
│ f64  ┆ f64  │
╞══════╪══════╡
│ 1.0  ┆ 2.0  │
│ 2.0  ┆ 2.0  │
│ 10.0 ┆ 10.0 │
│ 4.0  ┆ 10.0 │
│ 5.0  ┆ null │
│ null ┆ null │
└──────┴──────┘
>>>
>>> tf.with_columns(td.col("x").mul(td.col("y")).alias("z"))
>>>
┌──────┬──────┬──────┐
│ x    ┆ y    ┆ z    │
│ ---  ┆ ---  ┆ ---  │
│ f64  ┆ f64  ┆ f64  │
╞══════╪══════╪══════╡
│ 1.0  ┆ 2.0  ┆ 2.0  │
│ 2.0  ┆ 2.0  ┆ 4.0  │
│ NaN  ┆ NaN  ┆ NaN  │
│ 4.0  ┆ NaN  ┆ NaN  │
│ 5.0  ┆ null ┆ null │
│ null ┆ null ┆ null │
└──────┴──────┴──────┘
```

<ParamField path="exprs">
  Columns or expressions to add.
</ParamField>

<ParamField path="named_exprs">
  Named expressions to add.
</ParamField>

</details>

</details>

 <details>
<summary>

**`assert_has_cols(cols: str | list[str], exact: bool | None = False)`**

</summary>

Ensures that the (non-system) columns in the TableFrame match the expected
columns.

Raises an exception if the expectation is not met.

If exact is True, the check verifies that the TableFrame contains exactly the
expected columns, with no extra or missing ones.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>> tf.assert_has_cols("a")
>>> tf.assert_has_cols(["a", "b"], exact=True)
```

<ParamField path="cols">
  The expected column name(s). Can be a string or a list of strings.
</ParamField>

<ParamField path="exact">
  If True, checks that the TableFrame contains exactly the specified
columns.
</ParamField>

**Raises:** ValueError – If expected columns are missing or unexpected columns are
    present in the TableFrame.

</details>

 <details>
<summary>

**`cast(dtypes: Mapping[td_typing.ColumnNameOrSelector | td_typing.DataType, td_typing.DataType] | td_typing.DataType, *, strict: bool = True)`**

</summary>

Cast columns to a new data type.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌──────┬──────┐
│ a    ┆ b    │
│ ---  ┆ ---  │
│ str  ┆ i64  │
╞══════╪══════╡
│ A    ┆ 1    │
│ X    ┆ 10   │
│ C    ┆ 3    │
│ D    ┆ 5    │
│ M    ┆ 9    │
│ A    ┆ 100  │
│ M    ┆ 50   │
│ null ┆ 20   │
│ F    ┆ null │
└──────┴──────┘
>>>
>>> tf.cast({"b":td.Float32}).collect()
>>>
┌──────┬───────┐
│ a    ┆ b     │
│ ---  ┆ ---   │
│ str  ┆ f32   │
╞══════╪═══════╡
│ A    ┆ 1.0   │
│ X    ┆ 10.0  │
│ C    ┆ 3.0   │
│ D    ┆ 5.0   │
│ M    ┆ 9.0   │
│ A    ┆ 100.0 │
│ M    ┆ 50.0  │
│ null ┆ 20.0  │
│ F    ┆ null  │
└──────┴───────┘
```

<ParamField path="dtypes">
  Mapping of the column name(s) to the new data type(s).
</ParamField>

<ParamField path="strict">
  If True, raises an error if the cast cannot be performed.
</ParamField>

</details>

 <details>
<summary>

**`clear(n: int = 0)`**

</summary>

Clears all rows of the TableFrame preserving the schema.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌──────┬──────┐
│ a    ┆ b    │
│ ---  ┆ ---  │
│ str  ┆ i64  │
╞══════╪══════╡
│ A    ┆ 1    │
│ X    ┆ 10   │
│ C    ┆ 3    │
│ D    ┆ 5    │
│ M    ┆ 9    │
│ A    ┆ 100  │
│ M    ┆ 50   │
│ null ┆ 20   │
│ F    ┆ null │
└──────┴──────┘
>>>
>>> tf.cast({"b":td.Float32}).collect()
>>>
┌──────┬───────┐
│ a    ┆ b     │
│ ---  ┆ ---   │
│ str  ┆ f32   │
╞══════╪═══════╡
└──────┴───────┘
```

</details>

 <details>
<summary>

**`columns(kind: Literal['all', 'user', 'system'] | None = 'user')`**

</summary>

</details>

 <details>
<summary>

**`drop(*columns: td_typing.ColumnNameOrSelector | Iterable[td_typing.ColumnNameOrSelector], strict: bool = True)`**

</summary>

Discard columns from the TableFrame.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌──────┬──────┐
│ x    ┆ y    │
│ ---  ┆ ---  │
│ f64  ┆ f64  │
╞══════╪══════╡
│ 1.0  ┆ 2.0  │
│ 2.0  ┆ 2.0  │
│ 10.0 ┆ 10.0 │
│ 4.0  ┆ 10.0 │
│ 5.0  ┆ null │
│ null ┆ null │
└──────┴──────┘
>>>
>>> tf.drop("y")
>>>
┌──────┐
│ x    │
│ ---  │
│ f64  │
╞══════╡
│ 1.0  │
│ 2.0  │
│ NaN  │
│ 4.0  │
│ 5.0  │
│ null │
└──────┘
```

<ParamField path="columns">
  Columns to drop.
</ParamField>

<ParamField path="strict">
  If True, raises an error if a column does not exist.
</ParamField>

</details>

 <details>
<summary>

**`drop_nans(subset: td_typing.ColumnNameOrSelector | Collection[td_typing.ColumnNameOrSelector] | None = None)`**

</summary>

Drop rows with NaN values.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌──────┬──────┬──────┐
│ ss   ┆ u    ┆ ff   │
│ ---  ┆ ---  ┆ ---  │
│ str  ┆ i64  ┆ f64  │
╞══════╪══════╪══════╡
│ A    ┆ 1    ┆ 1.1  │
│ B    ┆ 0    ┆ 0.0  │
│ A    ┆ 2    ┆ 2.2  │
│ B    ┆ 3    ┆ 3.3  │
│ B    ┆ 4    ┆ 4.4  │
│ C    ┆ 5    ┆ -1.1 │
│ C    ┆ 6    ┆ -2.2 │
│ C    ┆ 7    ┆ -3.3 │
│ D    ┆ 8    ┆ inf  │
│ F    ┆ 9    ┆ NaN  │
│ null ┆ null ┆ null │
└──────┴──────┴──────┘
>>>
>>> tf.unique("a", keep="last")
┌─────┬─────┬──────┐
│ ss  ┆ u   ┆ ff   │
│ --- ┆ --- ┆ ---  │
│ str ┆ i64 ┆ f64  │
╞═════╪═════╪══════╡
│ A   ┆ 1   ┆ 1.1  │
│ B   ┆ 0   ┆ 0.0  │
│ A   ┆ 2   ┆ 2.2  │
│ B   ┆ 3   ┆ 3.3  │
│ B   ┆ 4   ┆ 4.4  │
│ C   ┆ 5   ┆ -1.1 │
│ C   ┆ 6   ┆ -2.2 │
│ C   ┆ 7   ┆ -3.3 │
│ D   ┆ 8   ┆ inf  │
└─────┴─────┴──────┘
>>>
```

</details>

 <details>
<summary>

**`drop_nulls(subset: td_typing.ColumnNameOrSelector | Collection[td_typing.ColumnNameOrSelector] | None = None)`**

</summary>

Drop rows with null values.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌──────┬──────┬──────┐
│ ss   ┆ u    ┆ ff   │
│ ---  ┆ ---  ┆ ---  │
│ str  ┆ i64  ┆ f64  │
╞══════╪══════╪══════╡
│ A    ┆ 1    ┆ 1.1  │
│ B    ┆ 0    ┆ 0.0  │
│ A    ┆ 2    ┆ 2.2  │
│ B    ┆ 3    ┆ 3.3  │
│ B    ┆ 4    ┆ 4.4  │
│ C    ┆ 5    ┆ -1.1 │
│ C    ┆ 6    ┆ -2.2 │
│ C    ┆ 7    ┆ -3.3 │
│ D    ┆ 8    ┆ inf  │
│ F    ┆ 9    ┆ NaN  │
│ G    ┆ null ┆ 2.3  │
└──────┴──────┴──────┘
>>>
>>> tf.drop_nulls("a")
>>>
┌─────┬─────┬──────┐
│ ss  ┆ u   ┆ ff   │
│ --- ┆ --- ┆ ---  │
│ str ┆ i64 ┆ f64  │
╞═════╪═════╪══════╡
│ A   ┆ 1   ┆ 1.1  │
│ B   ┆ 0   ┆ 0.0  │
│ A   ┆ 2   ┆ 2.2  │
│ B   ┆ 3   ┆ 3.3  │
│ B   ┆ 4   ┆ 4.4  │
│ C   ┆ 5   ┆ -1.1 │
│ C   ┆ 6   ┆ -2.2 │
│ C   ┆ 7   ┆ -3.3 │
│ D   ┆ 8   ┆ inf  │
│ F   ┆ 9   ┆ NaN  │
└─────┴─────┴──────┘
```

</details>

 <details>
<summary>

**`empty(schema: SimpleSchema = None)`**

</summary>

Creates an empty (no column - no row) TableFrame.

</details>

 <details>
<summary>

**`extract_as_columns(offset: int, length: int)`**

</summary>

Extract a slice of rows from the table as a column-oriented dictionary.

The result is a mapping of column names to lists of values from the selected
rows.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌─────┬─────┐
│ a   ┆ b   │
│ --- ┆ --- │
│ str ┆ i64 │
╞═════╪═════╡
│ A   ┆ 1   │
│ X   ┆ 10  │
│ C   ┆ 3   │
│ D   ┆ 5   │
│ M   ┆ 9   │
└─────┴─────┘
>>>
>>> tf.extract_as_columns(offset=0, length=2)
    {
        "a": ["A", 1],
        "b": ["X", 10]
    }
```

<ParamField path="offset" type="int">
  The starting row index of the slice.
</ParamField>

<ParamField path="length" type="int">
  The number of rows to include in the slice.
</ParamField>

**Returns:** A dictionary where each key is a column name,
and its value is a list of values from the selected slice.

**Return type:** dict[str, list[Any]]

</details>

 <details>
<summary>

**`extract_as_rows(offset: int, length: int)`**

</summary>

Extract a slice of rows from the TableFrame as a list of dictionaries.

Each dictionary represents one row, where keys are column names
and values are the corresponding cell values.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌─────┬─────┐
│ a   ┆ b   │
│ --- ┆ --- │
│ str ┆ i64 │
╞═════╪═════╡
│ A   ┆ 1   │
│ X   ┆ 10  │
│ C   ┆ 3   │
│ D   ┆ 5   │
│ M   ┆ 9   │
└─────┴─────┘
>>>
>>> tf.extract_as_rows(offset=0, length=2)
[
>>>
>>> tf.extract_as_rows(offset=0, length=2)
    [
        {"a": "A", "b": 1},
        {"a": "X", "b": 10},
    ]
```

<ParamField path="offset" type="int">
  The starting row index of the slice.
</ParamField>

<ParamField path="length" type="int">
  The number of rows to include in the slice.
</ParamField>

**Returns:** A list of row dictionaries.

**Return type:** list[dict[str, Any]]

</details>

 <details>
<summary>

**`fill_nan(value: int | float | Expr | None)`**

</summary>

Replace all NaN values in the TableFrame with the given value.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌──────┬──────┐
│ x    ┆ y    │
│ ---  ┆ ---  │
│ f64  ┆ f64  │
╞══════╪══════╡
│ 1.0  ┆ 2.0  │
│ 2.0  ┆ 2.0  │
│ NaN  ┆ NaN  │
│ 4.0  ┆ NaN  │
│ 5.0  ┆ null │
│ null ┆ null │
└──────┴──────┘
>>>
>>> tf.fill_nan(10)
>>>
┌──────┬──────┐
│ x    ┆ y    │
│ ---  ┆ ---  │
│ f64  ┆ f64  │
╞══════╪══════╡
│ 1.0  ┆ 2.0  │
│ 2.0  ┆ 2.0  │
│ 10.0 ┆ 10.0 │
│ 4.0  ┆ 10.0 │
│ 5.0  ┆ null │
│ null ┆ null │
└──────┴──────┘
```

</details>

 <details>
<summary>

**`fill_null(value: Any | Expr | None = None)`**

</summary>

Replace all null values in the TableFrame with the given value.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌──────┬──────┐
│ x    ┆ y    │
│ ---  ┆ ---  │
│ f64  ┆ f64  │
╞══════╪══════╡
│ 1.0  ┆ 2.0  │
│ 2.0  ┆ 2.0  │
│ NaN  ┆ NaN  │
│ 4.0  ┆ NaN  │
│ 5.0  ┆ null │
│ null ┆ null │
└──────┴──────┘
>>>
>>> tf.fill_null(20)
>>>
┌──────┬──────┐
│ x    ┆ y    │
│ ---  ┆ ---  │
│ f64  ┆ f64  │
╞══════╪══════╡
│ 1.0  ┆ 2.0  │
│ 2.0  ┆ 2.0  │
│ NaN  ┆ NaN  │
│ 4.0  ┆ NaN  │
│ 5.0  ┆ 20.0 │
│ 20.0 ┆ 20.0 │
└──────┴──────┘
```

</details>

 <details>
<summary>

**`filter(*predicates: td_typing.IntoExprColumn | Iterable[td_typing.IntoExprColumn] | bool | list[bool] | np.ndarray[Any, Any])`**

</summary>

Filter the TableFrame based on the given predicates.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌─────┬─────┐
│ a   ┆ b   │
│ --- ┆ --- │
│ str ┆ i64 │
╞═════╪═════╡
│ A   ┆ 1   │
│ X   ┆ 10  │
│ C   ┆ 3   │
│ D   ┆ 5   │
│ M   ┆ 9   │
│ A   ┆ 100 │
│ M   ┆ 50  │
└─────┴─────┘
>>>
>>> tf.filter(td.col("a").is_in(["A", "C"]).or_(td.col("b").eq(10)))
>>>
┌─────┬─────┐
│ a   ┆ b   │
│ --- ┆ --- │
│ str ┆ i64 │
╞═════╪═════╡
│ A   ┆ 1   │
│ X   ┆ 10  │
│ C   ┆ 3   │
│ A   ┆ 100 │
└─────┴─────┘
```

</details>

 <details>
<summary>

**`first`**

</summary>

Return a TableFrame with the first row.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌─────┬─────┐
│ a   ┆ b   │
│ --- ┆ --- │
│ str ┆ i64 │
╞═════╪═════╡
│ A   ┆ 1   │
│ X   ┆ 10  │
│ C   ┆ 3   │
│ D   ┆ 5   │
│ M   ┆ 9   │
└─────┴─────┘
>>>
>>> tf.first()
>>>
┌─────┬─────┐
│ a   ┆ b   │
│ --- ┆ --- │
│ str ┆ i64 │
╞═════╪═════╡
│ A   ┆ 1   │
└─────┴─────┘
```

</details>

 <details>
<summary>

**`first_row(named: bool = False)`**

</summary>

Return a tuple or dictionary with the first row, or None if no row.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌─────┬─────┐
│ A   ┆ B   │
│ --- ┆ --- │
│ str ┆ i64 │
╞═════╪═════╡
│ a   ┆ 1   │
│ b   ┆ 2   │
│ c   ┆ 3   │
└─────┴─────┘
>>>
>>> tf.last_row()
>>>
('a', 1)
>>>
>>> tf.last_row(named=True)
>>>
{'A': 'a', 'B': '1'}
```

</details>

 <details>
<summary>

**`from_dict(data: Mapping[str, Sequence[object] | Mapping[str, Sequence[object]]] | None = None)`**

</summary>

Creates tableframe from a dictionary, or None.
None produces as an empty (no column - no row) tableframe.

</details>

 <details>
<summary>

**`from_pandas(data: pd.DataFrame | None = None)`**

</summary>

Creates tableframe from a pandas dataframe, or None.
None produces as an empty (no column - no row) tableframe.

</details>

 <details>
<summary>

**`from_polars(data: LazyFrame | DataFrame | None = None)`**

</summary>

Creates tableframe from a polars dataframe or lazyframe, or None.
None produces as an empty (no column - no row) tableframe.

</details>

 <details>
<summary>

**`grok(expr: td_typing.IntoExpr, pattern: str, schema: dict[str, td_col.Column])`**

</summary>

Parse log text into structured fields using a Grok pattern.

Applies a Grok pattern to the given column or expression and directly appends
**one new column per named capture** in the pattern to the output TableFrame.
Rows that do not match the pattern will contain null values for the
extracted columns.

Example

```text
>>> import tabsdata as td
>>> tf = td.TableFrame({"logs": [
...     "alice-login-2023",
...     "bob-logout-2024",
... ]})
>>>
>>> # Capture 3 fields: user, action, year
>>> log_pattern = r"%{WORD:user}-%{WORD:action}-%{INT:year}"
>>> log_schema = {
>>>     "word": td_col.Column("user", td.String),
>>>     "action": td_col.Column("action", td.String),
>>>     "year": td_col.Column("year", td.Int8),
>>> }
>>> out = tf.grok("logs", log_pattern, log_schema)
>>> out.collect()
┌──────────────────┬───────┬────────┬──────┐
│ logs             ┆ user  ┆ action ┆ year │
│ ---              ┆ ---   ┆ ---    ┆ ---  │
│ str              ┆ str   ┆ str    ┆ i64  │
╞══════════════════╪═══════╪════════╪══════╡
│ alice-login-2023 ┆ alice ┆ login  ┆ 2023 │
│ bob-logout-2024  ┆ bob   ┆ logout ┆ 2024 │
└──────────────────┴───────┴────────┴──────┘
```

Notes

- The function automatically expands the Grok captures into separate
columns.
- Non-matching rows will show null for the extracted columns.
- If a pattern defines duplicate capture names, numeric suffixes like
field, field[1] will be used to disambiguate them.

<ParamField path="expr" type="IntoExpr">
  Column name or expression that resolves to a single
string column containing log lines.
</ParamField>

<ParamField path="pattern" type="str">
  Grok pattern with named captures (e.g., %&#123;WORD:user&#125;).
</ParamField>

<ParamField path="schema" type="dict">
  A mapping where each capture name
is associated with its corresponding column definition, specifying
both the column name and its data type.
</ParamField>

**Returns:** A new TableFrame with one column per Grok capture added.

**Return type:** TableFrame

</details>

 <details>
<summary>

**`group_by(*by: td_typing.IntoExpr | Iterable[td_typing.IntoExpr])`**

</summary>

Perform a group by on the TableFrame.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌─────┬─────┐
│ a   ┆ b   │
│ --- ┆ --- │
│ str ┆ i64 │
╞═════╪═════╡
│ A   ┆ 1   │
│ X   ┆ 10  │
│ C   ┆ 3   │
│ D   ┆ 5   │
│ M   ┆ 9   │
│ A   ┆ 100 │
│ M   ┆ 50  │
└─────┴─────┘
>>>
>>> tf.group_by(td.col("a")).agg(td.col("b").sum())
>>>
┌─────┬─────┐
│ a   ┆ b   │
│ --- ┆ --- │
│ str ┆ i64 │
╞═════╪═════╡
│ M   ┆ 59  │
│ A   ┆ 101 │
│ C   ┆ 3   │
│ D   ┆ 5   │
│ X   ┆ 10  │
└─────┴─────┘
```

</details>

 <details>
<summary>

**`has_cols(cols: str | list[str], exact: bool | None = False)`**

</summary>

Verifies the presence of (non-system) columns in the TableFrame.

If exact is True, the check ensures that the TableFrame contains exactly the
specified columns (excluding system columns), with no extras or omissions.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌──────┬──────┐
│ a    ┆ b    │
│ ---  ┆ ---  │
│ str  ┆ i64  │
╞══════╪══════╡
│ A    ┆ 1    │
└──────┴──────┘
>>>
>>> tf.has_cols("a")
>>>
(True, {}, {"b"})
>>>
>>> tf.has_cols(["a", "c", "d"])
>>>
(False, {"c", "d"}, {"b"})
>>>
>>> tf.has_cols("a", exact=True)
>>>
(False, {}, {"b"})
>>>
>>> tf.has_cols(["a", "b"], exact=True)
>>>
(True, {}, {})
```

<ParamField path="cols">
  The column name(s) to verify. Can be a string or a list of strings.
</ParamField>

<ParamField path="exact">
  If True, checks that the TableFrame contains exactly the specified
columns.
</ParamField>

**Returns:** A boolean indicating whether the check was successful.
A set of columns in cols missing in the TableFrame.
A set of columns in the TableFrame missing in cols.

**Return type:** tuple[bool, set[str], set[str]]

</details>

 <details>
<summary>

**`has_same_schema(tf: TableFrame)`**

</summary>

Verifies if the schema of the current TableFrame is same than the provided
TableFrame.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf1: td.TableFrame ...
>>>
┌──────┬──────┐
│ a    ┆ b    │
│ ---  ┆ ---  │
│ str  ┆ i64  │
╞══════╪══════╡
│ A    ┆ 1    │
└──────┴──────┘
>>>
>>> tf2: td.TableFrame ...
>>>
┌──────┬──────┐
│ a    ┆ c    │
│ ---  ┆ ---  │
│ str  ┆ i64  │
╞══════╪══════╡
│ A    ┆ 1    │
└──────┴──────┘
>>> tf1.has_same_schema(tf2)
>>>
False
>>>
>>> tf1: td.TableFrame ...
>>>
┌──────┬──────┐
│ a    ┆ b    │
│ ---  ┆ ---  │
│ str  ┆ i64  │
╞══════╪══════╡
│ A    ┆ 1    │
└──────┴──────┘
>>>
>>> tf2: td.TableFrame ...
>>>
┌──────┬──────┐
│ a    ┆ b    │
│ ---  ┆ ---  │
│ str  ┆ str  │
╞══════╪══════╡
│ A    ┆ 1    │
└──────┴──────┘
>>> tf1.has_same_schema(tf2)
>>>
False
```

**Returns:** Whether the condition is met or not.

**Return type:** bool

</details>

 <details>
<summary>

**`head(n: int = 5)`**

</summary>

Return a TableFrame with the first n rows.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌─────┬─────┐
│ a   ┆ b   │
│ --- ┆ --- │
│ str ┆ i64 │
╞═════╪═════╡
│ A   ┆ 1   │
│ X   ┆ 10  │
│ C   ┆ 3   │
│ D   ┆ 5   │
│ M   ┆ 9   │
└─────┴─────┘
>>>
>>> tf.head(2)
>>>
┌─────┬─────┐
│ a   ┆ b   │
│ --- ┆ --- │
│ str ┆ i64 │
╞═════╪═════╡
│ A   ┆ 1   │
│ X   ┆ 10  │
└─────┴─────┘
```

</details>

 <details>
<summary>

**`is_empty`**

</summary>

Checks if a TableFrame has no rows.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌──────┬──────┐
│ a    ┆ b    │
│ ---  ┆ ---  │
│ str  ┆ i64  │
╞══════╪══════╡
│ A    ┆ 1    │
└──────┴──────┘
>>>
>>> tf.is_empty()
>>>
False
>>>
>>> tf: td.TableFrame ...
>>>
┌──────┬──────┐
│ a    ┆ b    │
│ ---  ┆ ---  │
│ str  ┆ i64  │
╞══════╪══════╡
└──────┴──────┘
>>>
>>> tf.is_empty()
>>>
True
```

</details>

 <details>
<summary>

**`item`**

</summary>

Returns a scalar value if the TableFrame contains exactly one user column and
one row.

Raises an exception if there is more than one user column or more than one row.

Returns None if the TableFrame is empty.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌─────┐
│ a   │
│ --- │
│ str │
╞═════╡
│ A   │
└─────┘
>>>
>>> tf.python_version()
>>>
A
```

</details>

 <details>
<summary>

**`join(other: TableFrame, on: str | Expr | Sequence[str | Expr] | None = None, how: Literal['inner', 'left', 'right', 'full', 'semi', 'anti', 'cross', 'outer'] = 'inner', *, left_on: str | Expr | Sequence[str | Expr] | None = None, right_on: str | Expr | Sequence[str | Expr] | None = None, suffix: str = '_right', join_nulls: bool = False, coalesce: bool | None = None)`**

</summary>

Join the TableFrame with another TableFrame.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf1: td.TableFrame ...
>>>
┌──────┬──────┐
│ a    ┆ b    │
│ ---  ┆ ---  │
│ str  ┆ i64  │
╞══════╪══════╡
│ A    ┆ 1    │
│ X    ┆ 10   │
│ C    ┆ 3    │
│ D    ┆ 5    │
│ M    ┆ 9    │
│ A    ┆ 100  │
│ M    ┆ 50   │
│ null ┆ 20   │
│ F    ┆ null │
└──────┴──────┘
>>>
>>> tf2: td.TableFrame ...
>>>
┌──────┬──────┐
│ a    ┆ b    │
│ ---  ┆ ---  │
│ str  ┆ i64  │
╞══════╪══════╡
│ A    ┆ 3    │
│ Y    ┆ 4    │
│ Z    ┆ 5    │
│ A    ┆ 0    │
│ M    ┆ 6    │
│ null ┆ 8    │
│ F    ┆ null │
└──────┴──────┘
>>>
An inner join:
>>>
>>> tf1.join(tf2, on="a", how="inner")
>>>
┌─────┬──────┬─────────┐
│ a   ┆ b    ┆ b_right │
│ --- ┆ ---  ┆ ---     │
│ str ┆ i64  ┆ i64     │
╞═════╪══════╪═════════╡
│ A   ┆ 1    ┆ 3       │
│ A   ┆ 1    ┆ 0       │
│ M   ┆ 9    ┆ 6       │
│ A   ┆ 100  ┆ 3       │
│ A   ┆ 100  ┆ 0       │
│ M   ┆ 50   ┆ 6       │
│ F   ┆ null ┆ null    │
└─────┴──────┴─────────┘
>>>
A left join:
>>>
>>> tf1.join(tf2, on="a", how="left")
>>>
┌──────┬──────┬─────────┐
│ a    ┆ b    ┆ b_right │
│ ---  ┆ ---  ┆ ---     │
│ str  ┆ i64  ┆ i64     │
╞══════╪══════╪═════════╡
│ A    ┆ 1    ┆ 3       │
│ A    ┆ 1    ┆ 0       │
│ X    ┆ 10   ┆ null    │
│ C    ┆ 3    ┆ null    │
│ D    ┆ 5    ┆ null    │
│ …    ┆ …    ┆ …       │
│ A    ┆ 100  ┆ 3       │
│ A    ┆ 100  ┆ 0       │
│ M    ┆ 50   ┆ 6       │
│ null ┆ 20   ┆ null    │
│ F    ┆ null ┆ null    │
└──────┴──────┴─────────┘
>>>
Turning off column coalesce:
>>>
>>> tf1.join(tf2, on="a", coalesce=False)
>>>
┌─────┬──────┬─────────┬─────────┐
│ a   ┆ b    ┆ a_right ┆ b_right │
│ --- ┆ ---  ┆ ---     ┆ ---     │
│ str ┆ i64  ┆ str     ┆ i64     │
╞═════╪══════╪═════════╪═════════╡
│ A   ┆ 1    ┆ A       ┆ 3       │
│ A   ┆ 1    ┆ A       ┆ 0       │
│ M   ┆ 9    ┆ M       ┆ 6       │
│ A   ┆ 100  ┆ A       ┆ 3       │
│ A   ┆ 100  ┆ A       ┆ 0       │
│ M   ┆ 50   ┆ M       ┆ 6       │
│ F   ┆ null ┆ F       ┆ null    │
└─────┴──────┴─────────┴─────────┘
```

<ParamField path="other">
  The TableFrame to join.
</ParamField>

<ParamField path="on">
  Name(s) of the columns to join on. The column name(s) must be in
both TableFrame’s. Don’t use this parameter if using `left_on
and right_on parameters, or if how=”cross”.
</ParamField>

<ParamField path="how">
  Join strategy:

inner: An inner join.
left: A left join.
right: A rigth join.
full: A full join.
cross: The cartesian product.
semi: An inner join but only returning the columns from left
TableFrame.
anti: Rows from the left TableFrame that have no match
in the right TableFrame.
</ParamField>

<ParamField path="">
  
</ParamField>

<ParamField path="">
  
</ParamField>

<ParamField path="">
  
</ParamField>

<ParamField path="">
  
</ParamField>

<ParamField path="">
  
</ParamField>

<ParamField path="">
  
</ParamField>

<ParamField path="">
  
</ParamField>

<ParamField path="left_on">
  Name(s) of the columns to join on from the left TableFrame.
It must be used together wit the right_on parameter.
It cannot be used with the on parameter.
</ParamField>

<ParamField path="right_on">
  Name(s) of the columns to join on from the right TableFrame.
It must be used together wit the left_on parameter.
It cannot be used with the on parameter.
</ParamField>

<ParamField path="suffix">
  Duplicate columns on the right Table are appended this suffix.
</ParamField>

<ParamField path="join_nulls">
  If null value matches should produce join rows or not.
</ParamField>

<ParamField path="coalesce">
  Collapse join columns into a single column.
</ParamField>

</details>

 <details>
<summary>

**`last`**

</summary>

Return a TableFrame with the last row.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌─────┬─────┐
│ a   ┆ b   │
│ --- ┆ --- │
│ str ┆ i64 │
╞═════╪═════╡
│ A   ┆ 1   │
│ X   ┆ 10  │
│ C   ┆ 3   │
│ D   ┆ 5   │
│ M   ┆ 9   │
└─────┴─────┘
>>>
>>> tf.last()
>>>
┌─────┬─────┐
│ a   ┆ b   │
│ --- ┆ --- │
│ str ┆ i64 │
╞═════╪═════╡
│ M   ┆ 9   │
└─────┴─────┘
```

</details>

 <details>
<summary>

**`last_row(named: bool = False)`**

</summary>

Return a tuple or dictionary with the last row, or None if no row.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌─────┬─────┐
│ A   ┆ B   │
│ --- ┆ --- │
│ str ┆ i64 │
╞═════╪═════╡
│ a   ┆ 1   │
│ b   ┆ 2   │
│ c   ┆ 3   │
└─────┴─────┘
>>>
>>> tf.last_row()
>>>
('c', 3)
>>>
>>> tf.last_row(named=True)
>>>
{'A': 'c', 'B': 3}
```

</details>

 <details>
<summary>

**`limit(n: int = 5)`**

</summary>

Return a TableFrame with the first n rows.
This is equivalent to head.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌─────┬─────┐
│ a   ┆ b   │
│ --- ┆ --- │
│ str ┆ i64 │
╞═════╪═════╡
│ A   ┆ 1   │
│ X   ┆ 10  │
│ C   ┆ 3   │
│ D   ┆ 5   │
│ M   ┆ 9   │
└─────┴─────┘
>>>
>>> tf.limit(2)
>>>
┌─────┬─────┐
│ a   ┆ b   │
│ --- ┆ --- │
│ str ┆ i64 │
╞═════╪═════╡
│ A   ┆ 1   │
│ X   ┆ 10  │
└─────┴─────┘
```

</details>

 <details>
<summary>

**`rename(mapping: dict[str, str])`**

</summary>

Rename columns from the TableFrame.

Examples:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌──────┬──────┐
│ i    ┆ f    │
│ ---  ┆ ---  │
│ i32  ┆ f64  │
╞══════╪══════╡
│ 1    ┆ 3.1  │
│ 2    ┆ 4.1  │
│ 3    ┆ 5.9  │
│ 4    ┆ 2.6  │
│ 5    ┆ 53.5 │
│ 6    ┆ 8.97 │
└──────┴──────┘
>>>
>>> tf.{"i": "index", "f": "amount"})
>>>
┌───────┬────────┐
│ index ┆ amount │
│ ----- ┆ ------ │
│ i32   ┆ f64    │
╞═══════╪════════╡
│ 1     ┆ 3.1    │
│ 2     ┆ 4.1    │
│ 3     ┆ 5.9    │
│ 4     ┆ 2.6    │
│ 5     ┆ 53.5   │
│ 6     ┆ 8.97   │
└───────┴────────┘
```

</details>

 <details>
<summary>

**`select(*exprs: td_typing.IntoExpr | Iterable[td_typing.IntoExpr], **named_exprs: td_typing.IntoExpr)`**

</summary>

Select column(s) from the TableFrame.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌─────┬─────┐
│ a   ┆ b   │
│ --- ┆ --- │
│ str ┆ i64 │
╞═════╪═════╡
│ A   ┆ 1   │
│ X   ┆ 10  │
│ C   ┆ 3   │
│ D   ┆ 5   │
│ M   ┆ 9   │
│ A   ┆ 100 │
│ M   ┆ 50  │
└─────┴─────┘
>>>
>>> tf.select(td.col("a"), td.col("b").mul(2).alias("bx2"),)
>>>
┌─────┬─────┐
│ a   ┆ bx2 │
│ --- ┆ --- │
│ str ┆ i64 │
╞═════╪═════╡
│ A   ┆ 2   │
│ X   ┆ 20  │
│ C   ┆ 6   │
│ D   ┆ 10  │
│ M   ┆ 18  │
│ A   ┆ 200 │
│ M   ┆ 100 │
└─────┴─────┘
```

<ParamField path="exprs">
  Columns or expressions to select.
</ParamField>

<ParamField path="named_exprs">
  Named expressions to select.
</ParamField>

</details>

 <details>
<summary>

**`slice(offset: int, length: int | None = None)`**

</summary>

Return a TableFrame with a slice of the original TableFrame

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌─────┬─────┐
│ a   ┆ b   │
│ --- ┆ --- │
│ str ┆ i64 │
╞═════╪═════╡
│ A   ┆ 1   │
│ X   ┆ 10  │
│ C   ┆ 3   │
│ D   ┆ 5   │
│ M   ┆ 9   │
└─────┴─────┘
>>>
>>> tf.slice(2,2)
>>>
┌─────┬─────┐
│ a   ┆ b   │
│ --- ┆ --- │
│ str ┆ i64 │
╞═════╪═════╡
│ C   ┆ 3   │
│ D   ┆ 5   │
└─────┴─────┘
```

<ParamField path="offset">
  Slice starting index.
</ParamField>

<ParamField path="length">
  The length of the slice. None means all the way to the end.
</ParamField>

</details>

 <details>
<summary>

**`sort(by: td_typing.IntoExpr | Iterable[td_typing.IntoExpr], *more_by: td_typing.IntoExpr, descending: bool | Sequence[bool] = False, nulls_last: bool | Sequence[bool] = False, maintain_order: bool = False)`**

</summary>

Sort the TableFrame by the given column(s) or expression(s).

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌──────┬──────┐
│ a    ┆ b    │
│ ---  ┆ ---  │
│ str  ┆ i64  │
╞══════╪══════╡
│ A    ┆ 1    │
│ X    ┆ 10   │
│ C    ┆ 3    │
│ D    ┆ 5    │
│ M    ┆ 9    │
│ A    ┆ 100  │
│ M    ┆ 50   │
│ null ┆ 20   │
│ F    ┆ null │
└──────┴──────┘
>>>
>>> tf.sort(td.col("a"), descending = True)
>>>
┌──────┬───────┐
│ a    ┆ b     │
│ ---  ┆ ---   │
│ str  ┆ f32   │
╞══════╪═══════╡
│ A    ┆ 1.0   │
│ X    ┆ 10.0  │
│ C    ┆ 3.0   │
│ D    ┆ 5.0   │
│ M    ┆ 9.0   │
│ A    ┆ 100.0 │
│ M    ┆ 50.0  │
│ null ┆ 20.0  │
│ F    ┆ null  │
└──────┴───────┘
```

<ParamField path="by">
  Column(s) or expression(s) to sort by.
</ParamField>

<ParamField path="more_by">
  Additional colums to sort by.
</ParamField>

<ParamField path="descending">
  Specifies if the sorting should be descending or not.
</ParamField>

<ParamField path="nulls_last">
  Specifies if null values should be placed last.
</ParamField>

<ParamField path="maintain_order">
  Preserve the order of equal rows.
</ParamField>

</details>

 <details>
<summary>

**`tail(n: int = 5)`**

</summary>

Return a TableFrame with the last n rows.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌─────┬─────┐
│ a   ┆ b   │
│ --- ┆ --- │
│ str ┆ i64 │
╞═════╪═════╡
│ A   ┆ 1   │
│ X   ┆ 10  │
│ C   ┆ 3   │
│ D   ┆ 5   │
│ M   ┆ 9   │
└─────┴─────┘
>>>
>>> tf.tail(2)
>>>
┌─────┬─────┐
│ a   ┆ b   │
│ --- ┆ --- │
│ str ┆ i64 │
╞═════╪═════╡
│ D   ┆ 5   │
│ M   ┆ 9   │
└─────┴─────┘
```

</details>

 <details>
<summary>

**`to_dict`**

</summary>

Creates a dictionary from this tableframe.

</details>

 <details>
<summary>

**`to_pandas`**

</summary>

Creates a pandas dataframe from this tableframe.

</details>

 <details>
<summary>

**`to_polars_df`**

</summary>

Creates a polars dataframe from this tableframe.

</details>

 <details>
<summary>

**`to_polars_lf`**

</summary>

Creates a polars lazyframe from this tableframe.

</details>

 <details>
<summary>

**`udf(on: td_typing.IntoExpr | list[td_typing.IntoExpr], function: td_udf.UDF)`**

</summary>

Apply a user-defined function (UDF) to the columns resolved by expr.

The selected columns are supplied to function, which can implement either
on_batch or on_element. An on_batch implementation receives a list of
Polars series representing the selected columns and must return a list of
Polars series with matching length. An on_element implementation receives
a list of Python scalars for each row and returns a list of scalars; the
framework wraps this in an efficient batch executor, so data still flows in
batches even when authoring row-wise logic. In both cases the returned series
become new columns appended to the original TableFrame.

By default, both methods receive their inputs as a list. Override the
signature property in your UDF class to return “unpacked” to have each column
passed as a separate argument instead.

 Creating UDFs:1. Subclass [tabsdata.tableframe.udf.function.UDF](bymod-tabsdata.tableframe.udf.function#tabsdata.tableframe.udf.function.UDF).
2. Implement `__init__` to call `super().__init__(output_columns)` where
`output_columns` is a tuple or list of tuples `(name, data type)`
specifying the UDF default output schema (column names and data types).
Each tuple must contain a column name (string) and a data type
(DataType).
3. Override exactly one of on_batch or on_element, to implement the UDF
function logic.
4. Return a list of TabsData Series (for on_batch) or TabsData supported
scalars (for on_element) with the same length as specified in the
output schema.
5. If overriding the on_batch method, the return type must be a list of
TabsData Series. If overriding the on_element method, the return type
must be a list of supported TabsData scalar values. For both cases, the
number of elements in the returned lists must match the number of
elements in the output_columns list provided to the UDF constructor.

  Using UDFs:

 1. Instantiate a function created as above.
2. Pass it to TableFrame method udf().
3. Optionally use `UDF.output_columns()` to override output column names
or data types after instantiation.

 Examples

```text
>>> import tabsdata as td
>>> import tabsdata.tableframe as tdf
>>>
>>> class SumUDF(tdf.UDF):
...     def __init__(self):
...         super().__init__(("total", tdf.Int64))
...
...     def on_batch(self, series):
...         return [series[0] + series[1]]
>>>
>>> tf = td.TableFrame({"a": [1, 2, 3], "b": [10, 20, 30]})
>>> print(tf)
┌─────┬─────┐
│ a   ┆ b   │
│ --- ┆ --- │
│ i64 ┆ i64 │
╞═════╪═════╡
│ 1   ┆ 10  │
│ 2   ┆ 20  │
│ 3   ┆ 30  │
└─────┴─────┘
>>> tf.udf(td.col("a", "b"), SumUDF())
>>> print(tf)
┌─────┬─────┬───────┐
│ a   ┆ b   ┆ total │
│ --- ┆ --- ┆ ---   │
│ i64 ┆ i64 ┆ i64   │
╞═════╪═════╪═══════╡
│ 1   ┆ 10  ┆ 11    │
│ 2   ┆ 20  ┆ 22    │
│ 3   ┆ 30  ┆ 33    │
└─────┴─────┴───────┘
```

```text
>>> class RatioUDF(tdf.UDF):
...     def __init__(self):
...         super().__init__(("ratio", tdf.Float64))
...
...     def on_element(self, values):
...         return [values[0] / values[1]]
>>>
>>> tf = td.TableFrame({"numerator": [10, 20, 30],
>>>                     "denominator": [2, 5, 10],})
>>> print(tf)
┌───────────┬──────────────┐
│ numerator ┆ denominator  │
│ ---       ┆ ---          │
│ i64       ┆ i64          │
╞═══════════╪══════════════╡
│ 10        ┆ 2            │
│ 20        ┆ 5            │
│ 30        ┆ 10           │
└───────────┴──────────────┘
>>> tf.udf(td.col("numerator", "denominator"), RatioUDF()).collect()
>>> print(tf)
┌───────────┬──────────────┬──────┐
│ numerator ┆ denominator  ┆ ratio│
│ ---       ┆ ---          ┆ ---  │
│ i64       ┆ i64          ┆ f64  │
╞═══════════╪══════════════╪══════╡
│ 10        ┆ 2            ┆ 5.0  │
│ 20        ┆ 5            ┆ 4.0  │
│ 30        ┆ 10           ┆ 3.0  │
└───────────┴──────────────┴──────┘
```

Using signature property to receive individual arguments:

```text
>>> class RatioUnpackedUDF(tdf.UDF):
...     def __init__(self):
...         super().__init__(("ratio", tdf.Float64))
...
...     @property
...     def signature(self):
...         return "unpacked"
...
...     def on_element(self, numerator, denominator):
...         return [numerator / denominator]
>>>
>>> tf = td.TableFrame({"numerator": [10, 20, 30],
>>>                     "denominator": [2, 5, 10],})
>>> tf.udf(td.col("numerator", "denominator"), RatioUnpackedUDF()).collect()
>>> print(tf)
┌───────────┬──────────────┬──────┐
│ numerator ┆ denominator  ┆ ratio│
│ ---       ┆ ---          ┆ ---  │
│ i64       ┆ i64          ┆ f64  │
╞═══════════╪══════════════╪══════╡
│ 10        ┆ 2            ┆ 5.0  │
│ 20        ┆ 5            ┆ 4.0  │
│ 30        ┆ 10           ┆ 3.0  │
└───────────┴──────────────┴──────┘
```

<ParamField path="on">
  Expression selecting the input column(s) of the UDF.
</ParamField>

<ParamField path="function">
  Instance of tabsdata.tableframe.udf.function.UDF
defining on_batch or on_element to produce the output series.
</ParamField>

</details>

 <details>
<summary>

**`unique(subset: td_typing.ColumnNameOrSelector | Collection[td_typing.ColumnNameOrSelector] | None = None, *, keep: td_typing.UniqueKeepStrategy = 'any', maintain_order: bool = False)`**

</summary>

Deduplicate rows from the TableFrame.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌──────┬──────┐
│ a    ┆ b    │
│ ---  ┆ ---  │
│ str  ┆ i64  │
╞══════╪══════╡
│ A    ┆ 1    │
│ X    ┆ 10   │
│ C    ┆ 3    │
│ D    ┆ 5    │
│ M    ┆ 9    │
│ A    ┆ 100  │
│ M    ┆ 50   │
│ null ┆ 20   │
│ F    ┆ null │
└──────┴──────┘
>>>
>>> tf.unique("a", keep="last")
>>>
┌──────┬──────┐
│ a    ┆ b    │
│ ---  ┆ ---  │
│ str  ┆ i64  │
╞══════╪══════╡
│ D    ┆ 5    │
│ C    ┆ 3    │
│ X    ┆ 10   │
│ A    ┆ 100  │
│ M    ┆ 50   │
│ F    ┆ null │
│ null ┆ 20   │
└──────┴──────┘
```

<ParamField path="subset">
  Columns to evaluate for duplicate values. If None, all columns are
considered.
</ParamField>

<ParamField path="keep">
  Strategy to keep duplicates: first, last, any, none (
eliminate duplicate rows).
</ParamField>

<ParamField path="maintain_order">
  Preserve the order of the rows.
</ParamField>

</details>

 <details>
<summary>

**`unnest(columns: td_typing.ColumnNameOrSelector | Collection[td_typing.ColumnNameOrSelector], *more_columns: td_typing.ColumnNameOrSelector)`**

</summary>

Expand one or more struct columns so that each field within the struct becomes
a separate column in the TableFrame.

The resulting TableFrame will place these new columns in the same position
as the original struct column, effectively replacing it. This makes it easier
to work directly with the inner fields as standard columns.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf = td.TableFrame({
...     "id": [1, 2],
...     "info": [
...         {"name": "Alice", "age": 30},
...         {"name": "Bob", "age": None},
...     ],
...     "status": ["active", "inactive"],
... })
>>>
>>> tf
>>>
┌─────┬───────────────┬───────────┐
│ id  ┆ info          ┆ status    │
│ --- ┆ ---           ┆ ---       │
│ i64 ┆ struct[2]     ┆ str       │
╞═════╪═══════════════╪═══════════╡
│ 1   ┆ {"Alice",30}  ┆ active    │
│ 2   ┆ {"Bob",null}  ┆ inactive  │
└─────┴───────────────┴───────────┘
>>>
>>> tf.unnest("info")
>>>
┌─────┬───────┬──────┬───────────┐
│ id  ┆ name  ┆ age  ┆ status    │
│ --- ┆ ---   ┆ ---  ┆ ---       │
│ i64 ┆ str   ┆ i64  ┆ str       │
╞═════╪═══════╪══════╪═══════════╡
│ 1   ┆ Alice ┆ 30   ┆ active    │
│ 2   ┆ Bob   ┆ null ┆ inactive  │
└─────┴───────┴──────┴───────────┘
```

<ParamField path="columns">
  Name of the struct column(s) to expand.
</ParamField>

<ParamField path="more_columns">
  Additional struct columns to expand, provided as positional
arguments.
</ParamField>

</details>

 <details>
<summary>

**`with_columns(*exprs: td_typing.IntoExpr | Iterable[td_typing.IntoExpr], **named_exprs: td_typing.IntoExpr)`**

</summary>

Add columns to the TableFrame.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
┌──────┬──────┐
│ x    ┆ y    │
│ ---  ┆ ---  │
│ f64  ┆ f64  │
╞══════╪══════╡
│ 1.0  ┆ 2.0  │
│ 2.0  ┆ 2.0  │
│ 10.0 ┆ 10.0 │
│ 4.0  ┆ 10.0 │
│ 5.0  ┆ null │
│ null ┆ null │
└──────┴──────┘
>>>
>>> tf.with_columns(td.col("x").mul(td.col("y")).alias("z"))
>>>
┌──────┬──────┬──────┐
│ x    ┆ y    ┆ z    │
│ ---  ┆ ---  ┆ ---  │
│ f64  ┆ f64  ┆ f64  │
╞══════╪══════╪══════╡
│ 1.0  ┆ 2.0  ┆ 2.0  │
│ 2.0  ┆ 2.0  ┆ 4.0  │
│ NaN  ┆ NaN  ┆ NaN  │
│ 4.0  ┆ NaN  ┆ NaN  │
│ 5.0  ┆ null ┆ null │
│ null ┆ null ┆ null │
└──────┴──────┴──────┘
```

<ParamField path="exprs">
  Columns or expressions to add.
</ParamField>

<ParamField path="named_exprs">
  Named expressions to add.
</ParamField>

</details>

 <details>
<summary>

**`TableInput(table: str | List[str])`**

</summary>

Bases: SourcePlugin

Table-based data inputs.

<details>
<summary>

**`stream(working_dir: str)`**

</summary>

</details>

<details>
<summary>`table`</summary>

The table(s) to load.

 **Type:** str | List[str]

</details>

</details>

 <details>
<summary>

**`stream(working_dir: str)`**

</summary>

</details>

 <details>
<summary>

**`TableOutput(table: str | list[str])`**

</summary>

Bases: DestinationPlugin

Table-based data outputs.

<details>
<summary>`table`</summary>

The table(s) to create. If multiple tables are provided,
they must be provided as a list.

 **Type:** str | list[str]

</details>

</details>

 <details>
<summary>

**`TabsdataFunction(func: Callable, name: str | None, input: SourcePlugin = None, output: DestinationPlugin = None, trigger_by: str | list[str] | CronTrigger | StageTrigger | None = None, output_actions: DataQuality | list[DataQuality] | None = None)`**

</summary>

Bases: [object](https://docs.python.org/3/library/functions.html#object)

 Class to decorate a function with metadata and methods for use in a Tabsdataenvironment.

  Attributes:

<details>
<summary>`input`</summary>

The data to be used when running the function.

 **Type:** SourcePlugin | None

</details>

<details>
<summary>`name`</summary>

The name with which the function will be registered.

 **Type:** str

</details>

<details>
<summary>`original_file`</summary>

The file where the original function is defined in the user’s computer

 **Type:** str

</details>

<details>
<summary>`original_folder`</summary>

The folder where the original function is defined, as a local path in the
user’s computer.

 **Type:** str

</details>

<details>
<summary>`original_function`</summary>

The original function that was decorated, without any behaviour
modifications.

 **Type:** Callable

</details>

<details>
<summary>`output`</summary>

The location where the function results will be saved when
run.

 **Type:** DestinationPlugin

</details>

<details>
<summary>`output_actions`</summary>

Actions to perform on the output tables.

 **Type:** list[OnTablesAction]

</details>

<details>
<summary>`trigger_by`</summary>

The trigger(s) that will cause the function to execute. It must be
another table or tables in the system, a CronTrigger, or a StageTrigger in
the case of a publisher.

 **Type:** List[str]

</details>

<details>
<summary>`type`</summary>

The type of the function. It can be “publisher”, “transformer” or
“subscriber”.

 **Type:** str

</details>

</details>

 <details>
<summary>

**`UserPasswordCredentials(user: str | Secret, password: str | Secret)`**

</summary>

Bases: `Credentials`

Represents credentials consisting of a user and a password.

This class is used to manage and securely handle user and password
credentials. It includes functionalities to set user and password
details, convert them into a dictionary representation, and provide
a string representation for debugging purposes.

<details>
<summary>`password`</summary>

The password.

 **Type:** Secret

</details>

<details>
<summary>`user`</summary>

The user.

 **Type:** Secret

</details>

</details>

 <details>
<summary>

**`concat(items: Iterable[TdType] | None, how: Literal['vertical', 'vertical_relaxed', 'diagonal', 'diagonal_relaxed'] = 'vertical')`**

</summary>

Combine multiple TableFrames by stacking their rows.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf1: td.TableFrame ...
>>>
┌──────┬──────┐
│ a    ┆ b    │
│ ---  ┆ ---  │
│ str  ┆ i64  │
╞══════╪══════╡
│ a    ┆ 1    │
│ b    ┆ 2    │
└──────┴──────┘
>>>
>>> tf2: td.TableFrame ...
>>>
┌──────┬──────┐
│ a    ┆ b    │
│ ---  ┆ ---  │
│ str  ┆ i64  │
╞══════╪══════╡
│ x    ┆ 10   │
│ y    ┆ 20   │
└──────┴──────┘
>>>
>>> tf = td.concat([tf1, tf2])
>>>
┌──────┬──────┐
│ a    ┆ b    │
│ ---  ┆ ---  │
│ str  ┆ i64  │
╞══════╪══════╡
│ a    ┆ 1    │
│ b    ┆ 2    │
│ x    ┆ 10   │
│ y    ┆ 20   │
└──────┴──────┘
```

<ParamField path="items">
  The TableFrames to concatenate. If None or empty, returns None.
None entries within the iterable are ignored. If all entries are None,
returns None.
</ParamField>

<ParamField path="how">
  &#123;‘vertical’, ‘vertical_relaxed’, ‘diagonal’, ‘diagonal_relaxed’&#125;

vertical: Appends the rows of each input below the previous one. All
inputs must have exactly the same column names and types; otherwise the
operation fails.
vertical_relaxed: Same as vertical, but if columns with the same name
have different data types across inputs, they are converted to a common
type (e.g. Int32 → Int64).
diagonal: Aligns columns by name across all inputs. If a column is missing
from a particular input, that input is padded with null values for the
missing column. Matching columns keep their original type if consistent.
diagonal_relaxed: Same as diagonal, but when matching columns have
different data types, they are converted to a common type
(e.g. Int32 → Int64).
</ParamField>

<ParamField path="">
  
</ParamField>

<ParamField path="">
  
</ParamField>

<ParamField path="">
  
</ParamField>

<ParamField path="">
  
</ParamField>

</details>

 <details>
<summary>

**`publisher(source: AzureSource | LocalFileSource | MariaDBSource | MySQLSource | OracleSource | PostgresSource | S3Source | SourcePlugin, tables: TableOutput | str | List[str], name: str = None, trigger_by: PublisherTriggerBySpec = None, on_tables: OnTablesSpec = None)`**

</summary>

Decorator to define a function to publish data to Tabsdata.

<ParamField path="source">
  Where to obtain the data that will be provided as an input to the
function.
</ParamField>

<ParamField path="tables">
  Where to store the output of the function.
</ParamField>

<ParamField path="name">
  The name with which the function will be registered.
If not provided, the current function name will be used.
</ParamField>

<ParamField path="trigger_by">
  The trigger that will cause
the function to execute. It can be a table in the system, a list of
tables, None (in which case it must be triggered manually), or a
CronTrigger.
</ParamField>

<ParamField path="on_tables" type="OnTablesSpec">
  Actions
to perform on output tables after executing the function. Can be a
single action or a list of them.
Defaults to None.
</ParamField>

**Returns:** The function converted to a Tabsdata Function.

**Return type:** callable

</details>

 <details>
<summary>

**`subscriber(tables: TableInput | str | List[str], destination: AzureDestination | LocalFileDestination | MariaDBDestination | MySQLDestination | OracleDestination | PostgresDestination | S3Destination | DestinationPlugin, name: str = None, trigger_by: SubscriberTriggerBySpec = '*')`**

</summary>

Decorator to define a function to subscribe to data present in Tabsdata.

<ParamField path="tables">
  Where to obtain the data that will be provided as an input to the
function.
</ParamField>

<ParamField path="destination">
  Where to store the output of the function.
</ParamField>

<ParamField path="name">
  The name with which the function will be registered.
If not provided, the current function name will be used.
</ParamField>

<ParamField path="trigger_by">
  The trigger that will cause
the function to execute. It can be a table in the system, a list of
tables, None (in which case it must be triggered manually),
or a CronTrigger. Defaults to all dependencies.
</ParamField>

**Returns:** The function converted to a Tabsdata Function.

**Return type:** callable

</details>

 <details>
<summary>

**`transformer(input_tables: TableInput | str | List[str], output_tables: TableOutput | str | List[str], name: str = None, trigger_by: TransformerTriggerBySpec = '*', on_output_tables: OnTablesSpec = None)`**

</summary>

Decorator to define a function to transform data present in Tabsdata.

<ParamField path="input_tables" type="TableInput">
  Where to obtain the data that
will be provided as an input to the function.
</ParamField>

<ParamField path="output_tables" type="TableOutput">
  Where to store the
output of the function.
</ParamField>

<ParamField path="name" type="str">
  The name with which the function will be registered.
If not provided, the current function name will be used.
</ParamField>

<ParamField path="trigger_by" type="TransformerTriggerBySpec">
  The trigger that will cause
the function to execute. It can be a table in the system, a list of
tables, None (in which case it must be triggered manually),
or a CronTrigger. Defaults to all dependencies.
</ParamField>

<ParamField path="on_output_tables" type="OnTablesSpec">
  Actions to perform on output tables after executing the function.
Can be a single action or a list of them.
Defaults to None.
</ParamField>

**Returns:** The function converted to a Tabsdata Function.

**Return type:** callable

</details>
