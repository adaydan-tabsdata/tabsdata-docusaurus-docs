---
title: "tabsdata.tableframe.expr.string"
---

import { ParamField, ResponseField } from '@site/src/components/ApiField';

export const toc = [
  {value: '<span class="toc-kind-badge toc-kind-class">class</span>ExprStringNameSpace', id: 'tabsdata.tableframe.expr.string.ExprStringNameSpace', level: 2},
  {value: '<span class="toc-kind-badge toc-kind-method">method</span>contains()', id: 'tabsdata.tableframe.expr.string.ExprStringNameSpace.contains', level: 3},
  {value: '<span class="toc-kind-badge toc-kind-method">method</span>contains_any()', id: 'tabsdata.tableframe.expr.string.ExprStringNameSpace.contains_any', level: 3},
  {value: '<span class="toc-kind-badge toc-kind-method">method</span>count_matches()', id: 'tabsdata.tableframe.expr.string.ExprStringNameSpace.count_matches', level: 3},
  {value: '<span class="toc-kind-badge toc-kind-method">method</span>ends_with()', id: 'tabsdata.tableframe.expr.string.ExprStringNameSpace.ends_with', level: 3},
  {value: '<span class="toc-kind-badge toc-kind-method">method</span>extract()', id: 'tabsdata.tableframe.expr.string.ExprStringNameSpace.extract', level: 3},
  {value: '<span class="toc-kind-badge toc-kind-method">method</span>find()', id: 'tabsdata.tableframe.expr.string.ExprStringNameSpace.find', level: 3},
  {value: '<span class="toc-kind-badge toc-kind-method">method</span>grok()', id: 'tabsdata.tableframe.expr.string.ExprStringNameSpace.grok', level: 3},
  {value: '<span class="toc-kind-badge toc-kind-method">method</span>head()', id: 'tabsdata.tableframe.expr.string.ExprStringNameSpace.head', level: 3},
  {value: '<span class="toc-kind-badge toc-kind-method">method</span>len_bytes()', id: 'tabsdata.tableframe.expr.string.ExprStringNameSpace.len_bytes', level: 3},
  {value: '<span class="toc-kind-badge toc-kind-method">method</span>len_chars()', id: 'tabsdata.tableframe.expr.string.ExprStringNameSpace.len_chars', level: 3},
  {value: '<span class="toc-kind-badge toc-kind-method">method</span>pad_end()', id: 'tabsdata.tableframe.expr.string.ExprStringNameSpace.pad_end', level: 3},
  {value: '<span class="toc-kind-badge toc-kind-method">method</span>pad_start()', id: 'tabsdata.tableframe.expr.string.ExprStringNameSpace.pad_start', level: 3},
  {value: '<span class="toc-kind-badge toc-kind-method">method</span>replace()', id: 'tabsdata.tableframe.expr.string.ExprStringNameSpace.replace', level: 3},
  {value: '<span class="toc-kind-badge toc-kind-method">method</span>replace_all()', id: 'tabsdata.tableframe.expr.string.ExprStringNameSpace.replace_all', level: 3},
  {value: '<span class="toc-kind-badge toc-kind-method">method</span>replace_many()', id: 'tabsdata.tableframe.expr.string.ExprStringNameSpace.replace_many', level: 3},
  {value: '<span class="toc-kind-badge toc-kind-method">method</span>reverse()', id: 'tabsdata.tableframe.expr.string.ExprStringNameSpace.reverse', level: 3},
  {value: '<span class="toc-kind-badge toc-kind-method">method</span>slice()', id: 'tabsdata.tableframe.expr.string.ExprStringNameSpace.slice', level: 3},
  {value: '<span class="toc-kind-badge toc-kind-method">method</span>starts_with()', id: 'tabsdata.tableframe.expr.string.ExprStringNameSpace.starts_with', level: 3},
  {value: '<span class="toc-kind-badge toc-kind-method">method</span>strip_chars()', id: 'tabsdata.tableframe.expr.string.ExprStringNameSpace.strip_chars', level: 3},
  {value: '<span class="toc-kind-badge toc-kind-method">method</span>strip_chars_end()', id: 'tabsdata.tableframe.expr.string.ExprStringNameSpace.strip_chars_end', level: 3},
  {value: '<span class="toc-kind-badge toc-kind-method">method</span>strip_chars_start()', id: 'tabsdata.tableframe.expr.string.ExprStringNameSpace.strip_chars_start', level: 3},
  {value: '<span class="toc-kind-badge toc-kind-method">method</span>strip_prefix()', id: 'tabsdata.tableframe.expr.string.ExprStringNameSpace.strip_prefix', level: 3},
  {value: '<span class="toc-kind-badge toc-kind-method">method</span>strip_suffix()', id: 'tabsdata.tableframe.expr.string.ExprStringNameSpace.strip_suffix', level: 3},
  {value: '<span class="toc-kind-badge toc-kind-method">method</span>tail()', id: 'tabsdata.tableframe.expr.string.ExprStringNameSpace.tail', level: 3},
  {value: '<span class="toc-kind-badge toc-kind-method">method</span>to_date()', id: 'tabsdata.tableframe.expr.string.ExprStringNameSpace.to_date', level: 3},
  {value: '<span class="toc-kind-badge toc-kind-method">method</span>to_datetime()', id: 'tabsdata.tableframe.expr.string.ExprStringNameSpace.to_datetime', level: 3},
  {value: '<span class="toc-kind-badge toc-kind-method">method</span>to_integer()', id: 'tabsdata.tableframe.expr.string.ExprStringNameSpace.to_integer', level: 3},
  {value: '<span class="toc-kind-badge toc-kind-method">method</span>to_lowercase()', id: 'tabsdata.tableframe.expr.string.ExprStringNameSpace.to_lowercase', level: 3},
  {value: '<span class="toc-kind-badge toc-kind-method">method</span>to_time()', id: 'tabsdata.tableframe.expr.string.ExprStringNameSpace.to_time', level: 3},
  {value: '<span class="toc-kind-badge toc-kind-method">method</span>to_titlecase()', id: 'tabsdata.tableframe.expr.string.ExprStringNameSpace.to_titlecase', level: 3},
  {value: '<span class="toc-kind-badge toc-kind-method">method</span>to_uppercase()', id: 'tabsdata.tableframe.expr.string.ExprStringNameSpace.to_uppercase', level: 3},
  {value: '<span class="toc-kind-badge toc-kind-method">method</span>zfill()', id: 'tabsdata.tableframe.expr.string.ExprStringNameSpace.zfill', level: 3},
  {value: '<span class="toc-kind-badge toc-kind-class">class</span>GrokParser', id: 'tabsdata.tableframe.expr.string.GrokParser', level: 2},
  {value: '<span class="toc-kind-badge toc-kind-attribute">attribute</span>index', id: 'tabsdata.tableframe.expr.string.GrokParser.index', level: 3},
  {value: '<span class="toc-kind-badge toc-kind-attribute">attribute</span>pattern', id: 'tabsdata.tableframe.expr.string.GrokParser.pattern', level: 3},
  {value: '<span class="toc-kind-badge toc-kind-attribute">attribute</span>schema', id: 'tabsdata.tableframe.expr.string.GrokParser.schema', level: 3},
  {value: '<span class="toc-kind-badge toc-kind-attribute">attribute</span>temp_column', id: 'tabsdata.tableframe.expr.string.GrokParser.temp_column', level: 3},
  {value: '<span class="toc-kind-badge toc-kind-method">method</span>rust()', id: 'tabsdata.tableframe.expr.string.GrokParser.rust', level: 3},
];

# tabsdata.tableframe.expr.string

 <a id="tabsdata.tableframe.expr.string.ExprStringNameSpace"></a>
<div class="api-entry-wrap">
<details open>
<summary>ExprStringNameSpace</summary>

```python
class ExprStringNameSpace(
    expr: ExprStringNameSpace,
)
```

Bases: [object](https://docs.python.org/3/library/functions.html#object)

<a id="tabsdata.tableframe.expr.string.ExprStringNameSpace.contains"></a>
<ResponseField name="contains" kind="method" source="https://github.com/tabsdata/tabsdata/blob/b1.9/client/platform/src/main/python/tabsdata/tableframe/expr/string.py#L575">

```python
def contains(
    pattern: str | Expr,
    *,
    literal: bool = False,
    strict: bool = True,
)
```

Evaluate if the string contains a pattern.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.contains("ab").alias("contains"))
>>>
┌──────┬──────────┐
│ a    ┆ contains │
│ ---  ┆ ---      │
│ str  ┆ bool     │
╞══════╪══════════╡
│ a    ┆ false    │
│ ab   ┆ true     │
│ b    ┆ false    │
│ xaby ┆ true     │
│ null ┆ null     │
└──────┴──────────┘
```

**Categories:** string

**Parameters:**

<ParamField path="pattern">
  The pattern to search for.
</ParamField>

<ParamField path="literal">
  Take the pattern as a literal string (not a regex).
</ParamField>

<ParamField path="strict">
  if the given pattern is not valid regex, raise an error.
</ParamField>

</ResponseField>

<a id="tabsdata.tableframe.expr.string.ExprStringNameSpace.contains_any"></a>
<ResponseField name="contains_any" kind="method" source="https://github.com/tabsdata/tabsdata/blob/b1.9/client/platform/src/main/python/tabsdata/tableframe/expr/string.py#L1090">

```python
def contains_any(
    patterns: td_typing.IntoExpr,
    *,
    ascii_case_insensitive: bool = False,
)
```

Evaluate if the string contains any of the given patterns.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a")
      .str.contains_any(["a", "b"]).alias("contains_any"))
>>>
┌──────┬──────────────┐
│ a    ┆ contains_any │
│ ---  ┆ ---          │
│ str  ┆ bool         │
╞══════╪══════════════╡
│ abc  ┆ true         │
│ axy  ┆ true         │
│ xyb  ┆ true         │
│ xyz  ┆ false        │
│ null ┆ null         │
└──────┴──────────────┘
```

**Categories:** string

**Parameters:**

<ParamField path="patterns">
  The patterns to search for.
</ParamField>

<ParamField path="ascii_case_insensitive">
  If true, the search is case-insensitive.
</ParamField>

</ResponseField>

<a id="tabsdata.tableframe.expr.string.ExprStringNameSpace.count_matches"></a>
<ResponseField name="count_matches" kind="method" source="https://github.com/tabsdata/tabsdata/blob/b1.9/client/platform/src/main/python/tabsdata/tableframe/expr/string.py#L776">

```python
def count_matches(
    pattern: str | Expr,
    *,
    literal: bool = False,
)
```

Counts the ocurrrences of the given pattern in the string.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a")
      .str.count_matches("b.").alias("count_matches"))
>>>
┌───────────┬───────────────┐
│ a         ┆ count_matches │
│ ---       ┆ ---           │
│ str       ┆ u32           │
╞═══════════╪═══════════════╡
│ a bAb c d ┆ 2             │
│ bCbb c d  ┆ 2             │
│ bb        ┆ 1             │
│ b         ┆ 0             │
│ a         ┆ 0             │
│ null      ┆ null          │
└───────────┴───────────────┘
```

**Categories:** string

**Parameters:**

<ParamField path="pattern">
  The pattern to extract.
</ParamField>

<ParamField path="literal">
  Take the pattern as a literal string (not a regex).
</ParamField>

</ResponseField>

<a id="tabsdata.tableframe.expr.string.ExprStringNameSpace.ends_with"></a>
<ResponseField name="ends_with" kind="method" source="https://github.com/tabsdata/tabsdata/blob/b1.9/client/platform/src/main/python/tabsdata/tableframe/expr/string.py#L667">

```python
def ends_with(
    suffix: str | Expr,
)
```

Evaluate if the string ends with.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.ends_with("b").alias("ends_with"))
>>>
┌──────┬───────────┐
│ a    ┆ ends_with │
│ ---  ┆ ---       │
│ str  ┆ bool      │
╞══════╪═══════════╡
│ a    ┆ false     │
│ ab   ┆ true      │
│ b    ┆ true      │
│ xaby ┆ false     │
│ null ┆ null      │
└──────┴───────────┘
```

**Categories:** string

**Parameters:**

<ParamField path="suffix">
  The suffix to search for.
</ParamField>

</ResponseField>

<a id="tabsdata.tableframe.expr.string.ExprStringNameSpace.extract"></a>
<ResponseField name="extract" kind="method" source="https://github.com/tabsdata/tabsdata/blob/b1.9/client/platform/src/main/python/tabsdata/tableframe/expr/string.py#L736">

```python
def extract(
    pattern: td_typing.IntoExprColumn,
    group_index: int = 1,
)
```

Extract a pattern from the string.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.extract("(b.b)", 1).alias("extract"))
>>>
┌───────────┬─────────┐
│ a         ┆ extract │
│ ---       ┆ ---     │
│ str       ┆ str     │
╞═══════════╪═════════╡
│ a bAb c d ┆ bAb     │
│ bCbb c d  ┆ bCb     │
│ bb        ┆ null    │
│ null      ┆ null    │
└───────────┴─────────┘
```

**Categories:** string

**Parameters:**

<ParamField path="pattern">
  The pattern to extract.
</ParamField>

<ParamField path="group_index">
  The group index to extract.
</ParamField>

</ResponseField>

<a id="tabsdata.tableframe.expr.string.ExprStringNameSpace.find"></a>
<ResponseField name="find" kind="method" source="https://github.com/tabsdata/tabsdata/blob/b1.9/client/platform/src/main/python/tabsdata/tableframe/expr/string.py#L621">

```python
def find(
    pattern: str | Expr,
    *,
    literal: bool = False,
    strict: bool = True,
)
```

Find the position of the first occurrence of the given pattern.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.find("b").alias("find"))
>>>
┌──────┬──────┐
│ a    ┆ find │
│ ---  ┆ ---  │
│ str  ┆ u32  │
╞══════╪══════╡
│ a    ┆ null │
│ ab   ┆ 1    │
│ b    ┆ 0    │
│ xaby ┆ 2    │
│ null ┆ null │
└──────┴──────┘
```

**Categories:** string

**Parameters:**

<ParamField path="pattern">
  The pattern to search for.
</ParamField>

<ParamField path="literal">
  Take the pattern as a literal string (not a regex).
</ParamField>

<ParamField path="strict">
  if the given pattern is not valid regex, raise an error.
</ParamField>

</ResponseField>

<a id="tabsdata.tableframe.expr.string.ExprStringNameSpace.grok"></a>
<ResponseField name="grok" kind="method" source="https://github.com/tabsdata/tabsdata/blob/b1.9/client/platform/src/main/python/tabsdata/tableframe/expr/string.py#L1179">

```python
def grok(
    pattern: str,
    schema: dict[str, Column],
)
```

Parse log text into structured fields using a Grok pattern.

Applies the given Grok pattern to the values in the current string expression.
Each **named capture group** in the pattern becomes a new output column.
Rows that do not match the pattern will return null for the extracted fields.

Example:

```text
>>> import tabsdata as td
>>> tf = td.TableFrame({"logs": [
...     "alice-login-2023",
...     "bob-logout-2024",
... ]})
>>>
>>> log_pattern = r"%{WORD:user}-%{WORD:action}-%{INT:year}"
>>> log_schema = {
>>>     "word": td_col.Column("user", td.String),
>>>     "action": td_col.Column("action", td.String),
>>>     "year": td_col.Column("year", td.Int8),
>>> }
>>> out = tf.grok("logs", log_pattern, log_schema)
>>> tf.select(
...     td.col("logs"),
...     td.col("logs").str.grok(log_pattern, log_schema)
... )
>>>
┌──────────────────┬───────┬────────┬──────┐
│ logs             ┆ user  ┆ action ┆ year │
│ ---              ┆ ---   ┆ ---    ┆ ---  │
│ str              ┆ str   ┆ str    ┆ i64  │
╞══════════════════╪═══════╪════════╪══════╡
│ alice-login-2023 ┆ alice ┆ login  ┆ 2023 │
│ bob-logout-2024  ┆ bob   ┆ logout ┆ 2024 │
└──────────────────┴───────┴────────┴──────┘
```

Notes

- The function automatically expands the Grok captures into separate
columns.
- Non-matching rows will show null for the extracted columns.
- If a pattern defines duplicate capture names, numeric suffixes like
field, field[1] will be used to disambiguate them.

**Categories:** string

**Parameters:**

<ParamField path="pattern" type="str">
  Grok pattern with named captures (e.g., %&#123;WORD:user&#125;).
</ParamField>

<ParamField path="schema" type="dict">
  A mapping where each capture name
is associated with its corresponding column definition, specifying
both the column name and its data type.
</ParamField>

</ResponseField>

<a id="tabsdata.tableframe.expr.string.ExprStringNameSpace.head"></a>
<ResponseField name="head" kind="method" source="https://github.com/tabsdata/tabsdata/blob/b1.9/client/platform/src/main/python/tabsdata/tableframe/expr/string.py#L984">

```python
def head(
    n: int | td_typing.IntoExprColumn,
)
```

Extract the start of the string up to the given length.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.head(2).alias("head"))
>>>
┌──────┬──────┐
│ a    ┆ head │
│ ---  ┆ ---  │
│ str  ┆ str  │
╞══════╪══════╡
│ abc  ┆ ab   │
│ a    ┆ a    │
│ null ┆ null │
└──────┴──────┘
```

**Categories:** string

**Parameters:**

<ParamField path="n">
  The length of the head.
</ParamField>

</ResponseField>

<a id="tabsdata.tableframe.expr.string.ExprStringNameSpace.len_bytes"></a>
<ResponseField name="len_bytes" kind="method" source="https://github.com/tabsdata/tabsdata/blob/b1.9/client/platform/src/main/python/tabsdata/tableframe/expr/string.py#L169">

```python
def len_bytes()
```

Return number of bytes (not chars) of a string.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.len_bytes().alias("len_bytes"))
>>>
┌──────┬────────────┐
│ a    ┆ to_decimal │
│ ---  ┆ ---        │
│ str  ┆ u32        │
╞══════╪════════════╡
│ ab   ┆ 2          │
│ 再   ┆ 3          │
│ null ┆ null       │
└──────┴────────────┘
```

**Categories:** string

</ResponseField>

<a id="tabsdata.tableframe.expr.string.ExprStringNameSpace.len_chars"></a>
<ResponseField name="len_chars" kind="method" source="https://github.com/tabsdata/tabsdata/blob/b1.9/client/platform/src/main/python/tabsdata/tableframe/expr/string.py#L196">

```python
def len_chars()
```

Return number of chars (not bytes) of a string.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.len_chars().alias("len_chars"))
>>>
┌──────┬────────────┐
│ a    ┆ to_decimal │
│ ---  ┆ ---        │
│ str  ┆ u32        │
╞══════╪════════════╡
│ ab   ┆ 2          │
│ 再   ┆ 3          │
│ null ┆ null       │
└──────┴────────────┘
```

**Categories:** string

</ResponseField>

<a id="tabsdata.tableframe.expr.string.ExprStringNameSpace.pad_end"></a>
<ResponseField name="pad_end" kind="method" source="https://github.com/tabsdata/tabsdata/blob/b1.9/client/platform/src/main/python/tabsdata/tableframe/expr/string.py#L512">

```python
def pad_end(
    length: int,
    fill_char: str = ' ',
)
```

Pad string values at the end to the given length using the given
fill character.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.pad_end(6, "-").alias("pad_end"))
>>>
┌────────┬─────────┐
│ a      ┆ pad_end │
│ ---    ┆ ---     │
│ str    ┆ str     │
╞════════╪═════════╡
│ abc    ┆ abc---  │
│    def ┆    def  │
│ null   ┆ null    │
└────────┴─────────┘
```

**Categories:** string

**Parameters:**

<ParamField path="length">
  The length to end pad the string to.
</ParamField>

<ParamField path="fill_char">
  The character to use for padding.
</ParamField>

</ResponseField>

<a id="tabsdata.tableframe.expr.string.ExprStringNameSpace.pad_start"></a>
<ResponseField name="pad_start" kind="method" source="https://github.com/tabsdata/tabsdata/blob/b1.9/client/platform/src/main/python/tabsdata/tableframe/expr/string.py#L480">

```python
def pad_start(
    length: int,
    fill_char: str = ' ',
)
```

Pad string values at the front to the given length using the given
fill character.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.pad_start(6, "-").alias("pad_start"))
>>>
┌────────┬───────────┐
│ a      ┆ pad_start │
│ ---    ┆ ---       │
│ str    ┆ str       │
╞════════╪═══════════╡
│ abc    ┆ ---abc    │
│    def ┆    def    │
│ null   ┆ null      │
└────────┴───────────┘
```

**Categories:** string

**Parameters:**

<ParamField path="length">
  The length to front pad the string to.
</ParamField>

<ParamField path="fill_char">
  The character to use for padding.
</ParamField>

</ResponseField>

<a id="tabsdata.tableframe.expr.string.ExprStringNameSpace.replace"></a>
<ResponseField name="replace" kind="method" source="https://github.com/tabsdata/tabsdata/blob/b1.9/client/platform/src/main/python/tabsdata/tableframe/expr/string.py#L818">

```python
def replace(
    pattern: str | Expr,
    value: str | Expr,
    *,
    literal: bool = False,
    n: int = 1,
)
```

Replace the first occurence of a pattern with the given string.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.replace("b", "X").alias("replace"))
>>>
┌───────────┬───────────┐
│ a         ┆ replace   │
│ ---       ┆ ---       │
│ str       ┆ str       │
╞═══════════╪═══════════╡
│ a bAb c d ┆ a XAb c d │
│ bCbb c d  ┆ XCbb c d  │
│ bb        ┆ Xb        │
│ b         ┆ X         │
│ a         ┆ a         │
│ null      ┆ null      │
└───────────┴───────────┘
```

**Categories:** string

**Parameters:**

<ParamField path="pattern">
  The pattern to replace.
</ParamField>

<ParamField path="value">
  The value to replace the pattern with.
</ParamField>

<ParamField path="literal">
  Take the pattern as a literal string (not a regex).
</ParamField>

<ParamField path="n">
  Number of matches to replace.
</ParamField>

</ResponseField>

<a id="tabsdata.tableframe.expr.string.ExprStringNameSpace.replace_all"></a>
<ResponseField name="replace_all" kind="method" source="https://github.com/tabsdata/tabsdata/blob/b1.9/client/platform/src/main/python/tabsdata/tableframe/expr/string.py#L868">

```python
def replace_all(
    pattern: str | Expr,
    value: str | Expr,
    *,
    literal: bool = False,
)
```

Replace the all occurences of a pattern with the given string.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.replace("b", "X").alias("replace"))
>>>
┌───────────┬─────────────┐
│ a         ┆ replace_all │
│ ---       ┆ ---         │
│ str       ┆ str         │
╞═══════════╪═════════════╡
│ a bAb c d ┆ a XAX c d   │
│ bCbb c d  ┆ XCXX c d    │
│ bb        ┆ XX          │
│ b         ┆ X           │
│ a         ┆ a           │
│ null      ┆ null        │
└───────────┴─────────────┘
```

**Categories:** string

**Parameters:**

<ParamField path="pattern">
  The pattern to replace.
</ParamField>

<ParamField path="value">
  The value to replace the pattern with.
</ParamField>

<ParamField path="literal">
  Take the pattern as a literal string (not a regex).
</ParamField>

</ResponseField>

<a id="tabsdata.tableframe.expr.string.ExprStringNameSpace.replace_many"></a>
<ResponseField name="replace_many" kind="method" source="https://github.com/tabsdata/tabsdata/blob/b1.9/client/platform/src/main/python/tabsdata/tableframe/expr/string.py#L1131">

```python
def replace_many(
    patterns: td_typing.IntoExpr | Mapping[str, str], replace_with: td_typing.IntoExpr | NoDefault = <no_default>, *, ascii_case_insensitive: bool = False,
)
```

Replace the all occurences of any the given patterns with the given string.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a")
      .str.replace_many(["a", "b"], "X").alias("replace_many"))
>>>
┌──────┬──────────────┐
│ a    ┆ replace_many │
│ ---  ┆ ---          │
│ str  ┆ str          │
╞══════╪══════════════╡
│ abc  ┆ XXc          │
│ axy  ┆ Xxy          │
│ xyb  ┆ xyX          │
│ xyz  ┆ xyz          │
│ null ┆ null         │
└──────┴──────────────┘
```

**Categories:** string

**Parameters:**

<ParamField path="patterns">
  The patterns to replace.
</ParamField>

<ParamField path="replace_with">
  The value to replace the pattern with.
</ParamField>

<ParamField path="ascii_case_insensitive">
  If true, the search is case-insensitive.
</ParamField>

</ResponseField>

<a id="tabsdata.tableframe.expr.string.ExprStringNameSpace.reverse"></a>
<ResponseField name="reverse" kind="method" source="https://github.com/tabsdata/tabsdata/blob/b1.9/client/platform/src/main/python/tabsdata/tableframe/expr/string.py#L916">

```python
def reverse()
```

Reverse the string.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.reverse().alias("reverse"))
>>>
┌──────┬─────────┐
│ a    ┆ reverse │
│ ---  ┆ ---     │
│ str  ┆ str     │
╞══════╪═════════╡
│ abc  ┆ cba     │
│ a    ┆ a       │
│ null ┆ null    │
└──────┴─────────┘
```

**Categories:** string

</ResponseField>

<a id="tabsdata.tableframe.expr.string.ExprStringNameSpace.slice"></a>
<ResponseField name="slice" kind="method" source="https://github.com/tabsdata/tabsdata/blob/b1.9/client/platform/src/main/python/tabsdata/tableframe/expr/string.py#L943">

```python
def slice(
    offset: int | td_typing.IntoExprColumn,
    length: int | td_typing.IntoExprColumn | None = None,
)
```

Extract the substring at the given offset for the given length.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.slice(1,1).alias("slice"))
>>>
┌──────┬───────┐
│ a    ┆ slice │
│ ---  ┆ ---   │
│ str  ┆ str   │
╞══════╪═══════╡
│ abc  ┆ b     │
│ a    ┆       │
│ null ┆ null  │
└──────┴───────┘
```

**Categories:** string

**Parameters:**

<ParamField path="offset">
  The offset to start the slice.
</ParamField>

<ParamField path="length">
  The length of the slice. If None, slice until the end of the string.
</ParamField>

</ResponseField>

<a id="tabsdata.tableframe.expr.string.ExprStringNameSpace.starts_with"></a>
<ResponseField name="starts_with" kind="method" source="https://github.com/tabsdata/tabsdata/blob/b1.9/client/platform/src/main/python/tabsdata/tableframe/expr/string.py#L701">

```python
def starts_with(
    prefix: str | Expr,
)
```

Evaluate if the string start with.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a")
      .str.starts_with("a").alias("starts_with"))
>>>
┌──────┬────────────┐
│ a    ┆ start_with │
│ ---  ┆ ---        │
│ str  ┆ bool       │
╞══════╪════════════╡
│ a    ┆ true       │
│ ab   ┆ true       │
│ b    ┆ false      │
│ xaby ┆ false      │
│ null ┆ null       │
└──────┴────────────┘
```

**Categories:** string

**Parameters:**

<ParamField path="prefix">
  The suffix to search for.
</ParamField>

</ResponseField>

<a id="tabsdata.tableframe.expr.string.ExprStringNameSpace.strip_chars"></a>
<ResponseField name="strip_chars" kind="method" source="https://github.com/tabsdata/tabsdata/blob/b1.9/client/platform/src/main/python/tabsdata/tableframe/expr/string.py#L304">

```python
def strip_chars(
    characters: td_typing.IntoExpr = None,
)
```

Trim string values.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a")
      .str.strip_chars("a ").alias("strip_chars"))
>>>
┌─────────────────────────────────┬─────────────┐
│ a                               ┆ strip_chars │
│ ---                             ┆ ---         │
│ str                             ┆ str         │
╞═════════════════════════════════╪═════════════╡
│ acba cda                      … ┆ cba cd      │
│    xy z                         ┆ xy z        │
│ null                            ┆ null        │
└─────────────────────────────────┴─────────────┘
```

**Categories:** string

**Parameters:**

<ParamField path="characters">
  Characters to trim from start and end of the string.
</ParamField>

<ParamField path="All characteres in the given string are removed,">
  
</ParamField>

<ParamField path="regardless the order. Default is whitespace.">
  
</ParamField>

</ResponseField>

<a id="tabsdata.tableframe.expr.string.ExprStringNameSpace.strip_chars_end"></a>
<ResponseField name="strip_chars_end" kind="method" source="https://github.com/tabsdata/tabsdata/blob/b1.9/client/platform/src/main/python/tabsdata/tableframe/expr/string.py#L380">

```python
def strip_chars_end(
    characters: td_typing.IntoExpr = None,
)
```

Trim string values from the end of the string.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a")
      .str.strip_chars_end("dc ").alias("strip_chars_end"))
>>>
┌───────────────────────────────┬─────────────────┐
│ a                             ┆ strip_chars_end │
│ ---                           ┆ ---             │
│ str                           ┆ str             │
╞═══════════════════════════════╪═════════════════╡
│ cba cd                        ┆ cba             │
│    xy z                       ┆    xy z         │
│ null                          ┆ null            │
└───────────────────────────────┴─────────────────┘
```

**Categories:** string

**Parameters:**

<ParamField path="characters">
  Characters to trim from start of the string.
</ParamField>

<ParamField path="All ending characteres in the given string are removed,">
  
</ParamField>

<ParamField path="regardless the order. Default is whitespace.">
  
</ParamField>

</ResponseField>

<a id="tabsdata.tableframe.expr.string.ExprStringNameSpace.strip_chars_start"></a>
<ResponseField name="strip_chars_start" kind="method" source="https://github.com/tabsdata/tabsdata/blob/b1.9/client/platform/src/main/python/tabsdata/tableframe/expr/string.py#L342">

```python
def strip_chars_start(
    characters: td_typing.IntoExpr = None,
)
```

Trim string values from the start of the string.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a")
      .str.strip_chars_start("abc").alias("strip_chars_start"))
>>>
┌───────────────────────────────┬────────────────────────────┐
│ a                             ┆ strip_chars_start          │
│ ---                           ┆ ---                        │
│ str                           ┆ str                        │
╞═══════════════════════════════╪════════════════════════════╡
│ cba cd                        ┆  cd                        │
│    xy z                       ┆    xy z                    │
│ null                          ┆ null                       │
└───────────────────────────────┴────────────────────────────┘
```

**Categories:** string

**Parameters:**

<ParamField path="characters">
  Characters to trim from start of the string.
</ParamField>

<ParamField path="All starting characteres in the given string are removed,">
  
</ParamField>

<ParamField path="regardless the order. Default is whitespace.">
  
</ParamField>

</ResponseField>

<a id="tabsdata.tableframe.expr.string.ExprStringNameSpace.strip_prefix"></a>
<ResponseField name="strip_prefix" kind="method" source="https://github.com/tabsdata/tabsdata/blob/b1.9/client/platform/src/main/python/tabsdata/tableframe/expr/string.py#L418">

```python
def strip_prefix(
    prefix: td_typing.IntoExpr,
)
```

Trim string values removing the given prefix

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a")
      .str.strip_prefix("cb").alias("strip_prefix"))
>>>
┌───────────────────────────────┬─────────────────┐
│ a                             ┆ strip_prefix    │
│ ---                           ┆ ---             │
│ str                           ┆ str             │
╞═══════════════════════════════╪═════════════════╡
│ cba cd                        ┆ a cd            │
│ bx                            ┆ bx              │
│ null                          ┆ null            │
└───────────────────────────────┴─────────────────┘
```

**Categories:** string

**Parameters:**

<ParamField path="prefix">
  Prefix to remove from the string.
</ParamField>

</ResponseField>

<a id="tabsdata.tableframe.expr.string.ExprStringNameSpace.strip_suffix"></a>
<ResponseField name="strip_suffix" kind="method" source="https://github.com/tabsdata/tabsdata/blob/b1.9/client/platform/src/main/python/tabsdata/tableframe/expr/string.py#L449">

```python
def strip_suffix(
    suffix: td_typing.IntoExpr,
)
```

Trim string values removing the given suffix

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a")
      .str.strip_suffix("cd").alias("strip_suffix"))
>>>
┌───────────────────────────────┬─────────────────┐
│ a                             ┆ strip_suffix    │
│ ---                           ┆ ---             │
│ str                           ┆ str             │
╞═══════════════════════════════╪═════════════════╡
│ cba cd                        ┆ cba             │
│ bx                            ┆ bx              │
│ null                          ┆ null            │
└───────────────────────────────┴─────────────────┘
```

**Categories:** string

**Parameters:**

<ParamField path="suffix">
  Suffix to remove from the string.
</ParamField>

</ResponseField>

<a id="tabsdata.tableframe.expr.string.ExprStringNameSpace.tail"></a>
<ResponseField name="tail" kind="method" source="https://github.com/tabsdata/tabsdata/blob/b1.9/client/platform/src/main/python/tabsdata/tableframe/expr/string.py#L1017">

```python
def tail(
    n: int | td_typing.IntoExprColumn,
)
```

Extract the end of the string up to the given length.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.tail(2).alias("tail"))
>>>
┌──────┬──────┐
│ a    ┆ tail │
│ ---  ┆ ---  │
│ str  ┆ str  │
╞══════╪══════╡
│ abc  ┆ bc   │
│ a    ┆ a    │
│ null ┆ null │
└──────┴──────┘
```

**Categories:** string

**Parameters:**

<ParamField path="n">
  The length of the tail.
</ParamField>

</ResponseField>

<a id="tabsdata.tableframe.expr.string.ExprStringNameSpace.to_date"></a>
<ResponseField name="to_date" kind="method" source="https://github.com/tabsdata/tabsdata/blob/b1.9/client/platform/src/main/python/tabsdata/tableframe/expr/string.py#L39">

```python
def to_date(
    fmt: str | None = None,
    *,
    strict: bool = True,
)
```

Convert the string to a date.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.to_date().alias("to_date"))
>>>
┌────────────┬────────────┐
│ a          ┆ to_date    │
│ ---        ┆ ---        │
│ str        ┆ date       │
╞════════════╪════════════╡
│ 2024-12-13 ┆ 2024-12-13 │
│ 2024-12-15 ┆ 2024-12-15 │
│ null       ┆ null       │
└────────────┴────────────┘
```

**Categories:** type_casting

**Parameters:**

<ParamField path="fmt">
  The date format string (default %Y-%m-%d).
See formats.
</ParamField>

<ParamField path="strict">
  Whether to parse the date strictly.
</ParamField>

</ResponseField>

<a id="tabsdata.tableframe.expr.string.ExprStringNameSpace.to_datetime"></a>
<ResponseField name="to_datetime" kind="method" source="https://github.com/tabsdata/tabsdata/blob/b1.9/client/platform/src/main/python/tabsdata/tableframe/expr/string.py#L78">

```python
def to_datetime(
    format: str | None = None,
    *,
    time_unit: Literal['ns', 'us', 'ms'] | None = None,
    time_zone: str | None = None,
    strict: bool = True,
    ambiguous: Literal['earliest', 'latest', 'raise', 'null'] | Expr = 'raise',
)
```

Convert the string to a datetime.

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.to_datetime().alias("to_datetime"))
>>>
┌─────────────────────┬─────────────────────┐
│ a                   ┆ to_datetime         │
│ ---                 ┆ ---                 │
│ str                 ┆ datetime[μs]        │
╞═════════════════════╪═════════════════════╡
│ 2024-12-13 08:45:34 ┆ 2024-12-13 08:45:34 │
│ 2024-12-15 18:33:00 ┆ 2024-12-15 18:33:00 │
│ null                ┆ null                │
└─────────────────────┴─────────────────────┘
```

**Categories:** type_casting

**Parameters:**

<ParamField path="format">
  The datetime format string (default %Y-%m-%d %H:%M:%S).
See formats.
</ParamField>

<ParamField path="time_unit">
  &#123;None, ‘us’, ‘ns’, ‘ms’&#125;
If None (default), it inferred from the format string
</ParamField>

<ParamField path="time_zone">
  Time zone for the resulting value.
</ParamField>

<ParamField path="strict">
  If the conversion fails an error will be raised.
</ParamField>

<ParamField path="ambiguous">
  Policy to apply on ambiguos Datetimes:
‘raise’: saises an error
‘earliest’: use the earliest datetime
‘latest’: use the latest datetime
‘null’: set to null
</ParamField>

</ResponseField>

<a id="tabsdata.tableframe.expr.string.ExprStringNameSpace.to_integer"></a>
<ResponseField name="to_integer" kind="method" source="https://github.com/tabsdata/tabsdata/blob/b1.9/client/platform/src/main/python/tabsdata/tableframe/expr/string.py#L1050">

```python
def to_integer(
    *,
    base: int | td_typing.IntoExprColumn = 10,
    strict: bool = True,
)
```

Covert a string to integer.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a")
      .str.to_integer(strict=False).alias("to_integer"))
>>>
┌──────┬────────────┐
│ a    ┆ to_integer │
│ ---  ┆ ---        │
│ str  ┆ i64        │
╞══════╪════════════╡
│ 1    ┆ 1          │
│ 2.2  ┆ null       │
│ a    ┆ null       │
│ null ┆ null       │
└──────┴────────────┘
```

**Categories:** type_casting

**Parameters:**

<ParamField path="base">
  The base of the integer.
</ParamField>

<ParamField path="strict">
  If true, raise an error if the string is not a valid integer.
</ParamField>

</ResponseField>

<a id="tabsdata.tableframe.expr.string.ExprStringNameSpace.to_lowercase"></a>
<ResponseField name="to_lowercase" kind="method" source="https://github.com/tabsdata/tabsdata/blob/b1.9/client/platform/src/main/python/tabsdata/tableframe/expr/string.py#L249">

```python
def to_lowercase()
```

Return the lowercase of a string.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.to_lowercase().alias("to_lowercase"))
>>>
┌──────┬───────────────┐
│ a    ┆ to_lowerrcase │
│ ---  ┆ ---           │
│ str  ┆ u32           │
╞══════╪═══════════════╡
│ aB   ┆ ab            │
│ null ┆ null          │
└──────┴───────────────┘
```

**Categories:** string

</ResponseField>

<a id="tabsdata.tableframe.expr.string.ExprStringNameSpace.to_time"></a>
<ResponseField name="to_time" kind="method" source="https://github.com/tabsdata/tabsdata/blob/b1.9/client/platform/src/main/python/tabsdata/tableframe/expr/string.py#L135">

```python
def to_time(
    fmt: str | None = None,
    *,
    strict: bool = True,
    cache: bool = True,
)
```

Convert the string to a time.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.to_time().alias("to_time"))
>>>
┌─────────────────────┬─────────────────────┐
│ a                   ┆ to_datetime         │
│ ---                 ┆ ---                 │
│ str                 ┆ datetime[μs]        │
╞═════════════════════╪═════════════════════╡
│ 2024-12-13 08:45:34 ┆ 2024-12-13 08:45:34 │
│ 2024-12-15 18:33:00 ┆ 2024-12-15 18:33:00 │
│ null                ┆ null                │
└─────────────────────┴─────────────────────┘
```

**Categories:** type_casting

**Parameters:**

<ParamField path="fmt">
  The time format string (default %H:%M:%S).
See formats.
</ParamField>

<ParamField path="strict">
  Whether to parse the date strictly.
</ParamField>

<ParamField path="cache">
  Whether to cache the date.
</ParamField>

</ResponseField>

<a id="tabsdata.tableframe.expr.string.ExprStringNameSpace.to_titlecase"></a>
<ResponseField name="to_titlecase" kind="method" source="https://github.com/tabsdata/tabsdata/blob/b1.9/client/platform/src/main/python/tabsdata/tableframe/expr/string.py#L275">

```python
def to_titlecase()
```

Uppercase the first character and lowercase all the others ones of a string.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.to_titlecase().alias("titlecase"))
>>>
┌──────┬───────────┐
│ a    ┆ titlecase │
│ ---  ┆ ---       │
│ str  ┆ str       │
╞══════╪═══════════╡
│ ab   ┆ Ab        │
│ Ab   ┆ Ab        │
│ AB   ┆ Ab        │
│ aB   ┆ Ab        │
│ null ┆ null      │
└──────┴───────────┘
```

**Categories:** string

</ResponseField>

<a id="tabsdata.tableframe.expr.string.ExprStringNameSpace.to_uppercase"></a>
<ResponseField name="to_uppercase" kind="method" source="https://github.com/tabsdata/tabsdata/blob/b1.9/client/platform/src/main/python/tabsdata/tableframe/expr/string.py#L223">

```python
def to_uppercase()
```

Return the uppercase of a string.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.to_uppercase().alias("to_uppercase"))
>>>
┌──────┬──────────────┐
│ a    ┆ to_uppercase │
│ ---  ┆ ---          │
│ str  ┆ u32          │
╞══════╪══════════════╡
│ aB   ┆ AB           │
│ null ┆ null         │
└──────┴──────────────┘
```

**Categories:** string

</ResponseField>

<a id="tabsdata.tableframe.expr.string.ExprStringNameSpace.zfill"></a>
<ResponseField name="zfill" kind="method" source="https://github.com/tabsdata/tabsdata/blob/b1.9/client/platform/src/main/python/tabsdata/tableframe/expr/string.py#L544">

```python
def zfill(
    length: int | td_typing.IntoExprColumn,
)
```

Pad numeric string values at the start to the given length using zeros.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.zfill(2).alias("zfill"))
>>>
┌──────┬───────┐
│ a    ┆ zfill │
│ ---  ┆ ---   │
│ str  ┆ str   │
╞══════╪═══════╡
│ 0    ┆ 00    │
│ 1    ┆ 01    │
│ 1000 ┆ 1000  │
│ null ┆ null  │
└──────┴───────┘
```

**Categories:** string

**Parameters:**

<ParamField path="length">
  The length to end pad the string to.
</ParamField>

</ResponseField>

</details>
<a class="entry-source-link" href="https://github.com/tabsdata/tabsdata/blob/b1.9/client/platform/src/main/python/tabsdata/tableframe/expr/string.py#L34" target="_blank" rel="noopener noreferrer">View source ↗</a>
</div>

                                 <a id="tabsdata.tableframe.expr.string.GrokParser"></a>
<div class="api-entry-wrap">
<details open>
<summary>GrokParser</summary>

```python
class GrokParser(
    pattern: 'str',
    schema: 'dict[str, td_col.Column]',
)
```

Bases: [object](https://docs.python.org/3/library/functions.html#object)

<a id="tabsdata.tableframe.expr.string.GrokParser.index"></a>
<ResponseField name="index" kind="attribute">

</ResponseField>

<a id="tabsdata.tableframe.expr.string.GrokParser.pattern"></a>
<ResponseField name="pattern" kind="attribute">

</ResponseField>

<a id="tabsdata.tableframe.expr.string.GrokParser.schema"></a>
<ResponseField name="schema" kind="attribute">

</ResponseField>

<a id="tabsdata.tableframe.expr.string.GrokParser.temp_column"></a>
<ResponseField name="temp_column" kind="attribute">

</ResponseField>

<a id="tabsdata.tableframe.expr.string.GrokParser.rust"></a>
<ResponseField name="rust" kind="method" source="https://github.com/tabsdata/tabsdata/blob/b1.9/expansions/polars/modules/ty-tableframe/python/src/main/python/tabsdata/expansions/tableframe/features/grok/api/_handler.py#L30">

```python
def rust(
    expression: IntoExpr,
)
```

</ResponseField>

</details>
<a class="entry-source-link" href="https://github.com/tabsdata/tabsdata/blob/b1.9/expansions/polars/modules/ty-tableframe/python/src/main/python/tabsdata/expansions/tableframe/features/grok/api/_handler.py#L21" target="_blank" rel="noopener noreferrer">View source ↗</a>
</div>
