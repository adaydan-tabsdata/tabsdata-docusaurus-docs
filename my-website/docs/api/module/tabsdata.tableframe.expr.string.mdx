---
title: "tabsdata.tableframe.expr.string"
---

import { ParamField, ResponseField } from '@site/src/components/ApiField';

# tabsdata.tableframe.expr.string

 <details>
<summary>

**`ExprStringNameSpace(expr: ExprStringNameSpace)`**

</summary>

Bases: [object](https://docs.python.org/3/library/functions.html#object)

<details>
<summary>

**`contains(pattern: str | Expr, *, literal: bool = False, strict: bool = True)`**

</summary>

Evaluate if the string contains a pattern.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.contains("ab").alias("contains"))
>>>
┌──────┬──────────┐
│ a    ┆ contains │
│ ---  ┆ ---      │
│ str  ┆ bool     │
╞══════╪══════════╡
│ a    ┆ false    │
│ ab   ┆ true     │
│ b    ┆ false    │
│ xaby ┆ true     │
│ null ┆ null     │
└──────┴──────────┘
```

<ParamField path="pattern">
  The pattern to search for.
</ParamField>

<ParamField path="literal">
  Take the pattern as a literal string (not a regex).
</ParamField>

<ParamField path="strict">
  if the given pattern is not valid regex, raise an error.
</ParamField>

</details>

<details>
<summary>

**`contains_any(patterns: td_typing.IntoExpr, *, ascii_case_insensitive: bool = False)`**

</summary>

Evaluate if the string contains any of the given patterns.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a")
      .str.contains_any(["a", "b"]).alias("contains_any"))
>>>
┌──────┬──────────────┐
│ a    ┆ contains_any │
│ ---  ┆ ---          │
│ str  ┆ bool         │
╞══════╪══════════════╡
│ abc  ┆ true         │
│ axy  ┆ true         │
│ xyb  ┆ true         │
│ xyz  ┆ false        │
│ null ┆ null         │
└──────┴──────────────┘
```

<ParamField path="patterns">
  The patterns to search for.
</ParamField>

<ParamField path="ascii_case_insensitive">
  If true, the search is case-insensitive.
</ParamField>

</details>

<details>
<summary>

**`count_matches(pattern: str | Expr, *, literal: bool = False)`**

</summary>

Counts the ocurrrences of the given pattern in the string.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a")
      .str.count_matches("b.").alias("count_matches"))
>>>
┌───────────┬───────────────┐
│ a         ┆ count_matches │
│ ---       ┆ ---           │
│ str       ┆ u32           │
╞═══════════╪═══════════════╡
│ a bAb c d ┆ 2             │
│ bCbb c d  ┆ 2             │
│ bb        ┆ 1             │
│ b         ┆ 0             │
│ a         ┆ 0             │
│ null      ┆ null          │
└───────────┴───────────────┘
```

<ParamField path="pattern">
  The pattern to extract.
</ParamField>

<ParamField path="literal">
  Take the pattern as a literal string (not a regex).
</ParamField>

</details>

<details>
<summary>

**`ends_with(suffix: str | Expr)`**

</summary>

Evaluate if the string ends with.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.ends_with("b").alias("ends_with"))
>>>
┌──────┬───────────┐
│ a    ┆ ends_with │
│ ---  ┆ ---       │
│ str  ┆ bool      │
╞══════╪═══════════╡
│ a    ┆ false     │
│ ab   ┆ true      │
│ b    ┆ true      │
│ xaby ┆ false     │
│ null ┆ null      │
└──────┴───────────┘
```

</details>

<details>
<summary>

**`extract(pattern: td_typing.IntoExprColumn, group_index: int = 1)`**

</summary>

Extract a pattern from the string.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.extract("(b.b)", 1).alias("extract"))
>>>
┌───────────┬─────────┐
│ a         ┆ extract │
│ ---       ┆ ---     │
│ str       ┆ str     │
╞═══════════╪═════════╡
│ a bAb c d ┆ bAb     │
│ bCbb c d  ┆ bCb     │
│ bb        ┆ null    │
│ null      ┆ null    │
└───────────┴─────────┘
```

<ParamField path="pattern">
  The pattern to extract.
</ParamField>

<ParamField path="group_index">
  The group index to extract.
</ParamField>

</details>

<details>
<summary>

**`find(pattern: str | Expr, *, literal: bool = False, strict: bool = True)`**

</summary>

Find the position of the first occurrence of the given pattern.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.find("b").alias("find"))
>>>
┌──────┬──────┐
│ a    ┆ find │
│ ---  ┆ ---  │
│ str  ┆ u32  │
╞══════╪══════╡
│ a    ┆ null │
│ ab   ┆ 1    │
│ b    ┆ 0    │
│ xaby ┆ 2    │
│ null ┆ null │
└──────┴──────┘
```

<ParamField path="pattern">
  The pattern to search for.
</ParamField>

<ParamField path="literal">
  Take the pattern as a literal string (not a regex).
</ParamField>

<ParamField path="strict">
  if the given pattern is not valid regex, raise an error.
</ParamField>

</details>

<details>
<summary>

**`grok(pattern: str, schema: dict[str, Column])`**

</summary>

Parse log text into structured fields using a Grok pattern.

Applies the given Grok pattern to the values in the current string expression.
Each **named capture group** in the pattern becomes a new output column.
Rows that do not match the pattern will return null for the extracted fields.

Example:

```text
>>> import tabsdata as td
>>> tf = td.TableFrame({"logs": [
...     "alice-login-2023",
...     "bob-logout-2024",
... ]})
>>>
>>> log_pattern = r"%{WORD:user}-%{WORD:action}-%{INT:year}"
>>> log_schema = {
>>>     "word": td_col.Column("user", td.String),
>>>     "action": td_col.Column("action", td.String),
>>>     "year": td_col.Column("year", td.Int8),
>>> }
>>> out = tf.grok("logs", log_pattern, log_schema)
>>> tf.select(
...     td.col("logs"),
...     td.col("logs").str.grok(log_pattern, log_schema)
... )
>>>
┌──────────────────┬───────┬────────┬──────┐
│ logs             ┆ user  ┆ action ┆ year │
│ ---              ┆ ---   ┆ ---    ┆ ---  │
│ str              ┆ str   ┆ str    ┆ i64  │
╞══════════════════╪═══════╪════════╪══════╡
│ alice-login-2023 ┆ alice ┆ login  ┆ 2023 │
│ bob-logout-2024  ┆ bob   ┆ logout ┆ 2024 │
└──────────────────┴───────┴────────┴──────┘
```

Notes

- The function automatically expands the Grok captures into separate
columns.
- Non-matching rows will show null for the extracted columns.
- If a pattern defines duplicate capture names, numeric suffixes like
field, field[1] will be used to disambiguate them.

<ParamField path="pattern" type="str">
  Grok pattern with named captures (e.g., %&#123;WORD:user&#125;).
</ParamField>

<ParamField path="schema" type="dict">
  A mapping where each capture name
is associated with its corresponding column definition, specifying
both the column name and its data type.
</ParamField>

</details>

<details>
<summary>

**`head(n: int | td_typing.IntoExprColumn)`**

</summary>

Extract the start of the string up to the given length.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.head(2).alias("head"))
>>>
┌──────┬──────┐
│ a    ┆ head │
│ ---  ┆ ---  │
│ str  ┆ str  │
╞══════╪══════╡
│ abc  ┆ ab   │
│ a    ┆ a    │
│ null ┆ null │
└──────┴──────┘
```

</details>

<details>
<summary>

**`len_bytes`**

</summary>

Return number of bytes (not chars) of a string.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.len_bytes().alias("len_bytes"))
>>>
┌──────┬────────────┐
│ a    ┆ to_decimal │
│ ---  ┆ ---        │
│ str  ┆ u32        │
╞══════╪════════════╡
│ ab   ┆ 2          │
│ 再   ┆ 3          │
│ null ┆ null       │
└──────┴────────────┘
```

</details>

<details>
<summary>

**`len_chars`**

</summary>

Return number of chars (not bytes) of a string.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.len_chars().alias("len_chars"))
>>>
┌──────┬────────────┐
│ a    ┆ to_decimal │
│ ---  ┆ ---        │
│ str  ┆ u32        │
╞══════╪════════════╡
│ ab   ┆ 2          │
│ 再   ┆ 3          │
│ null ┆ null       │
└──────┴────────────┘
```

</details>

<details>
<summary>

**`pad_end(length: int, fill_char: str = ' ')`**

</summary>

Pad string values at the end to the given length using the given
fill character.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.pad_end(6, "-").alias("pad_end"))
>>>
┌────────┬─────────┐
│ a      ┆ pad_end │
│ ---    ┆ ---     │
│ str    ┆ str     │
╞════════╪═════════╡
│ abc    ┆ abc---  │
│    def ┆    def  │
│ null   ┆ null    │
└────────┴─────────┘
```

<ParamField path="length">
  The length to end pad the string to.
</ParamField>

<ParamField path="fill_char">
  The character to use for padding.
</ParamField>

</details>

<details>
<summary>

**`pad_start(length: int, fill_char: str = ' ')`**

</summary>

Pad string values at the front to the given length using the given
fill character.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.pad_start(6, "-").alias("pad_start"))
>>>
┌────────┬───────────┐
│ a      ┆ pad_start │
│ ---    ┆ ---       │
│ str    ┆ str       │
╞════════╪═══════════╡
│ abc    ┆ ---abc    │
│    def ┆    def    │
│ null   ┆ null      │
└────────┴───────────┘
```

<ParamField path="length">
  The length to front pad the string to.
</ParamField>

<ParamField path="fill_char">
  The character to use for padding.
</ParamField>

</details>

<details>
<summary>

**`replace(pattern: str | Expr, value: str | Expr, *, literal: bool = False, n: int = 1)`**

</summary>

Replace the first occurence of a pattern with the given string.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.replace("b", "X").alias("replace"))
>>>
┌───────────┬───────────┐
│ a         ┆ replace   │
│ ---       ┆ ---       │
│ str       ┆ str       │
╞═══════════╪═══════════╡
│ a bAb c d ┆ a XAb c d │
│ bCbb c d  ┆ XCbb c d  │
│ bb        ┆ Xb        │
│ b         ┆ X         │
│ a         ┆ a         │
│ null      ┆ null      │
└───────────┴───────────┘
```

<ParamField path="pattern">
  The pattern to replace.
</ParamField>

<ParamField path="value">
  The value to replace the pattern with.
</ParamField>

<ParamField path="literal">
  Take the pattern as a literal string (not a regex).
</ParamField>

<ParamField path="n">
  Number of matches to replace.
</ParamField>

</details>

<details>
<summary>

**`replace_all(pattern: str | Expr, value: str | Expr, *, literal: bool = False)`**

</summary>

Replace the all occurences of a pattern with the given string.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.replace("b", "X").alias("replace"))
>>>
┌───────────┬─────────────┐
│ a         ┆ replace_all │
│ ---       ┆ ---         │
│ str       ┆ str         │
╞═══════════╪═════════════╡
│ a bAb c d ┆ a XAX c d   │
│ bCbb c d  ┆ XCXX c d    │
│ bb        ┆ XX          │
│ b         ┆ X           │
│ a         ┆ a           │
│ null      ┆ null        │
└───────────┴─────────────┘
```

<ParamField path="pattern">
  The pattern to replace.
</ParamField>

<ParamField path="value">
  The value to replace the pattern with.
</ParamField>

<ParamField path="literal">
  Take the pattern as a literal string (not a regex).
</ParamField>

</details>

<details>
<summary>

**`replace_many(patterns: td_typing.IntoExpr | Mapping[str, str], replace_with: td_typing.IntoExpr | NoDefault = <no_default>, *, ascii_case_insensitive: bool = False)`**

</summary>

Replace the all occurences of any the given patterns with the given string.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a")
      .str.replace_many(["a", "b"], "X").alias("replace_many"))
>>>
┌──────┬──────────────┐
│ a    ┆ replace_many │
│ ---  ┆ ---          │
│ str  ┆ str          │
╞══════╪══════════════╡
│ abc  ┆ XXc          │
│ axy  ┆ Xxy          │
│ xyb  ┆ xyX          │
│ xyz  ┆ xyz          │
│ null ┆ null         │
└──────┴──────────────┘
```

<ParamField path="patterns">
  The patterns to replace.
</ParamField>

<ParamField path="replace_with">
  The value to replace the pattern with.
</ParamField>

<ParamField path="ascii_case_insensitive">
  If true, the search is case-insensitive.
</ParamField>

</details>

<details>
<summary>

**`reverse`**

</summary>

Reverse the string.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.reverse().alias("reverse"))
>>>
┌──────┬─────────┐
│ a    ┆ reverse │
│ ---  ┆ ---     │
│ str  ┆ str     │
╞══════╪═════════╡
│ abc  ┆ cba     │
│ a    ┆ a       │
│ null ┆ null    │
└──────┴─────────┘
```

</details>

<details>
<summary>

**`slice(offset: int | td_typing.IntoExprColumn, length: int | td_typing.IntoExprColumn | None = None)`**

</summary>

Extract the substring at the given offset for the given length.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.slice(1,1).alias("slice"))
>>>
┌──────┬───────┐
│ a    ┆ slice │
│ ---  ┆ ---   │
│ str  ┆ str   │
╞══════╪═══════╡
│ abc  ┆ b     │
│ a    ┆       │
│ null ┆ null  │
└──────┴───────┘
```

<ParamField path="offset">
  The offset to start the slice.
</ParamField>

<ParamField path="length">
  The length of the slice. If None, slice until the end of the string.
</ParamField>

</details>

<details>
<summary>

**`starts_with(prefix: str | Expr)`**

</summary>

Evaluate if the string start with.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a")
      .str.starts_with("a").alias("starts_with"))
>>>
┌──────┬────────────┐
│ a    ┆ start_with │
│ ---  ┆ ---        │
│ str  ┆ bool       │
╞══════╪════════════╡
│ a    ┆ true       │
│ ab   ┆ true       │
│ b    ┆ false      │
│ xaby ┆ false      │
│ null ┆ null       │
└──────┴────────────┘
```

</details>

<details>
<summary>

**`strip_chars(characters: td_typing.IntoExpr = None)`**

</summary>

Trim string values.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a")
      .str.strip_chars("a ").alias("strip_chars"))
>>>
┌─────────────────────────────────┬─────────────┐
│ a                               ┆ strip_chars │
│ ---                             ┆ ---         │
│ str                             ┆ str         │
╞═════════════════════════════════╪═════════════╡
│ acba cda                      … ┆ cba cd      │
│    xy z                         ┆ xy z        │
│ null                            ┆ null        │
└─────────────────────────────────┴─────────────┘
```

</details>

<details>
<summary>

**`strip_chars_end(characters: td_typing.IntoExpr = None)`**

</summary>

Trim string values from the end of the string.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a")
      .str.strip_chars_end("dc ").alias("strip_chars_end"))
>>>
┌───────────────────────────────┬─────────────────┐
│ a                             ┆ strip_chars_end │
│ ---                           ┆ ---             │
│ str                           ┆ str             │
╞═══════════════════════════════╪═════════════════╡
│ cba cd                        ┆ cba             │
│    xy z                       ┆    xy z         │
│ null                          ┆ null            │
└───────────────────────────────┴─────────────────┘
```

</details>

<details>
<summary>

**`strip_chars_start(characters: td_typing.IntoExpr = None)`**

</summary>

Trim string values from the start of the string.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a")
      .str.strip_chars_start("abc").alias("strip_chars_start"))
>>>
┌───────────────────────────────┬────────────────────────────┐
│ a                             ┆ strip_chars_start          │
│ ---                           ┆ ---                        │
│ str                           ┆ str                        │
╞═══════════════════════════════╪════════════════════════════╡
│ cba cd                        ┆  cd                        │
│    xy z                       ┆    xy z                    │
│ null                          ┆ null                       │
└───────────────────────────────┴────────────────────────────┘
```

</details>

<details>
<summary>

**`strip_prefix(prefix: td_typing.IntoExpr)`**

</summary>

Trim string values removing the given prefix

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a")
      .str.strip_prefix("cb").alias("strip_prefix"))
>>>
┌───────────────────────────────┬─────────────────┐
│ a                             ┆ strip_prefix    │
│ ---                           ┆ ---             │
│ str                           ┆ str             │
╞═══════════════════════════════╪═════════════════╡
│ cba cd                        ┆ a cd            │
│ bx                            ┆ bx              │
│ null                          ┆ null            │
└───────────────────────────────┴─────────────────┘
```

</details>

<details>
<summary>

**`strip_suffix(suffix: td_typing.IntoExpr)`**

</summary>

Trim string values removing the given suffix

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a")
      .str.strip_suffix("cd").alias("strip_suffix"))
>>>
┌───────────────────────────────┬─────────────────┐
│ a                             ┆ strip_suffix    │
│ ---                           ┆ ---             │
│ str                           ┆ str             │
╞═══════════════════════════════╪═════════════════╡
│ cba cd                        ┆ cba             │
│ bx                            ┆ bx              │
│ null                          ┆ null            │
└───────────────────────────────┴─────────────────┘
```

</details>

<details>
<summary>

**`tail(n: int | td_typing.IntoExprColumn)`**

</summary>

Extract the end of the string up to the given length.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.tail(2).alias("tail"))
>>>
┌──────┬──────┐
│ a    ┆ tail │
│ ---  ┆ ---  │
│ str  ┆ str  │
╞══════╪══════╡
│ abc  ┆ bc   │
│ a    ┆ a    │
│ null ┆ null │
└──────┴──────┘
```

</details>

<details>
<summary>

**`to_date(fmt: str | None = None, *, strict: bool = True)`**

</summary>

Convert the string to a date.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.to_date().alias("to_date"))
>>>
┌────────────┬────────────┐
│ a          ┆ to_date    │
│ ---        ┆ ---        │
│ str        ┆ date       │
╞════════════╪════════════╡
│ 2024-12-13 ┆ 2024-12-13 │
│ 2024-12-15 ┆ 2024-12-15 │
│ null       ┆ null       │
└────────────┴────────────┘
```

<ParamField path="fmt">
  The date format string (default %Y-%m-%d).
See formats.
</ParamField>

<ParamField path="strict">
  Whether to parse the date strictly.
</ParamField>

</details>

<details>
<summary>

**`to_datetime(format: str | None = None, *, time_unit: Literal['ns', 'us', 'ms'] | None = None, time_zone: str | None = None, strict: bool = True, ambiguous: Literal['earliest', 'latest', 'raise', 'null'] | Expr = 'raise')`**

</summary>

Convert the string to a datetime.

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.to_datetime().alias("to_datetime"))
>>>
┌─────────────────────┬─────────────────────┐
│ a                   ┆ to_datetime         │
│ ---                 ┆ ---                 │
│ str                 ┆ datetime[μs]        │
╞═════════════════════╪═════════════════════╡
│ 2024-12-13 08:45:34 ┆ 2024-12-13 08:45:34 │
│ 2024-12-15 18:33:00 ┆ 2024-12-15 18:33:00 │
│ null                ┆ null                │
└─────────────────────┴─────────────────────┘
```

<ParamField path="format">
  The datetime format string (default %Y-%m-%d %H:%M:%S).
See formats.
</ParamField>

<ParamField path="time_unit">
  &#123;None, ‘us’, ‘ns’, ‘ms’&#125;
If None (default), it inferred from the format string
</ParamField>

<ParamField path="time_zone">
  Time zone for the resulting value.
</ParamField>

<ParamField path="strict">
  If the conversion fails an error will be raised.
</ParamField>

<ParamField path="ambiguous">
  Policy to apply on ambiguos Datetimes:
‘raise’: saises an error
‘earliest’: use the earliest datetime
‘latest’: use the latest datetime
‘null’: set to null
</ParamField>

</details>

<details>
<summary>

**`to_integer(*, base: int | td_typing.IntoExprColumn = 10, strict: bool = True)`**

</summary>

Covert a string to integer.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a")
      .str.to_integer(strict=False).alias("to_integer"))
>>>
┌──────┬────────────┐
│ a    ┆ to_integer │
│ ---  ┆ ---        │
│ str  ┆ i64        │
╞══════╪════════════╡
│ 1    ┆ 1          │
│ 2.2  ┆ null       │
│ a    ┆ null       │
│ null ┆ null       │
└──────┴────────────┘
```

<ParamField path="base">
  The base of the integer.
</ParamField>

<ParamField path="strict">
  If true, raise an error if the string is not a valid integer.
</ParamField>

</details>

<details>
<summary>

**`to_lowercase`**

</summary>

Return the lowercase of a string.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.to_lowercase().alias("to_lowercase"))
>>>
┌──────┬───────────────┐
│ a    ┆ to_lowerrcase │
│ ---  ┆ ---           │
│ str  ┆ u32           │
╞══════╪═══════════════╡
│ aB   ┆ ab            │
│ null ┆ null          │
└──────┴───────────────┘
```

</details>

<details>
<summary>

**`to_time(fmt: str | None = None, *, strict: bool = True, cache: bool = True)`**

</summary>

Convert the string to a time.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.to_time().alias("to_time"))
>>>
┌─────────────────────┬─────────────────────┐
│ a                   ┆ to_datetime         │
│ ---                 ┆ ---                 │
│ str                 ┆ datetime[μs]        │
╞═════════════════════╪═════════════════════╡
│ 2024-12-13 08:45:34 ┆ 2024-12-13 08:45:34 │
│ 2024-12-15 18:33:00 ┆ 2024-12-15 18:33:00 │
│ null                ┆ null                │
└─────────────────────┴─────────────────────┘
```

<ParamField path="fmt">
  The time format string (default %H:%M:%S).
See formats.
</ParamField>

<ParamField path="strict">
  Whether to parse the date strictly.
</ParamField>

<ParamField path="cache">
  Whether to cache the date.
</ParamField>

</details>

<details>
<summary>

**`to_titlecase`**

</summary>

Uppercase the first character and lowercase all the others ones of a string.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.to_titlecase().alias("titlecase"))
>>>
┌──────┬───────────┐
│ a    ┆ titlecase │
│ ---  ┆ ---       │
│ str  ┆ str       │
╞══════╪═══════════╡
│ ab   ┆ Ab        │
│ Ab   ┆ Ab        │
│ AB   ┆ Ab        │
│ aB   ┆ Ab        │
│ null ┆ null      │
└──────┴───────────┘
```

</details>

<details>
<summary>

**`to_uppercase`**

</summary>

Return the uppercase of a string.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.to_uppercase().alias("to_uppercase"))
>>>
┌──────┬──────────────┐
│ a    ┆ to_uppercase │
│ ---  ┆ ---          │
│ str  ┆ u32          │
╞══════╪══════════════╡
│ aB   ┆ AB           │
│ null ┆ null         │
└──────┴──────────────┘
```

</details>

<details>
<summary>

**`zfill(length: int | td_typing.IntoExprColumn)`**

</summary>

Pad numeric string values at the start to the given length using zeros.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.zfill(2).alias("zfill"))
>>>
┌──────┬───────┐
│ a    ┆ zfill │
│ ---  ┆ ---   │
│ str  ┆ str   │
╞══════╪═══════╡
│ 0    ┆ 00    │
│ 1    ┆ 01    │
│ 1000 ┆ 1000  │
│ null ┆ null  │
└──────┴───────┘
```

</details>

</details>

 <details>
<summary>

**`contains(pattern: str | Expr, *, literal: bool = False, strict: bool = True)`**

</summary>

Evaluate if the string contains a pattern.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.contains("ab").alias("contains"))
>>>
┌──────┬──────────┐
│ a    ┆ contains │
│ ---  ┆ ---      │
│ str  ┆ bool     │
╞══════╪══════════╡
│ a    ┆ false    │
│ ab   ┆ true     │
│ b    ┆ false    │
│ xaby ┆ true     │
│ null ┆ null     │
└──────┴──────────┘
```

<ParamField path="pattern">
  The pattern to search for.
</ParamField>

<ParamField path="literal">
  Take the pattern as a literal string (not a regex).
</ParamField>

<ParamField path="strict">
  if the given pattern is not valid regex, raise an error.
</ParamField>

</details>

 <details>
<summary>

**`contains_any(patterns: td_typing.IntoExpr, *, ascii_case_insensitive: bool = False)`**

</summary>

Evaluate if the string contains any of the given patterns.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a")
      .str.contains_any(["a", "b"]).alias("contains_any"))
>>>
┌──────┬──────────────┐
│ a    ┆ contains_any │
│ ---  ┆ ---          │
│ str  ┆ bool         │
╞══════╪══════════════╡
│ abc  ┆ true         │
│ axy  ┆ true         │
│ xyb  ┆ true         │
│ xyz  ┆ false        │
│ null ┆ null         │
└──────┴──────────────┘
```

<ParamField path="patterns">
  The patterns to search for.
</ParamField>

<ParamField path="ascii_case_insensitive">
  If true, the search is case-insensitive.
</ParamField>

</details>

 <details>
<summary>

**`count_matches(pattern: str | Expr, *, literal: bool = False)`**

</summary>

Counts the ocurrrences of the given pattern in the string.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a")
      .str.count_matches("b.").alias("count_matches"))
>>>
┌───────────┬───────────────┐
│ a         ┆ count_matches │
│ ---       ┆ ---           │
│ str       ┆ u32           │
╞═══════════╪═══════════════╡
│ a bAb c d ┆ 2             │
│ bCbb c d  ┆ 2             │
│ bb        ┆ 1             │
│ b         ┆ 0             │
│ a         ┆ 0             │
│ null      ┆ null          │
└───────────┴───────────────┘
```

<ParamField path="pattern">
  The pattern to extract.
</ParamField>

<ParamField path="literal">
  Take the pattern as a literal string (not a regex).
</ParamField>

</details>

 <details>
<summary>

**`ends_with(suffix: str | Expr)`**

</summary>

Evaluate if the string ends with.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.ends_with("b").alias("ends_with"))
>>>
┌──────┬───────────┐
│ a    ┆ ends_with │
│ ---  ┆ ---       │
│ str  ┆ bool      │
╞══════╪═══════════╡
│ a    ┆ false     │
│ ab   ┆ true      │
│ b    ┆ true      │
│ xaby ┆ false     │
│ null ┆ null      │
└──────┴───────────┘
```

</details>

 <details>
<summary>

**`extract(pattern: td_typing.IntoExprColumn, group_index: int = 1)`**

</summary>

Extract a pattern from the string.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.extract("(b.b)", 1).alias("extract"))
>>>
┌───────────┬─────────┐
│ a         ┆ extract │
│ ---       ┆ ---     │
│ str       ┆ str     │
╞═══════════╪═════════╡
│ a bAb c d ┆ bAb     │
│ bCbb c d  ┆ bCb     │
│ bb        ┆ null    │
│ null      ┆ null    │
└───────────┴─────────┘
```

<ParamField path="pattern">
  The pattern to extract.
</ParamField>

<ParamField path="group_index">
  The group index to extract.
</ParamField>

</details>

 <details>
<summary>

**`find(pattern: str | Expr, *, literal: bool = False, strict: bool = True)`**

</summary>

Find the position of the first occurrence of the given pattern.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.find("b").alias("find"))
>>>
┌──────┬──────┐
│ a    ┆ find │
│ ---  ┆ ---  │
│ str  ┆ u32  │
╞══════╪══════╡
│ a    ┆ null │
│ ab   ┆ 1    │
│ b    ┆ 0    │
│ xaby ┆ 2    │
│ null ┆ null │
└──────┴──────┘
```

<ParamField path="pattern">
  The pattern to search for.
</ParamField>

<ParamField path="literal">
  Take the pattern as a literal string (not a regex).
</ParamField>

<ParamField path="strict">
  if the given pattern is not valid regex, raise an error.
</ParamField>

</details>

 <details>
<summary>

**`grok(pattern: str, schema: dict[str, Column])`**

</summary>

Parse log text into structured fields using a Grok pattern.

Applies the given Grok pattern to the values in the current string expression.
Each **named capture group** in the pattern becomes a new output column.
Rows that do not match the pattern will return null for the extracted fields.

Example:

```text
>>> import tabsdata as td
>>> tf = td.TableFrame({"logs": [
...     "alice-login-2023",
...     "bob-logout-2024",
... ]})
>>>
>>> log_pattern = r"%{WORD:user}-%{WORD:action}-%{INT:year}"
>>> log_schema = {
>>>     "word": td_col.Column("user", td.String),
>>>     "action": td_col.Column("action", td.String),
>>>     "year": td_col.Column("year", td.Int8),
>>> }
>>> out = tf.grok("logs", log_pattern, log_schema)
>>> tf.select(
...     td.col("logs"),
...     td.col("logs").str.grok(log_pattern, log_schema)
... )
>>>
┌──────────────────┬───────┬────────┬──────┐
│ logs             ┆ user  ┆ action ┆ year │
│ ---              ┆ ---   ┆ ---    ┆ ---  │
│ str              ┆ str   ┆ str    ┆ i64  │
╞══════════════════╪═══════╪════════╪══════╡
│ alice-login-2023 ┆ alice ┆ login  ┆ 2023 │
│ bob-logout-2024  ┆ bob   ┆ logout ┆ 2024 │
└──────────────────┴───────┴────────┴──────┘
```

Notes

- The function automatically expands the Grok captures into separate
columns.
- Non-matching rows will show null for the extracted columns.
- If a pattern defines duplicate capture names, numeric suffixes like
field, field[1] will be used to disambiguate them.

<ParamField path="pattern" type="str">
  Grok pattern with named captures (e.g., %&#123;WORD:user&#125;).
</ParamField>

<ParamField path="schema" type="dict">
  A mapping where each capture name
is associated with its corresponding column definition, specifying
both the column name and its data type.
</ParamField>

</details>

 <details>
<summary>

**`head(n: int | td_typing.IntoExprColumn)`**

</summary>

Extract the start of the string up to the given length.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.head(2).alias("head"))
>>>
┌──────┬──────┐
│ a    ┆ head │
│ ---  ┆ ---  │
│ str  ┆ str  │
╞══════╪══════╡
│ abc  ┆ ab   │
│ a    ┆ a    │
│ null ┆ null │
└──────┴──────┘
```

</details>

 <details>
<summary>

**`len_bytes`**

</summary>

Return number of bytes (not chars) of a string.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.len_bytes().alias("len_bytes"))
>>>
┌──────┬────────────┐
│ a    ┆ to_decimal │
│ ---  ┆ ---        │
│ str  ┆ u32        │
╞══════╪════════════╡
│ ab   ┆ 2          │
│ 再   ┆ 3          │
│ null ┆ null       │
└──────┴────────────┘
```

</details>

 <details>
<summary>

**`len_chars`**

</summary>

Return number of chars (not bytes) of a string.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.len_chars().alias("len_chars"))
>>>
┌──────┬────────────┐
│ a    ┆ to_decimal │
│ ---  ┆ ---        │
│ str  ┆ u32        │
╞══════╪════════════╡
│ ab   ┆ 2          │
│ 再   ┆ 3          │
│ null ┆ null       │
└──────┴────────────┘
```

</details>

 <details>
<summary>

**`pad_end(length: int, fill_char: str = ' ')`**

</summary>

Pad string values at the end to the given length using the given
fill character.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.pad_end(6, "-").alias("pad_end"))
>>>
┌────────┬─────────┐
│ a      ┆ pad_end │
│ ---    ┆ ---     │
│ str    ┆ str     │
╞════════╪═════════╡
│ abc    ┆ abc---  │
│    def ┆    def  │
│ null   ┆ null    │
└────────┴─────────┘
```

<ParamField path="length">
  The length to end pad the string to.
</ParamField>

<ParamField path="fill_char">
  The character to use for padding.
</ParamField>

</details>

 <details>
<summary>

**`pad_start(length: int, fill_char: str = ' ')`**

</summary>

Pad string values at the front to the given length using the given
fill character.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.pad_start(6, "-").alias("pad_start"))
>>>
┌────────┬───────────┐
│ a      ┆ pad_start │
│ ---    ┆ ---       │
│ str    ┆ str       │
╞════════╪═══════════╡
│ abc    ┆ ---abc    │
│    def ┆    def    │
│ null   ┆ null      │
└────────┴───────────┘
```

<ParamField path="length">
  The length to front pad the string to.
</ParamField>

<ParamField path="fill_char">
  The character to use for padding.
</ParamField>

</details>

 <details>
<summary>

**`replace(pattern: str | Expr, value: str | Expr, *, literal: bool = False, n: int = 1)`**

</summary>

Replace the first occurence of a pattern with the given string.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.replace("b", "X").alias("replace"))
>>>
┌───────────┬───────────┐
│ a         ┆ replace   │
│ ---       ┆ ---       │
│ str       ┆ str       │
╞═══════════╪═══════════╡
│ a bAb c d ┆ a XAb c d │
│ bCbb c d  ┆ XCbb c d  │
│ bb        ┆ Xb        │
│ b         ┆ X         │
│ a         ┆ a         │
│ null      ┆ null      │
└───────────┴───────────┘
```

<ParamField path="pattern">
  The pattern to replace.
</ParamField>

<ParamField path="value">
  The value to replace the pattern with.
</ParamField>

<ParamField path="literal">
  Take the pattern as a literal string (not a regex).
</ParamField>

<ParamField path="n">
  Number of matches to replace.
</ParamField>

</details>

 <details>
<summary>

**`replace_all(pattern: str | Expr, value: str | Expr, *, literal: bool = False)`**

</summary>

Replace the all occurences of a pattern with the given string.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.replace("b", "X").alias("replace"))
>>>
┌───────────┬─────────────┐
│ a         ┆ replace_all │
│ ---       ┆ ---         │
│ str       ┆ str         │
╞═══════════╪═════════════╡
│ a bAb c d ┆ a XAX c d   │
│ bCbb c d  ┆ XCXX c d    │
│ bb        ┆ XX          │
│ b         ┆ X           │
│ a         ┆ a           │
│ null      ┆ null        │
└───────────┴─────────────┘
```

<ParamField path="pattern">
  The pattern to replace.
</ParamField>

<ParamField path="value">
  The value to replace the pattern with.
</ParamField>

<ParamField path="literal">
  Take the pattern as a literal string (not a regex).
</ParamField>

</details>

 <details>
<summary>

**`replace_many(patterns: td_typing.IntoExpr | Mapping[str, str], replace_with: td_typing.IntoExpr | NoDefault = <no_default>, *, ascii_case_insensitive: bool = False)`**

</summary>

Replace the all occurences of any the given patterns with the given string.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a")
      .str.replace_many(["a", "b"], "X").alias("replace_many"))
>>>
┌──────┬──────────────┐
│ a    ┆ replace_many │
│ ---  ┆ ---          │
│ str  ┆ str          │
╞══════╪══════════════╡
│ abc  ┆ XXc          │
│ axy  ┆ Xxy          │
│ xyb  ┆ xyX          │
│ xyz  ┆ xyz          │
│ null ┆ null         │
└──────┴──────────────┘
```

<ParamField path="patterns">
  The patterns to replace.
</ParamField>

<ParamField path="replace_with">
  The value to replace the pattern with.
</ParamField>

<ParamField path="ascii_case_insensitive">
  If true, the search is case-insensitive.
</ParamField>

</details>

 <details>
<summary>

**`reverse`**

</summary>

Reverse the string.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.reverse().alias("reverse"))
>>>
┌──────┬─────────┐
│ a    ┆ reverse │
│ ---  ┆ ---     │
│ str  ┆ str     │
╞══════╪═════════╡
│ abc  ┆ cba     │
│ a    ┆ a       │
│ null ┆ null    │
└──────┴─────────┘
```

</details>

 <details>
<summary>

**`slice(offset: int | td_typing.IntoExprColumn, length: int | td_typing.IntoExprColumn | None = None)`**

</summary>

Extract the substring at the given offset for the given length.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.slice(1,1).alias("slice"))
>>>
┌──────┬───────┐
│ a    ┆ slice │
│ ---  ┆ ---   │
│ str  ┆ str   │
╞══════╪═══════╡
│ abc  ┆ b     │
│ a    ┆       │
│ null ┆ null  │
└──────┴───────┘
```

<ParamField path="offset">
  The offset to start the slice.
</ParamField>

<ParamField path="length">
  The length of the slice. If None, slice until the end of the string.
</ParamField>

</details>

 <details>
<summary>

**`starts_with(prefix: str | Expr)`**

</summary>

Evaluate if the string start with.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a")
      .str.starts_with("a").alias("starts_with"))
>>>
┌──────┬────────────┐
│ a    ┆ start_with │
│ ---  ┆ ---        │
│ str  ┆ bool       │
╞══════╪════════════╡
│ a    ┆ true       │
│ ab   ┆ true       │
│ b    ┆ false      │
│ xaby ┆ false      │
│ null ┆ null       │
└──────┴────────────┘
```

</details>

 <details>
<summary>

**`strip_chars(characters: td_typing.IntoExpr = None)`**

</summary>

Trim string values.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a")
      .str.strip_chars("a ").alias("strip_chars"))
>>>
┌─────────────────────────────────┬─────────────┐
│ a                               ┆ strip_chars │
│ ---                             ┆ ---         │
│ str                             ┆ str         │
╞═════════════════════════════════╪═════════════╡
│ acba cda                      … ┆ cba cd      │
│    xy z                         ┆ xy z        │
│ null                            ┆ null        │
└─────────────────────────────────┴─────────────┘
```

</details>

 <details>
<summary>

**`strip_chars_end(characters: td_typing.IntoExpr = None)`**

</summary>

Trim string values from the end of the string.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a")
      .str.strip_chars_end("dc ").alias("strip_chars_end"))
>>>
┌───────────────────────────────┬─────────────────┐
│ a                             ┆ strip_chars_end │
│ ---                           ┆ ---             │
│ str                           ┆ str             │
╞═══════════════════════════════╪═════════════════╡
│ cba cd                        ┆ cba             │
│    xy z                       ┆    xy z         │
│ null                          ┆ null            │
└───────────────────────────────┴─────────────────┘
```

</details>

 <details>
<summary>

**`strip_chars_start(characters: td_typing.IntoExpr = None)`**

</summary>

Trim string values from the start of the string.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a")
      .str.strip_chars_start("abc").alias("strip_chars_start"))
>>>
┌───────────────────────────────┬────────────────────────────┐
│ a                             ┆ strip_chars_start          │
│ ---                           ┆ ---                        │
│ str                           ┆ str                        │
╞═══════════════════════════════╪════════════════════════════╡
│ cba cd                        ┆  cd                        │
│    xy z                       ┆    xy z                    │
│ null                          ┆ null                       │
└───────────────────────────────┴────────────────────────────┘
```

</details>

 <details>
<summary>

**`strip_prefix(prefix: td_typing.IntoExpr)`**

</summary>

Trim string values removing the given prefix

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a")
      .str.strip_prefix("cb").alias("strip_prefix"))
>>>
┌───────────────────────────────┬─────────────────┐
│ a                             ┆ strip_prefix    │
│ ---                           ┆ ---             │
│ str                           ┆ str             │
╞═══════════════════════════════╪═════════════════╡
│ cba cd                        ┆ a cd            │
│ bx                            ┆ bx              │
│ null                          ┆ null            │
└───────────────────────────────┴─────────────────┘
```

</details>

 <details>
<summary>

**`strip_suffix(suffix: td_typing.IntoExpr)`**

</summary>

Trim string values removing the given suffix

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a")
      .str.strip_suffix("cd").alias("strip_suffix"))
>>>
┌───────────────────────────────┬─────────────────┐
│ a                             ┆ strip_suffix    │
│ ---                           ┆ ---             │
│ str                           ┆ str             │
╞═══════════════════════════════╪═════════════════╡
│ cba cd                        ┆ cba             │
│ bx                            ┆ bx              │
│ null                          ┆ null            │
└───────────────────────────────┴─────────────────┘
```

</details>

 <details>
<summary>

**`tail(n: int | td_typing.IntoExprColumn)`**

</summary>

Extract the end of the string up to the given length.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.tail(2).alias("tail"))
>>>
┌──────┬──────┐
│ a    ┆ tail │
│ ---  ┆ ---  │
│ str  ┆ str  │
╞══════╪══════╡
│ abc  ┆ bc   │
│ a    ┆ a    │
│ null ┆ null │
└──────┴──────┘
```

</details>

 <details>
<summary>

**`to_date(fmt: str | None = None, *, strict: bool = True)`**

</summary>

Convert the string to a date.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.to_date().alias("to_date"))
>>>
┌────────────┬────────────┐
│ a          ┆ to_date    │
│ ---        ┆ ---        │
│ str        ┆ date       │
╞════════════╪════════════╡
│ 2024-12-13 ┆ 2024-12-13 │
│ 2024-12-15 ┆ 2024-12-15 │
│ null       ┆ null       │
└────────────┴────────────┘
```

<ParamField path="fmt">
  The date format string (default %Y-%m-%d).
See formats.
</ParamField>

<ParamField path="strict">
  Whether to parse the date strictly.
</ParamField>

</details>

 <details>
<summary>

**`to_datetime(format: str | None = None, *, time_unit: Literal['ns', 'us', 'ms'] | None = None, time_zone: str | None = None, strict: bool = True, ambiguous: Literal['earliest', 'latest', 'raise', 'null'] | Expr = 'raise')`**

</summary>

Convert the string to a datetime.

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.to_datetime().alias("to_datetime"))
>>>
┌─────────────────────┬─────────────────────┐
│ a                   ┆ to_datetime         │
│ ---                 ┆ ---                 │
│ str                 ┆ datetime[μs]        │
╞═════════════════════╪═════════════════════╡
│ 2024-12-13 08:45:34 ┆ 2024-12-13 08:45:34 │
│ 2024-12-15 18:33:00 ┆ 2024-12-15 18:33:00 │
│ null                ┆ null                │
└─────────────────────┴─────────────────────┘
```

<ParamField path="format">
  The datetime format string (default %Y-%m-%d %H:%M:%S).
See formats.
</ParamField>

<ParamField path="time_unit">
  &#123;None, ‘us’, ‘ns’, ‘ms’&#125;
If None (default), it inferred from the format string
</ParamField>

<ParamField path="time_zone">
  Time zone for the resulting value.
</ParamField>

<ParamField path="strict">
  If the conversion fails an error will be raised.
</ParamField>

<ParamField path="ambiguous">
  Policy to apply on ambiguos Datetimes:
‘raise’: saises an error
‘earliest’: use the earliest datetime
‘latest’: use the latest datetime
‘null’: set to null
</ParamField>

</details>

 <details>
<summary>

**`to_integer(*, base: int | td_typing.IntoExprColumn = 10, strict: bool = True)`**

</summary>

Covert a string to integer.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a")
      .str.to_integer(strict=False).alias("to_integer"))
>>>
┌──────┬────────────┐
│ a    ┆ to_integer │
│ ---  ┆ ---        │
│ str  ┆ i64        │
╞══════╪════════════╡
│ 1    ┆ 1          │
│ 2.2  ┆ null       │
│ a    ┆ null       │
│ null ┆ null       │
└──────┴────────────┘
```

<ParamField path="base">
  The base of the integer.
</ParamField>

<ParamField path="strict">
  If true, raise an error if the string is not a valid integer.
</ParamField>

</details>

 <details>
<summary>

**`to_lowercase`**

</summary>

Return the lowercase of a string.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.to_lowercase().alias("to_lowercase"))
>>>
┌──────┬───────────────┐
│ a    ┆ to_lowerrcase │
│ ---  ┆ ---           │
│ str  ┆ u32           │
╞══════╪═══════════════╡
│ aB   ┆ ab            │
│ null ┆ null          │
└──────┴───────────────┘
```

</details>

 <details>
<summary>

**`to_time(fmt: str | None = None, *, strict: bool = True, cache: bool = True)`**

</summary>

Convert the string to a time.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.to_time().alias("to_time"))
>>>
┌─────────────────────┬─────────────────────┐
│ a                   ┆ to_datetime         │
│ ---                 ┆ ---                 │
│ str                 ┆ datetime[μs]        │
╞═════════════════════╪═════════════════════╡
│ 2024-12-13 08:45:34 ┆ 2024-12-13 08:45:34 │
│ 2024-12-15 18:33:00 ┆ 2024-12-15 18:33:00 │
│ null                ┆ null                │
└─────────────────────┴─────────────────────┘
```

<ParamField path="fmt">
  The time format string (default %H:%M:%S).
See formats.
</ParamField>

<ParamField path="strict">
  Whether to parse the date strictly.
</ParamField>

<ParamField path="cache">
  Whether to cache the date.
</ParamField>

</details>

 <details>
<summary>

**`to_titlecase`**

</summary>

Uppercase the first character and lowercase all the others ones of a string.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.to_titlecase().alias("titlecase"))
>>>
┌──────┬───────────┐
│ a    ┆ titlecase │
│ ---  ┆ ---       │
│ str  ┆ str       │
╞══════╪═══════════╡
│ ab   ┆ Ab        │
│ Ab   ┆ Ab        │
│ AB   ┆ Ab        │
│ aB   ┆ Ab        │
│ null ┆ null      │
└──────┴───────────┘
```

</details>

 <details>
<summary>

**`to_uppercase`**

</summary>

Return the uppercase of a string.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.to_uppercase().alias("to_uppercase"))
>>>
┌──────┬──────────────┐
│ a    ┆ to_uppercase │
│ ---  ┆ ---          │
│ str  ┆ u32          │
╞══════╪══════════════╡
│ aB   ┆ AB           │
│ null ┆ null         │
└──────┴──────────────┘
```

</details>

 <details>
<summary>

**`zfill(length: int | td_typing.IntoExprColumn)`**

</summary>

Pad numeric string values at the start to the given length using zeros.

Example:

```text
>>> import tabsdata as td
>>>
>>> tf: td.TableFrame ...
>>>
>>> tf.select(td.col("a"), td.col("a").str.zfill(2).alias("zfill"))
>>>
┌──────┬───────┐
│ a    ┆ zfill │
│ ---  ┆ ---   │
│ str  ┆ str   │
╞══════╪═══════╡
│ 0    ┆ 00    │
│ 1    ┆ 01    │
│ 1000 ┆ 1000  │
│ null ┆ null  │
└──────┴───────┘
```

</details>

 <details>
<summary>

**`GrokParser(pattern: 'str', schema: 'dict[str, td_col.Column]')`**

</summary>

Bases: [object](https://docs.python.org/3/library/functions.html#object)

<details>
<summary>`index`</summary>

</details>

<details>
<summary>`pattern`</summary>

</details>

<details>
<summary>

**`rust(expression: IntoExpr)`**

</summary>

</details>

<details>
<summary>`schema`</summary>

</details>

<details>
<summary>`temp_column`</summary>

</details>

</details>

 <details>
<summary>

**`rust(expression: IntoExpr)`**

</summary>

</details>
